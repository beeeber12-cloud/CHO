# 📖 성경묵상 공유앱(CHO) — 작업 기록 & 인수인계

> 이 문서 하나로 창을 껐다 켜거나 다른 세션에서도 이어서 작업할 수 있습니다.
> **최종 업데이트: 2026-08-11**
> 이전 문서: `작업기록_HANDOFF_260807.md` (함정 5가지·구조 설명은 반드시 그쪽을 먼저 볼 것)

---

## 0. ⚡ 이번 세션 3줄 요약

1. **반응 버튼 색을 은은한 연녹색으로 통일** (노란색이 글 사이에서 너무 튀었다)
2. **알림을 누르면 그 글이 있는 탭으로 바로 들어간다** (전에는 항상 첫 화면)
3. **알림설정의 시간 알림이 실제로 동작한다** — 예전엔 설정만 저장되고 아무 일도 없었다

---

## 1. 핵심 주소·ID (변동 없음)

| 항목 | 값 |
|---|---|
| 로컬 폴더 | `C:\Users\beeeb\Downloads\CHO` |
| GitHub | https://github.com/beeeber12-cloud/CHO |
| 라이브 (Cloud Run) | https://cho-1006806795339.asia-northeast3.run.app |
| Firestore 문서 | `app_data/main_db` (**DB 전체가 문서 1개**) |
| 배포 방식 | `main` 브랜치 push → Cloud Run 자동 배포 (약 2~3분) |

---

## 2. 이번 세션 작업 내용

### ① 반응 색상 통일 (은은하게)
`src/components/ReactionBar.tsx`
- 누른 상태: `bg-[#FFBA00]`(진한 노랑) → **`bg-[#E8F0E9]` + `ring-1 ring-[#C3D6C6]`**
- 👍 좋아요 / 🙏 기도할게요 **둘 다 같은 색**을 쓴다
- `MeditationFeed.tsx` 의 "N명이 당신을 위해 기도했습니다" 상자도 같은 색으로 맞춤

> 이름 배지(`테스트지체` 같은 노란 라벨)는 손대지 않았다. 사람 구분용이라 일부러 남김.

### ② 알림 누르면 해당 화면으로 바로 이동
서버가 보내는 알림에 **주소를 같이 실어 보낸다.**

| 알림 | 주소 |
|---|---|
| 오늘의 말씀 공지 | `/?tab=notice` |
| 새 묵상 / 묵상 댓글 / 묵상 반응 | `/?tab=feed` |
| 감사 나눔 / 감사 댓글 / 감사 반응 | `/?tab=gratitude` |
| 시간 알림(아침 묵상) | `/?tab=notice` |
| 테스트 알림 | `/?tab=settings` |

동작 방식 (`public/sw.js` 의 `notificationclick`):
1. 이미 앱이 열려 있으면 → **서비스워커가 화면에 "이 탭 열어줘" 메시지**를 보낸다.
   `MessageChannel` 로 0.5초 안에 답이 오면 새로고침 없이 탭만 바뀐다 (빠름).
2. 답이 없으면(옛 버전이 열려 있는 경우) → `client.navigate()` 로 주소를 바꿔 다시 띄운다.
3. 앱이 꺼져 있으면 → `openWindow(주소)`.

`src/App.tsx`
- 시작할 때 `?tab=` 을 읽어 그 탭으로 연다 (`tabFromUrl()`)
- 읽은 뒤 `history.replaceState` 로 주소를 지운다 — **안 지우면 새로고침마다 그 탭으로 되돌아간다**
- 서비스워커 메시지를 받으면 탭을 바꾸고 `e.ports[0]` 로 **답장**한다.
  이 답장이 있어야 서비스워커가 새로고침을 생략한다.

### ③ 시간 알림이 실제로 울린다 (가장 큰 변화)
전에는 알림설정의 시간·요일이 **DB에 저장만 되고 아무것도 하지 않았다.**

`server.ts` 에 추가:
- `getKSTClock()` — 한국 기준 요일(0=일)과 자정부터 지난 분
- `runAlarmSweep()` — 울릴 차례인 사람을 찾아 그 사람에게만 푸시
- 서버 기동 시 1회 + **`setInterval` 1분마다** 실행
- `POST/GET /api/alarms/tick` — 밖에서 깨우는 통로 (아래 §3 참고)

안전장치:
- `AlarmConfig.lastFiredDate` 에 마지막 발송 날짜(한국 날짜)를 남긴다.
  **서버가 다시 켜져도 같은 날 두 번 울리지 않는다.**
- 예정 시각 **이후 30분 안**에만 보낸다 (`ALARM_GRACE_MINUTES`).
  컨테이너가 자다 깨어난 경우를 위한 여유이자, 엉뚱한 시각에 울리지 않게 하는 상한.
- 시간을 바꿔 저장하면 `lastFiredDate` 를 지운다 → 오늘 저녁으로 옮겨서 바로 확인 가능.
- **알림을 켜 둔 사람(푸시 구독이 있는 사람)만** 대상이다.

> ⚠️ 라우트 순서 주의: `/api/alarms/tick` 은 반드시 `/api/alarms/:userId` **앞에** 등록해야 한다.
> 뒤에 두면 `tick` 이 사용자 ID 로 잡혀서 엉뚱한 응답이 나간다.

**저장한 적 없는 사람은?**
알림설정 화면은 저장 전에도 "07:30 · 월~금 · 켜짐"으로 보인다.
그래서 저장 이력이 없어도 그 기본값(`DEFAULT_ALARM`)대로 울린다 — 화면과 실제가 어긋나지 않게.
실제로 울린 순간에만 설정이 목록에 들어간다 (쓸데없는 설정이 쌓이지 않게).

### ④ 감사칭찬 글 알림 — 이미 있던 기능
`POST /api/gratitudes/create` 는 **`8337ee6` 시점부터 전 교인에게 푸시를 보내고 있었다.**
감사 댓글·감사 반응 알림도 이미 있다. 이번엔 주소(`?tab=gratitude`)만 붙였다.
안 왔다면 코드 문제가 아니라 **각자 휴대폰에서 '새 글 알림'을 안 켰을 가능성**이 크다.

---

## 3. 🚨 아직 남은 문제 — Cloud Run 이 잠들면 알림이 안 간다

Cloud Run 은 **아무도 앱을 안 쓰면 컨테이너를 잠재운다.** 잠들면 서버 안의 1분 타이머도 멈춘다.
새벽 7시 30분에 아무도 앱을 안 켜고 있으면 **알림이 안 나갈 수 있다.**

해결 방법 두 가지 (둘 중 하나만 하면 됨):

**(A) Cloud Scheduler — 사실상 무료, 권장**
GCP 콘솔 → Cloud Scheduler → 작업 만들기
- 빈도: `*/5 * * * *` (5분마다) — 알림 시간대만 원하면 `*/5 5-11 * * *`
- 시간대: `Asia/Seoul`
- 대상: HTTP / `https://cho-1006806795339.asia-northeast3.run.app/api/alarms/tick` / **GET**

> ⚠️ POST 로 하려면 본문이 비어 있어도 `Content-Length: 0` 이 붙어야 한다.
> 구글 프론트엔드가 **본문 없는 POST 를 411 로 되돌려보낸다** (실제로 확인함).
> GET 은 그런 제약이 없으니 GET 으로 거는 편이 안전하다.

Cloud Scheduler 는 작업 3개까지 무료, 호출량도 Cloud Run 무료 한도 안이다.
`tick` 은 하루 한 번만 나가도록 막혀 있어 여러 번 불려도 중복 발송되지 않는다.

**(B) 최소 인스턴스 1개 고정** — 월 7~8달러쯤 든다. 정확하지만 비용이 붙는다.

---

## 4. 사용자에게 알려야 할 것

- **송지범 님의 알림 시간이 `00:00`(자정)으로 저장돼 있다.** 이대로 두면 평일 자정에 울린다.
- 최희란 10:00, 김민석 10:30, 조은미 06:00(토요일 포함), 나머지는 07:30 월~금.
- `GEMINI_API_KEY` 미설정 → 성경Q&A 탭 숨김(`SHOW_QNA_TAB = false`) — 이전과 동일.
- 스토어 출시 전 저작권 확인(개역개정=대한성서공회 유료, 우리말성경=두란노) — 이전과 동일.

---

## 5. 검증 방법 (이번에 실제로 한 것)

프로덕션 Firestore 를 건드리지 않으려고 **격리 사본**에서 확인했다.
`firebase-applet-config.json` 이 없으면 Firestore 연결이 아예 꺼지므로,
그 파일과 `db.json` 을 뺀 사본을 임시 폴더에 만들고 가짜 회원·가짜 구독으로 돌렸다.

확인한 것:
| 상황 | 결과 |
|---|---|
| 예정 시각이 막 지난 알림 | 1건 발송 ✅ |
| 같은 알림 두 번째 tick | 0건 (중복 없음) ✅ |
| 오늘이 아닌 요일 | 0건 ✅ |
| 아직 안 된 시각 / 1시간 넘게 지난 시각 | 0건 ✅ |
| 알림 꺼짐 | 0건 ✅ |
| 시간 변경 후 | `lastFiredDate` 지워짐 ✅ |
| `GET·POST /api/alarms/tick` | 사용자 ID 로 안 잡힘 ✅ |
| `/?tab=gratitude` 로 열기 | 감사칭찬 탭 열리고 주소는 `/` 로 정리됨 ✅ |
| 서비스워커 메시지로 탭 전환 | 새로고침 없이 전환, 엉뚱한 값은 무시 ✅ |
| 기도할게요 누른 색 | `rgb(232,240,233)` + 테두리 `rgb(195,214,198)` ✅ |

**⚠️ 프로덕션에서는 절대 테스트하지 말 것.**
`/api/gratitudes/create`, `/api/meditations/create`, 리액션, 그리고 이제 **시간 알림까지**
전 교인 휴대폰으로 알림이 나간다.

---

## 5-2. 추가 작업 (2026-08-12)

### ⑤ 구절명에 장이 빠지던 문제
고른 절로 묵상을 쓰면 `마가복음 3,5절` 처럼 **장이 사라졌다.**
어느 장인지 모를 뿐 아니라 다시 읽을 때 **3장으로 잘못 해석**된다.
→ `src/lib/verseRef.ts` 신규. 이제 `마가복음 1장 3,5절`.
`요한복음 3:16` 같은 형태도 `요한복음 3장` 으로 정규화한다. 시편은 `편` 유지.

### ⑥ 오늘 말씀 공지가 제대로 안 넘어가던 문제 🔥
실제 이력: 요한복음 2장이 **3일 연속**(08-07·08-07·08-08), 08-13 공지가 **08-11과 같은 2장**.

**원인 세 가지**
1. 동기화가 통독 진도를 **원격 값으로 무조건 덮어썼다** → 방금 넘긴 진도가 되돌아감
2. 공지 생성에 잠금이 없어 **같은 날 공지가 두 개** 생김 (동시 요청)
3. **내일 공지를 미리 만드는 구조** → 되돌아간 진도로 만든 앞날 공지가 그대로 남음

**수정**
- `biblePlan.updatedAt` 추가 + `pickNewerPlan()` — 합칠 때 더 최근 것을 남긴다
- `positionForNotice()` — 진도가 이미 지나온 자리를 가리키면 **공지 이력 기준으로 자가 교정**.
  단, 관리자가 방금 지정했으면(`lastUpdatedDate` 없음) 그대로 따른다
- `ensureTodayNotice()` — 하루 한 건 잠금 + **앞날 자동 공지 정리**
- `prepareTomorrowNotice` / `nextDayStr` 제거 — 앞날 공지를 더는 만들지 않는다
- 1분 타이머와 `/api/alarms/tick` 에서 오늘 공지를 확인 → **자정에 자동으로 다음 장**

⚠️ **관리자가 진도를 옮기면 `lastUpdatedDate` 를 지운다.** 이 값이 "이 자리에서 이미 공지했다"는
표시라서, 안 지우면 자가 교정이 관리자의 지정을 덮어쓴다.

**격리 사본 검증 5가지 전부 통과**
평소 진행 / 진도 되돌아감 자가교정 / 책 끝 → 다음 권 1장 / 관리자 지정 존중 / 통독 꺼짐.
두 번 두드려도 공지는 하나만 생긴다.

**정리한 프로덕션 데이터**
- `2026-08-13 마가복음 2장` 공지 삭제 (코드가 자동으로 걷어냄)
- 진도를 `마가복음 4장` 으로 지정 → 08-13 자정에 4장이 나간다

> 참고: **공지 삭제 API 가 없다.** 그래서 이번엔 코드가 스스로 걷어내게 했다.
> 앞으로 공지를 지워야 할 일이 생기면 관리자용 DELETE 라우트를 만들어야 한다.

## 6. 만진 파일

| 파일 | 내용 |
|---|---|
| `server.ts` | `getKSTClock`, `DEFAULT_ALARM`, `runAlarmSweep`, `/api/alarms/tick`, 푸시 주소에 `?tab=`,<br>`pickNewerPlan`, `positionForNotice`, `ensureTodayNotice`, `parseChapterTitle`, `lastAutoNoticePosition` |
| `src/lib/verseRef.ts` | **신규** — 장을 포함한 구절명 만들기 |
| `public/sw.js` | `notificationclick` 재작성 (`askToOpenTab`) |
| `src/App.tsx` | `tabFromUrl`, 서비스워커 메시지 수신 |
| `src/types.ts` | `AlarmConfig.lastFiredDate` |
| `src/components/ReactionBar.tsx` | 누른 상태 색 |
| `src/components/MeditationFeed.tsx` | 기도 인원 상자 색 |
| `src/components/NotificationSettings.tsx` | 시간 아래 안내 문구 |

---

## 7. 추가 작업 (2026-08-13)

### 🔥 알림이 거의 안 가던 진짜 원인 — Cloud Run 의 CPU 조이기
푸시를 `res.json()` **뒤에** 던져두고 기다리지 않았다.
Cloud Run 은 응답이 끝나면 그 인스턴스의 CPU 를 거의 0 으로 줄인다.
그래서 전송이 **시작만 되고 끝나지 않아** 알림이 조용히 사라졌다.

> 테스트 알림만 되던 이유가 이것이다 — 그것 하나만 `await` 하고 있었다.

**수정:** 새 공지 / 새 묵상 / 묵상 댓글·반응 / 감사 나눔·댓글·반응 **전부 응답 전에 await**.
한 건이 오래 걸려도 화면이 멈추지 않게 `PUSH_SEND_TIMEOUT_MS = 4000` 상한.

⚠️ **앞으로 절대 `res.json()` 뒤에 비동기 작업을 던져두지 말 것.** 안 끝난다.
(자정 공지·시간 알림을 1분 타이머 + Cloud Scheduler 로 돌리는 이유도 같다)

### 등록된 기기가 0대였다
`GET /api/push/status` 를 새로 만들어 확인해 보니 **전 교인 통틀어 구독 0건.**
코드를 고쳐도 켠 사람이 없으면 아무 알림도 안 간다.
→ 알림설정에 **지체별 알림 켜짐 현황**(관리자만) 추가. 누가 껐는지 눈으로 확인 가능.

### @이름 부르기
- 서버 `findMentionedUserIds` / `pushMentions` — 글·댓글에서 `@이름` 을 찾아 알림
- `관리자(목사님)` 처럼 괄호가 붙은 이름은 `@관리자` 로도 불린다
- 같은 글에서 다른 알림을 이미 받는 사람은 빼서 **두 번 울리지 않게** 함
- 화면: `MentionText` 로 강조, `MentionPicker`("지체 부르기")로 이름을 눌러 입력
  → 어르신께는 자동완성 드롭다운보다 이 방식이 쉽다

### 긴 묵상 접기
`CollapsibleText` — 윗부분만 보이고 '더 보기'로 편다.
높이를 **ResizeObserver 로 실제 측정**한다. 글씨 크기 설정을 바꿔도 맞게 동작하고,
짧은 글에는 버튼이 아예 안 나온다.

### 만진 파일 (추가)
| 파일 | 내용 |
|---|---|
| `server.ts` | `sendPushToUsers` 반환값·타임아웃·로그, `findMentionedUserIds`, `pushMentions`, `/api/push/status`, 모든 푸시 await |
| `src/components/CollapsibleText.tsx` | **신규** — 긴 글 접기 |
| `src/components/MentionText.tsx` | **신규** — `@이름` 강조 |
| `src/components/MentionPicker.tsx` | **신규** — 이름 눌러 넣기 |
| `src/lib/mentions.ts` | **신규** — 멘션 규칙 (서버와 동일하게 유지할 것) |
| `src/components/NotificationSettings.tsx` | 알림 켜짐 현황 카드 |

---

## 8. 0단계 보안 조치 (2026-08-19~20)

### 🚨 발견한 것
| 항목 | 상태 |
|---|---|
| GitHub 저장소 | **공개** — API 로 확인 (200) |
| `firebase-applet-config.json` | 그 저장소에 **커밋돼 있음** |
| `firestore.rules` | `allow read, write: if true` |
| 비밀번호 | 4자리 PIN **평문 저장**, 시도 제한 없음 |
| 권한 확인 | 몸통(body)의 `adminId`·`requestorId` 를 **그대로 신뢰** |

마지막 항목이 특히 나빴다. 회원 목록은 로그인 없이 볼 수 있으므로
**아무나 남의 계정을 지우거나 비밀번호를 바꿀 수 있었다.**

### ✅ 완료 (커밋 `b73b5d6`, 배포 확인)
- **비밀번호 암호화** — scrypt. Node 기본 기능이라 설치할 것 없음 (`server/auth.ts`)
  - 기동 시 남아 있는 평문을 일괄 변환
  - 등록·본인수정·관리자변경·로그인시 승급, **네 곳 모두** 해시
  - ⚠️ `admin-update-pin` 을 처음에 빠뜨렸다가 테스트로 잡았다. 비밀번호를 쓰는 곳은 반드시 전수 확인할 것
- **로그인 증표(토큰)** — HMAC 서명, 180일. `src/lib/session.ts` 의 `authFetch` 로 전송
  - 증표의 역할을 그대로 믿지 않고 **매번 DB 로 재확인**
  - 탈퇴자의 증표는 즉시 무효
- **시도 제한** — 10분 10회. 실서비스에서 11번째 차단 확인
- 보호한 라우트: `admin-update-pin`, 계정 삭제, 내 정보 수정, `/api/push/status`

**실서비스 확인:** 예전 구멍 401·403 / 로그인 화면 목록 200(정상) / 오늘말씀·묵상 34건 그대로

### ⏸ 남은 것 — Firestore 규칙 잠금
코드는 준비됐으나 **아직 커밋하지 않았다** (`server/firebaseDb.ts`, `firestore.rules`).

**이유: 프로젝트가 둘로 갈려 있다.**
| | |
|---|---|
| Firestore 가 있는 곳 | `elemental-diode-2w1xt` (Firebase) |
| Cloud Run 이 도는 곳 | `dulcet-medley-471901-m4` (GCP) |

지금은 브라우저용 SDK 로 접속해서 규칙만 열려 있으면 통했다.
관리자 SDK 로 바꾸면 **Cloud Run 서비스 계정에 Firestore 프로젝트 권한을 따로 줘야 한다.**
안 주고 배포하면 데이터를 못 읽어 앱이 빈 화면이 된다.
(`[SAFETY]` 가드가 있어 데이터가 지워지지는 않는다)

⚠️ **두 개의 함정**
1. `GOOGLE_CLOUD_PROJECT` 환경변수는 Cloud Run 프로젝트를 가리킨다. Firebase 프로젝트가 아니다.
   → `readSettings()` 는 **설정 파일을 먼저** 본다. 순서를 뒤집으면 엉뚱한 DB 를 본다
2. 규칙을 먼저 잠그고 SDK 를 나중에 바꾸면 **그 사이에 앱이 멈춘다**. 반드시 SDK 먼저

**순서:** ① 저장소 비공개 → ② IAM 권한 부여 → ③ 관리자 SDK 배포·확인 → ④ 규칙 잠금

---

## 9. 데이터를 목사님 소유 프로젝트로 이전 (2026-08-30)

### 왜 옮겼나
8단계에서 "Firestore 규칙 잠금"이 막혀 있었다. 원인을 파고 보니 권한 문제가 아니라
**소유권 문제**였다.

| | |
|---|---|
| `elemental-diode-2w1xt` (데이터) | 목사님 역할 = **로그 뷰어**. 소유자 줄 자체가 없음 |
| `dulcet-medley-471901-m4` (앱) | 목사님 = **소유자** |

데이터 프로젝트는 Google 이 자동 생성한 것이라 관리 권한이 넘어오지 않았다.
서비스 계정도 못 만들고("새 서비스 계정을 만들 권한이 없습니다"), IAM 부여 버튼도 비활성.
**즉 그 프로젝트의 규칙은 영원히 못 잠근다.** 그래서 옮기는 것 말고 답이 없었다.

### 얼마나 열려 있었나 (실측)
```
GET https://firestore.googleapis.com/v1/projects/elemental-diode-2w1xt/databases/<id>/documents/app_data/main_db
→ HTTP 200   (자격증명 없이, 아무나)
```
교인 18명의 묵상·기도제목·감사가 인터넷에 그대로 노출돼 있었다.

### 한 일
1. **백업 2부** — `C:\Users\beeeb\Downloads\CHO_백업\` (저장소 밖). 이전 전 / 배포 직전
2. **gcloud 설치 + 로그인** — 이 PC 에 Cloud SDK 582.0.0. `gcloud auth login` + `application-default login`
3. **Firestore 생성** — `dulcet-medley-471901-m4` / `(default)` / **asia-northeast3(서울)** / 삭제 잠금 켬
   - 위치는 나중에 못 바꾼다. Cloud Run 과 같은 서울로 맞췄다 (응답도 빨라짐)
4. **데이터 이전** — 17개 항목 전부 개수 대조 일치
5. **관리자 SDK 전환 + 배포** (`b4514d1` → 리비전 `cho-00084-qbr`)
   - `firebase-applet-config.json` 에서 apiKey 등 접속 정보 제거 (저장소가 아직 공개)
6. **Cloud Scheduler 등록** — `cho-alarm-tick`, `*/5 * * * *`, Asia/Seoul, GET `/api/alarms/tick`

### ⚠️ 규칙 잠금(8단계의 ④)은 **필요 없어졌다**
새 프로젝트에는 **Firebase 가 붙어 있지 않다.** 따라서 클라이언트 SDK 로 들어오는 길이
아예 없고, 접근은 IAM 으로만 통제된다. "규칙"이라는 무른 문이 없는 게 오히려 더 단단하다.
```
새 DB  → HTTP 403 (자격증명 없이)
옛 DB  → HTTP 200
```
`firestore.rules` 는 기록용으로 남겨 뒀다. 이 프로젝트에는 적용 대상이 없다.

### 이전 중 생긴 일 (정상)
배포 전환 시점이 자정과 겹쳐, 8/30 오늘말씀이 **양쪽에서 각각 자동 생성**됐다.
내용은 같고 id 만 다르다 (`notice-lbdmfulgg` / `notice-v6w6vv49c`). 데이터 손실은 없다.

### 함정 메모
- Cloud Run 서비스 계정은 자기 프로젝트에 이미 `roles/editor` 를 갖고 있다 → **권한 부여 불필요**
- Git Bash 에서 `gcloud.cmd` 는 **한 줄로** 불러야 한다. 줄바꿈(`\`)을 쓰면
  공백 들어간 설치 경로 때문에 깨진다. `cygpath -d` 로 짧은 경로를 쓰면 안전하다
- PowerShell 히어스트링(`@'...'@`)을 Bash 도구에 쓰면 `@` 가 그대로 들어간다 (커밋 제목 오염)

### 남은 것
- [ ] **옛 프로젝트 데이터 삭제** — 아직 HTTP 200 으로 열려 있다. 최우선
- [ ] **저장소 비공개 전환** — 목사님이 GitHub 설정에서
- [ ] 1MB 한 문서 구조를 낱장(서브컬렉션)으로 분리 — 현재 166KB/1024KB, 주당 25KB 증가

---

## 10. 공동체 나누기 — 앱 하나를 여러 교회가 (2026-08-30)

### 구조
```
app_config/global                     ← 앱 전체 공유 (VAPID, 증표 비밀키)
app_config/communities                ← 공동체 명단 (이름·가입코드)
communities/{cid}                     ← 회원·진도·설정
communities/{cid}/meditations/{id}    ← 낱장
```
우리 교회의 cid 는 `cho` (= DEFAULT_COMMUNITY_ID). 9단계에서 이미 그 자리에 옮겨 뒀다.

### 격리의 핵심은 딱 하나
**어느 공동체인지를 앱이 보낸 값으로 정하지 않고, 로그인 증표에 서명해 넣은 값으로만 정한다.**
(`cidOf()` / `TokenPayload.cid`)

앱이 보내는 `x-community` 머리말은 **로그인 전 화면**(이름 목록·로그인·가입)에서만 쓴다.
로그인한 요청은 증표가 무조건 이긴다. 그래서 남의 공동체 이름표를 적어 보내도 소용없다.
→ 시험 6번에서 실제로 확인함.

### 전역 `db` 를 없앤 이유
라우트마다 `const db = dbOf(req)` 로 자기 공동체 것을 집어야 하는데,
전역이 남아 있으면 **한 곳만 빠뜨려도 조용히 남의 공동체 데이터를 만진다.**
없애 두면 빠뜨린 자리가 타입 검사에서 바로 걸린다. (실제로 이 방법으로 라우트 52곳을 잡았다)

`saveDb(db)` 는 그대로 둘 수 있었다 — `ownerOf` (WeakMap) 가 그 덩어리의 주인을 기억한다.
호출부가 60군데라 매번 공동체를 넘기게 했으면 반드시 빠뜨렸을 것이다.

### 🚨 이번에 같이 막은 구멍들
| 구멍 | 상태 |
|---|---|
| **가입 화면에서 아무나 '관리자' 선택 가능** | 막음. 가입은 언제나 일반 지체, 권한은 기존 관리자가 준다 |
| 동기화 때마다 **모든** 공동체에 `admin-1`(비번 1234) 주입 | 우리 교회에만 적용되도록 한정 |
| 예시 데이터 정리 규칙이 **흔한 이름**을 지움 (김은혜·박요한·이선민·최영진) | 우리 교회에만 적용되도록 한정 |

첫 번째가 특히 나빴다. 주소만 알면 누구나 관리자가 되어 공지를 올리고 계정을 지울 수 있었다.

### 새로 생긴 API
| | |
|---|---|
| `GET /api/communities/current` | 로그인 화면 제목용. 이름만 알려준다 |
| `POST /api/communities/resolve` | 가입코드 → 공동체. **시도 제한 있음** (6자리는 두드리면 뚫린다) |
| `POST /api/communities/create` | 만든 사람이 첫 관리자. 관리자가 자동으로 생기는 유일한 곳 |
| `GET /api/communities/mine` | 가입코드는 관리자에게만 |
| `POST /api/communities/regenerate-code` | 코드가 새어나갔을 때 |
| `POST /api/auth/users/role` | 관리자 세우기·내리기. 마지막 관리자는 못 내린다 |

### 화면
- `CommunityGate` — 가입코드 입력 / 새 공동체 만들기
- `CommunitySettings` — 알림설정 탭 위. 가입코드 보기·재발급, 지체 권한
- `LoginScreen` — 제목이 공동체 이름으로. 맨 아래 '다른 공동체로 들어가기'
- `session.ts` 의 `installApiInterceptor()` — **모든 /api 요청에 증표와 공동체를 자동으로 붙인다.**
  화면 곳곳의 fetch 를 하나하나 고치지 않아도 되고, 빠뜨릴 자리도 없다

### 지금 쓰시는 분들에게 달라지는 것
**없다.** 기억해 둔 공동체가 없으면 서버가 우리 교회로 본다 (`cidOf` 마지막 줄).
증표에 `cid` 가 없던 옛 로그인도 우리 교회로 친다 → **아무도 튕기지 않는다.**

### 시험 (Firestore 끊고 로컬에서)
격리 24항목 + 회귀 13항목 + 버그 재발 방지 8항목 전부 통과.
특히 "B 증표 + A 이름표 → 여전히 B 것만" 을 반드시 유지할 것.

### 남은 것
- [ ] **저장소 비공개 전환** — 목사님이 GitHub 설정에서
- [ ] 성경 본문 저작권 (대한성서공회) — 스토어 출시 전 필수
- [ ] 앱 운영자(isAppOwner) 화면 — 공동체 목록·정지. 타입만 만들어 두고 아직 안 씀
- [ ] 공동체 탈퇴/이동, 공동체 삭제

---

## 11. 성경읽기 챌린지 + 탭 정리 (2026-08-31)

### 목표 팝업
하루 한 번, 그날 처음 들어올 때만 뜬다. 본 날짜를 `bible_med_goal_popup_seen` 에
한국 날짜로 적어 둔다. 예전에는 탭을 옮길 때마다 떠서 성가셨다.

### 챌린지 — 진행률을 따로 세지 않는다
**성경통독의 `completedChapters` 를 그대로 읽는다.** (`server/challenge.ts`)
통독에서 '읽음'을 누르면 챌린지 진행률에 바로 반영되고, 두 숫자가 어긋날 일이 없다.
읽음 표시 형식은 `"마가복음 3장"` — 이 형식이 바뀌면 챌린지도 같이 깨진다.

| API | |
|---|---|
| `GET /api/challenges/current` | 챌린지 + 참가자별 진행률. **여기서 완료 판정도 한다** |
| `POST /api/challenges` | 시작 (관리자). 한 번에 하나만 |
| `POST /api/challenges/:id/participants` | 참가자 바꾸기 (관리자) |
| `POST /api/challenges/:id/join` | 나도 참가 / `{leave:true}` 로 그만두기 |
| `POST /api/challenges/:id/finish` | 마치기 (관리자) |
| `GET /api/challenges/books` | 66권 목록 |

완료 판정: **목표일이 지났거나 참가자 전원이 다 읽으면** `done` + `completedDate`.

### 탭 규칙
```
평소:      오늘말씀 · 감사칭찬 · 묵상나눔 · 성경통독 · 나의기록 · 알림설정
챌린지 중: 오늘말씀 · 챌린지  · 묵상나눔 · 성경통독 · 나의기록 · 알림설정
```
- 감사칭찬과 묵상나눔의 **자리를 맞바꿨다** (감사가 앞)
- 챌린지가 도는 동안 감사 탭은 접힌다. 끝난 **당일까지는** 챌린지 탭이 남아 축하하고,
  **다음 날** 감사 탭이 돌아온다 (`visibleChallenge()` 가 날짜로 판정)
- 관리자는 챌린지가 없을 때도 그 화면에 들어갈 수 있다 — 거기서 시작하기 때문.
  입구는 **알림설정 탭 맨 위 '성경읽기 챌린지' 카드**

`TAB_DEFS` / `visibleTabs` 한 곳에서 정의하고 **PC 탭 바와 모바일 하단 바가 같이 따라온다.**
예전에는 두 바에 버튼을 각각 손으로 써 두어 하나만 고치면 서로 어긋났다.

### 알림
챌린지 참가자에게는 각자 정한 알림 시간에 **진행률 문구**가 대신 간다
(`challengeAlarmText`). 시간·요일 설정은 그대로 쓰고 내용만 바뀐다.

### ⚠️ 또 걸린 함정
스크립트로 코드를 넣을 때 **정규식의 백슬래시가 삼켜졌다.**
`/^\d{4}-\d{2}-\d{2}$/` → `/^d{4}-d{2}-d{2}$/` 가 되어 어떤 날짜도 통과하지 못했다.
(8월에 PIN 검사에서 똑같이 당했다) → 코드에 정규식을 넣을 때는 백슬래시를 피하거나
넣은 뒤 반드시 `grep` 으로 확인할 것.

### 시험
서버 18항목 통과 (권한·중복 시작 차단·통독 연동·중간 참가·완료 판정·삭제).
화면 확인: 탭 순서 교체, 챌린지 중 감사 탭 숨김, '읽으러 가기' → 통독 탭 이동.

---

## §12. 영성일기·챌린지가 재시작마다 사라지던 버그 (2026-09-01)

### 증상
일기를 쓰고 저장하면 그 자리에서는 보이는데, 나중에(배포 후·컨테이너 재시작 후)
다시 들어가면 목록이 텅 비어 있었다.

### 원인
`syncAndRefreshWithFirestore()` 가 로컬/원격을 합칠 때 `mergedDb` 를
**필드를 손으로 하나씩 적어** 만든다. 여기에 `journals` 와 `challenges` 를
적어 넣지 않아서, 서버가 뜰 때마다 두 항목이 `undefined` 가 되어 메모리에서 사라졌다.
(같은 실수를 `pushSubscriptions` 에서 이미 한 번 겪었고 주석까지 달아 뒀는데 또 했다.)

### 데이터는 살아 있었다
`saveList()` 의 안전장치 — "목록이 N건 → 0건이 되면 삭제를 건너뛴다" — 가
Firestore 의 낱장을 지키고 있었다. 확인 결과 `communities/cho/journals` 에 1건 그대로.
안전장치가 없었다면 교인이 쓴 글이 실제로 지워졌을 것이다.

### 고친 것
1. `mergedDb` 에 `challenges`, `journals` 를 id 기준 합치기로 추가.
2. `getEmptyDb()` 에도 두 칸 추가.
3. **마지막 그물** — 합치기 목록에 적히지 않은 키가 있어도
   `remoteDb[key] ?? localDb[key]` 로 무조건 들고 간다.
   앞으로 `DatabaseSchema` 에 필드를 더해도 조용히 지워지지 않는다.

### 새 필드를 추가할 때
`DatabaseSchema` 에 목록을 하나 더 붙이면 확인할 곳:
- `server/firebaseDb.ts` 의 `SPLIT_KEYS` (계속 쌓이는 목록이면)
- `server.ts` 의 `getEmptyDb()`
- `server.ts` 의 `mergedDb` (이제는 마지막 그물이 받쳐 주지만, 합치는 규칙이
  필요하면 여전히 직접 적어야 한다 — 예: id 로 합칠지, 최신 것만 둘지)

---

## §13. 새 비주얼 디자인 적용 — 그라데이션 헤더 · 테두리 없는 카드 (2026-09-04)

### 배경
Artifact 시안(claude.ai/code/artifact/07a4996a-...)으로 여러 라운드 확정한 새 디자인을
실제 코드에 반영했다. **겉모습만 바꾸는 작업** — 서버·상태·이벤트 핸들러는 전혀
건드리지 않았고, 특히 탭 좌우 스와이프(`App.tsx`)와 성경 장 좌우 스와이프
(`BibleReader.tsx`)의 `useSwipe`/`data-no-tab-swipe`/`dragRef` 구조는 그대로 뒀다.

### 색 토큰 (`src/index.css`)
```css
--color-mist: #F9F9F9;        /* 박스 채움색(외곽선 없이 배경차로만 구분) */
--color-mist-hover: #F0F0F0;
--color-teal: #2B8577;        /* 구약 */
--color-teal-deep: #195C50;
```
그리고 두 헬퍼 클래스:
- `.grad-forest` → `linear-gradient(135deg, #2F7358, #153A2B)` — 헤더·신약·기본 CTA
- `.grad-teal` → `linear-gradient(135deg, #2B8577, #195C50)` — 구약·보조 CTA

새 버튼/카드를 만들 때는 색을 직접 hex로 반복해 적지 말고 이 두 클래스를 쓸 것.

### 구조 변경 (`App.tsx`) — 이 세션 이전에 이미 반영됨
- 헤더가 `.grad-forest` 그라데이션으로, 톱니바퀴 아이콘이 생겼다.
- **설정이 하단 탭에서 빠지고 톱니바퀴로 이동** (`showSettings` state). 하단 탭 6→5개.
- `?tab=settings` 딥링크·푸시 postMessage 모두 `setShowSettings(true)`로 분기 처리됨.

### 이번에 한 것 — 나머지 화면 리스타일
- **`DailyNotice.tsx`**: 말씀 카드 회색 박스는 이전 세션에 이미 제거되어 있었음(코드에
  "상자를 뺐다" 주석 다수 발견) — 이번엔 색만 새 팔레트로. '체크인 한 동역자들'
  이름 목록을 **기본 접힘 + 클릭해서 펼치기**(`showReaders` state)로 바꿔 비중을 줄이되
  기능은 그대로 뒀다.
- **`BibleReader.tsx`**: 마지막 읽은 곳 + 구약/신약 버튼을 하나의 `#F9F9F9` 박스로
  묶고 `border-t` 구분선 추가. 구약 버튼 `.grad-teal`, 신약 버튼 `.grad-forest`.
  이전/다음 장, 통독완료, 묵상쓰기 버튼 전부 그라데이션으로. **스와이프 관련 JSX는
  한 줄도 안 건드림.**
- **`MeditationFeed.tsx` / `DailyGratitude.tsx`**: 글쓴이 이름의 노란 알약 배지
  (`bg-[#FFBA00]`)를 평범한 굵은 텍스트로. 속(sok) 필터 탭바는 원래 있던 기능
  그대로 두고 활성 탭만 그라데이션으로. 글쓰기 버튼들 그라데이션 적용.
- **`MyMeditations.tsx`**: 히어로 카드(목표 진행률) `.grad-forest`, 검색+필터
  카드는 `#F9F9F9`/외곽선 없이. "말씀 체크" 필터가 `.grad-teal`로 구분되게.
  (시안의 "두 박스 합치기"는 이 화면 구조와 안 맞아 적용 안 함 — 목적이 다른
  두 카드라 억지로 합치면 오히려 어색함)
- **`NotificationSettings.tsx`**: 글씨 크기 카드를 4칸 그리드 + 트랙 배경으로
  세그먼트 컨트롤처럼 바꿈(`scales` 배열·`onClick` 로직은 그대로, 마크업만).
  요일 선택 칩·PIN 변경 버튼도 그라데이션/외곽선 제거.
- **`CommunitySettings.tsx`**: 저장·초대문구 복사 버튼 `.grad-forest`.
- 남아있던 실제 박스 테두리(`border-[#E3E9E2]` 류, 장식용 아님) 전부 제거하고
  배경색 대비로 대체 — 체크박스 등 기능상 필요한 테두리는 그대로 둠.

### 확인한 것 / 안 한 것
`npm run build` 통과 확인(타입 에러 없음). **`npm run dev` 로 직접 눌러보며 확인은
안 했다** — 이 프로젝트의 개발 서버는 실제 운영 중인 Firestore(진짜 교인 데이터)에
그대로 붙는데, 로그인 계정 정보가 없어 자동화로 만지는 게 부적절하다고 판단해서다.
다음에 이어서 확인할 사람은 실제 로그인 후: 탭 스와이프, 장 스와이프, 톱니→설정→
뒤로가기, 오늘말씀 번역본 전환/읽음체크/동역자목록 펼치기, 통독완료/구약신약 팝업,
속 필터, 글씨크기 세그먼트가 실제로 커지는지를 확인할 것.

---

## §14. §13 이후 — 색만으로는 부족해서 구조까지 시안대로 맞춤 (2026-09-04)

### 배경
§13에서 색만 바꿨더니 목사님이 스크린샷을 여러 번 보내며 "시안이랑 아직 많이 다르다"고
지적하셨다. 실제로 놓친 것들:
- 헤더가 초록으로 안 보임 → `FontSizeControl` 버튼이 자체 흰 배경(`bg-[#F5F5F5]`)을 가지고 있어
  좁은 화면에서 헤더 대부분을 덮어버림. 톱니처럼 반투명 원형 버튼(`bg-white/15`)으로 교체.
- `DailyNotice.tsx` 제목이 옛 구조("오늘의 말씀 공지" + 아이콘뱃지 + 고정 캡션) 그대로였음
  → "오늘의 말씀" + "{구절} · {날짜}" 한 줄로 교체. 날짜도 저장된 `"2026-09-04"` 형식을
  그대로 보여주고 있어서 `formatKoreanDate()` 헬퍼를 새로 만들어 "9월 4일 금요일"로 변환.
- `MeditationFeed.tsx`에 있어야 할 탭 제목("묵상 나눔" + 부제)이 아예 없었음 — 추가.
- 여러 화면이 시안의 얇은 `.nav-card`/`.post-card`/`.row` 구조가 아니라 옛날처럼
  진한 그라데이션 히어로 카드, 이중 박스(박스 안에 또 박스), `shadow-sm` 흰 카드를 그대로 쓰고 있었음.

시안 HTML(`cho_settings_design_test.html`)을 처음부터 끝까지 다시 정독해서
`.page-title`/`.nav-card`/`.post-card`/`.mini-avatar`/`.row`/`.chip`/`.segment` 등
실제 px 값·색까지 그대로 뽑아낸 뒤, 그 스펙으로 구조 자체를 다시 짰다.

### 이번에 구조까지 바꾼 것
- **`src/index.css`**: `--color-mist`/`--color-teal` 계열에 `.grad-forest`/`.grad-teal`
  헬퍼 클래스 추가 (135deg 그라데이션, 신약=진초록/구약=청록). 새 버튼 만들 때 이 클래스를 쓸 것.
- **`App.tsx`**: 헤더 패딩을 시안 값(`pt-11 pb-8 px-[18px]`)에 맞추고, `<main>`을
  `-mt-[18px] bg-white rounded-t-[22px]`로 감싸 헤더 위로 살짝 겹쳐 올라오는
  "흰 시트" 효과를 넣었다(시안의 `.sheet`). 페이지 배경을 `#F0F0F0` → `bg-white`로
  바꿨다 — `#F9F9F9`/`#F0F0F0` mist 카드들은 흰 배경 위에서만 대비가 산다.
  설정 화면의 계정/안내 박스를 흰 카드에서 `#F9F9F9` row-group(아이콘 원형 칩 + 글 + 화살표)으로.
- **`DualBibleText.tsx`/`FormattedBibleText.tsx`**: 절 번호가 `w-[18px] text-right`
  고정폭 우측정렬이던 것을 시안처럼 자연스러운 좌측 흐름으로 바꿈(번호가 제목과 같은
  왼쪽 선에서 시작하게).
- **`DailyNotice.tsx`**: 제목 구조 교체(위 참조), 바깥 흰 카드·안쪽 `bg-white` 이중 래핑 제거,
  `pl-[10px]` 류 정렬 보정값 제거(이제 필요 없음).
- **`BibleReader.tsx`**: 그라데이션 히어로 카드를 없애고 진행률+마지막 읽은 곳+구약/신약을
  전부 하나의 `#F9F9F9` `.nav-card`에 통합(목표설정·체크리스트는 작은 아이콘 버튼으로 축소).
  본문을 감싸던 흰 카드(`bg-white rounded-3xl shadow-sm`)를 없애고 장 제목을 상자 밖으로 뺐다.
  **스와이프 관련 JSX(`data-no-tab-swipe`/`swipeHandlers`/`dragRef`)는 이번에도 손대지 않음.**
- **`MeditationFeed.tsx`**: 탭 제목 추가, 글쓰기 버튼 두 개를 `flex-1` 동일 너비로
  (순서도 시안대로 나누기→영성일기), 필터 바를 버튼 아래로 이동. 글 카드를
  `bg-white shadow-sm` → `bg-[#F9F9F9]` 무그림자로, 본문을 감싸던 이중 박스 제거.
- **`DailyGratitude.tsx`**: 진초록 그라데이션 히어로 배너를 통째로 제거하고 다른 탭과 같은
  평범한 페이지 제목 + 전체 너비 버튼으로. 글 카드에 미니 아바타(연녹 원, 이름 마지막 글자) 추가,
  이중 박스 제거.
- **`MyMeditations.tsx`**: 그라데이션 히어로 + 진행률 막대를 없애고, 시안처럼 "말씀
  체크리스트"/"내 나눔 목표" 평범한 두 행(`nav-card`)으로 — 실제 통계(이번 달 묵상/감사 횟수,
  체크해 둔 구절 수)는 각 행의 부제 텍스트로 그대로 살려서 정보 손실 없게 함.
  "내가 쓴 글" 섹션 라벨 추가, 카드 스타일 통일.
- **`NotificationSettings.tsx`**: 제일 큰 작업. "알림"/"계정" 섹션 라벨 + `#F9F9F9`
  row-group으로 전면 재구성. 가입코드·지체 권한 관리처럼 내용이 많은 항목은
  `CommunitySettings.tsx`와 같은 패턴(평소엔 행만 보이다가 누르면 펼쳐짐)으로 처리해서
  기능은 하나도 안 줄이면서 시안의 얇은 리스트 느낌을 유지했다.
- **`CommunitySettings.tsx`**: "가입코드·초대하기"/"지체 권한 관리"를 누르면 펼쳐지는
  행으로 바꿈(`showJoinCode`/`showMembers` state 추가) — 안의 실제 기능(재발급, 복사,
  공유, 권한 변경)은 전부 그대로.
- **`ReactionBar.tsx`**: 비활성 반응 버튼 배경을 `#F5F5F5` → `white`로 (카드가
  `#F9F9F9`가 되면서 거의 안 보였음).

### 다음에 참고할 것
시안 원본은 `C:\Users\beeeb\Downloads\cho_settings_design_test.html` (artifact:
`claude.ai/code/artifact/07a4996a-adbb-485b-92b5-300025e9d77a`). 새 화면을 만들 때
"이게 시안 어디에 해당하나"부터 그 HTML의 `<section class="panel" data-panel="...">` 를
찾아 대조할 것 — 색만 보고 넘기면 이번처럼 구조 차이를 놓친다.
여전히 `npm run dev` 라이브 확인은 못 했다(운영 DB). 목사님이 시크릿 창 또는
브라우저 캐시 지운 뒤 `localhost:3000` 새로고침해서 확인해야 함.
