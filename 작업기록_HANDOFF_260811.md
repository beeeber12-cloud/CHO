# 📖 성경묵상 공유앱(CHO) — 작업 기록 & 인수인계

> 이 문서 하나로 창을 껐다 켜거나 다른 세션에서도 이어서 작업할 수 있습니다.
> **최종 업데이트: 2026-08-11**
> 이전 문서: `작업기록_HANDOFF_260807.md` (함정 5가지·구조 설명은 반드시 그쪽을 먼저 볼 것)

---

## 0. ⚡ 이번 세션 3줄 요약

1. **반응 버튼 색을 은은한 연녹색으로 통일** (노란색이 글 사이에서 너무 튀었다)
2. **알림을 누르면 그 글이 있는 탭으로 바로 들어간다** (전에는 항상 첫 화면)
3. **알림설정의 시간 알림이 실제로 동작한다** — 예전엔 설정만 저장되고 아무 일도 없었다

---

## 1. 핵심 주소·ID (변동 없음)

| 항목 | 값 |
|---|---|
| 로컬 폴더 | `C:\Users\beeeb\Downloads\CHO` |
| GitHub | https://github.com/beeeber12-cloud/CHO |
| 라이브 (Cloud Run) | https://cho-1006806795339.asia-northeast3.run.app |
| Firestore 문서 | `app_data/main_db` (**DB 전체가 문서 1개**) |
| 배포 방식 | `main` 브랜치 push → Cloud Run 자동 배포 (약 2~3분) |

---

## 2. 이번 세션 작업 내용

### ① 반응 색상 통일 (은은하게)
`src/components/ReactionBar.tsx`
- 누른 상태: `bg-[#FFBA00]`(진한 노랑) → **`bg-[#E8F0E9]` + `ring-1 ring-[#C3D6C6]`**
- 👍 좋아요 / 🙏 기도할게요 **둘 다 같은 색**을 쓴다
- `MeditationFeed.tsx` 의 "N명이 당신을 위해 기도했습니다" 상자도 같은 색으로 맞춤

> 이름 배지(`테스트지체` 같은 노란 라벨)는 손대지 않았다. 사람 구분용이라 일부러 남김.

### ② 알림 누르면 해당 화면으로 바로 이동
서버가 보내는 알림에 **주소를 같이 실어 보낸다.**

| 알림 | 주소 |
|---|---|
| 오늘의 말씀 공지 | `/?tab=notice` |
| 새 묵상 / 묵상 댓글 / 묵상 반응 | `/?tab=feed` |
| 감사 나눔 / 감사 댓글 / 감사 반응 | `/?tab=gratitude` |
| 시간 알림(아침 묵상) | `/?tab=notice` |
| 테스트 알림 | `/?tab=settings` |

동작 방식 (`public/sw.js` 의 `notificationclick`):
1. 이미 앱이 열려 있으면 → **서비스워커가 화면에 "이 탭 열어줘" 메시지**를 보낸다.
   `MessageChannel` 로 0.5초 안에 답이 오면 새로고침 없이 탭만 바뀐다 (빠름).
2. 답이 없으면(옛 버전이 열려 있는 경우) → `client.navigate()` 로 주소를 바꿔 다시 띄운다.
3. 앱이 꺼져 있으면 → `openWindow(주소)`.

`src/App.tsx`
- 시작할 때 `?tab=` 을 읽어 그 탭으로 연다 (`tabFromUrl()`)
- 읽은 뒤 `history.replaceState` 로 주소를 지운다 — **안 지우면 새로고침마다 그 탭으로 되돌아간다**
- 서비스워커 메시지를 받으면 탭을 바꾸고 `e.ports[0]` 로 **답장**한다.
  이 답장이 있어야 서비스워커가 새로고침을 생략한다.

### ③ 시간 알림이 실제로 울린다 (가장 큰 변화)
전에는 알림설정의 시간·요일이 **DB에 저장만 되고 아무것도 하지 않았다.**

`server.ts` 에 추가:
- `getKSTClock()` — 한국 기준 요일(0=일)과 자정부터 지난 분
- `runAlarmSweep()` — 울릴 차례인 사람을 찾아 그 사람에게만 푸시
- 서버 기동 시 1회 + **`setInterval` 1분마다** 실행
- `POST/GET /api/alarms/tick` — 밖에서 깨우는 통로 (아래 §3 참고)

안전장치:
- `AlarmConfig.lastFiredDate` 에 마지막 발송 날짜(한국 날짜)를 남긴다.
  **서버가 다시 켜져도 같은 날 두 번 울리지 않는다.**
- 예정 시각 **이후 30분 안**에만 보낸다 (`ALARM_GRACE_MINUTES`).
  컨테이너가 자다 깨어난 경우를 위한 여유이자, 엉뚱한 시각에 울리지 않게 하는 상한.
- 시간을 바꿔 저장하면 `lastFiredDate` 를 지운다 → 오늘 저녁으로 옮겨서 바로 확인 가능.
- **알림을 켜 둔 사람(푸시 구독이 있는 사람)만** 대상이다.

> ⚠️ 라우트 순서 주의: `/api/alarms/tick` 은 반드시 `/api/alarms/:userId` **앞에** 등록해야 한다.
> 뒤에 두면 `tick` 이 사용자 ID 로 잡혀서 엉뚱한 응답이 나간다.

**저장한 적 없는 사람은?**
알림설정 화면은 저장 전에도 "07:30 · 월~금 · 켜짐"으로 보인다.
그래서 저장 이력이 없어도 그 기본값(`DEFAULT_ALARM`)대로 울린다 — 화면과 실제가 어긋나지 않게.
실제로 울린 순간에만 설정이 목록에 들어간다 (쓸데없는 설정이 쌓이지 않게).

### ④ 감사칭찬 글 알림 — 이미 있던 기능
`POST /api/gratitudes/create` 는 **`8337ee6` 시점부터 전 교인에게 푸시를 보내고 있었다.**
감사 댓글·감사 반응 알림도 이미 있다. 이번엔 주소(`?tab=gratitude`)만 붙였다.
안 왔다면 코드 문제가 아니라 **각자 휴대폰에서 '새 글 알림'을 안 켰을 가능성**이 크다.

---

## 3. 🚨 아직 남은 문제 — Cloud Run 이 잠들면 알림이 안 간다

Cloud Run 은 **아무도 앱을 안 쓰면 컨테이너를 잠재운다.** 잠들면 서버 안의 1분 타이머도 멈춘다.
새벽 7시 30분에 아무도 앱을 안 켜고 있으면 **알림이 안 나갈 수 있다.**

해결 방법 두 가지 (둘 중 하나만 하면 됨):

**(A) Cloud Scheduler — 사실상 무료, 권장**
GCP 콘솔 → Cloud Scheduler → 작업 만들기
- 빈도: `*/5 * * * *` (5분마다) — 알림 시간대만 원하면 `*/5 5-11 * * *`
- 시간대: `Asia/Seoul`
- 대상: HTTP / `https://cho-1006806795339.asia-northeast3.run.app/api/alarms/tick` / **GET**

> ⚠️ POST 로 하려면 본문이 비어 있어도 `Content-Length: 0` 이 붙어야 한다.
> 구글 프론트엔드가 **본문 없는 POST 를 411 로 되돌려보낸다** (실제로 확인함).
> GET 은 그런 제약이 없으니 GET 으로 거는 편이 안전하다.

Cloud Scheduler 는 작업 3개까지 무료, 호출량도 Cloud Run 무료 한도 안이다.
`tick` 은 하루 한 번만 나가도록 막혀 있어 여러 번 불려도 중복 발송되지 않는다.

**(B) 최소 인스턴스 1개 고정** — 월 7~8달러쯤 든다. 정확하지만 비용이 붙는다.

---

## 4. 사용자에게 알려야 할 것

- **송지범 님의 알림 시간이 `00:00`(자정)으로 저장돼 있다.** 이대로 두면 평일 자정에 울린다.
- 최희란 10:00, 김민석 10:30, 조은미 06:00(토요일 포함), 나머지는 07:30 월~금.
- `GEMINI_API_KEY` 미설정 → 성경Q&A 탭 숨김(`SHOW_QNA_TAB = false`) — 이전과 동일.
- 스토어 출시 전 저작권 확인(개역개정=대한성서공회 유료, 우리말성경=두란노) — 이전과 동일.

---

## 5. 검증 방법 (이번에 실제로 한 것)

프로덕션 Firestore 를 건드리지 않으려고 **격리 사본**에서 확인했다.
`firebase-applet-config.json` 이 없으면 Firestore 연결이 아예 꺼지므로,
그 파일과 `db.json` 을 뺀 사본을 임시 폴더에 만들고 가짜 회원·가짜 구독으로 돌렸다.

확인한 것:
| 상황 | 결과 |
|---|---|
| 예정 시각이 막 지난 알림 | 1건 발송 ✅ |
| 같은 알림 두 번째 tick | 0건 (중복 없음) ✅ |
| 오늘이 아닌 요일 | 0건 ✅ |
| 아직 안 된 시각 / 1시간 넘게 지난 시각 | 0건 ✅ |
| 알림 꺼짐 | 0건 ✅ |
| 시간 변경 후 | `lastFiredDate` 지워짐 ✅ |
| `GET·POST /api/alarms/tick` | 사용자 ID 로 안 잡힘 ✅ |
| `/?tab=gratitude` 로 열기 | 감사칭찬 탭 열리고 주소는 `/` 로 정리됨 ✅ |
| 서비스워커 메시지로 탭 전환 | 새로고침 없이 전환, 엉뚱한 값은 무시 ✅ |
| 기도할게요 누른 색 | `rgb(232,240,233)` + 테두리 `rgb(195,214,198)` ✅ |

**⚠️ 프로덕션에서는 절대 테스트하지 말 것.**
`/api/gratitudes/create`, `/api/meditations/create`, 리액션, 그리고 이제 **시간 알림까지**
전 교인 휴대폰으로 알림이 나간다.

---

## 5-2. 추가 작업 (2026-08-12)

### ⑤ 구절명에 장이 빠지던 문제
고른 절로 묵상을 쓰면 `마가복음 3,5절` 처럼 **장이 사라졌다.**
어느 장인지 모를 뿐 아니라 다시 읽을 때 **3장으로 잘못 해석**된다.
→ `src/lib/verseRef.ts` 신규. 이제 `마가복음 1장 3,5절`.
`요한복음 3:16` 같은 형태도 `요한복음 3장` 으로 정규화한다. 시편은 `편` 유지.

### ⑥ 오늘 말씀 공지가 제대로 안 넘어가던 문제 🔥
실제 이력: 요한복음 2장이 **3일 연속**(08-07·08-07·08-08), 08-13 공지가 **08-11과 같은 2장**.

**원인 세 가지**
1. 동기화가 통독 진도를 **원격 값으로 무조건 덮어썼다** → 방금 넘긴 진도가 되돌아감
2. 공지 생성에 잠금이 없어 **같은 날 공지가 두 개** 생김 (동시 요청)
3. **내일 공지를 미리 만드는 구조** → 되돌아간 진도로 만든 앞날 공지가 그대로 남음

**수정**
- `biblePlan.updatedAt` 추가 + `pickNewerPlan()` — 합칠 때 더 최근 것을 남긴다
- `positionForNotice()` — 진도가 이미 지나온 자리를 가리키면 **공지 이력 기준으로 자가 교정**.
  단, 관리자가 방금 지정했으면(`lastUpdatedDate` 없음) 그대로 따른다
- `ensureTodayNotice()` — 하루 한 건 잠금 + **앞날 자동 공지 정리**
- `prepareTomorrowNotice` / `nextDayStr` 제거 — 앞날 공지를 더는 만들지 않는다
- 1분 타이머와 `/api/alarms/tick` 에서 오늘 공지를 확인 → **자정에 자동으로 다음 장**

⚠️ **관리자가 진도를 옮기면 `lastUpdatedDate` 를 지운다.** 이 값이 "이 자리에서 이미 공지했다"는
표시라서, 안 지우면 자가 교정이 관리자의 지정을 덮어쓴다.

**격리 사본 검증 5가지 전부 통과**
평소 진행 / 진도 되돌아감 자가교정 / 책 끝 → 다음 권 1장 / 관리자 지정 존중 / 통독 꺼짐.
두 번 두드려도 공지는 하나만 생긴다.

**정리한 프로덕션 데이터**
- `2026-08-13 마가복음 2장` 공지 삭제 (코드가 자동으로 걷어냄)
- 진도를 `마가복음 4장` 으로 지정 → 08-13 자정에 4장이 나간다

> 참고: **공지 삭제 API 가 없다.** 그래서 이번엔 코드가 스스로 걷어내게 했다.
> 앞으로 공지를 지워야 할 일이 생기면 관리자용 DELETE 라우트를 만들어야 한다.

## 6. 만진 파일

| 파일 | 내용 |
|---|---|
| `server.ts` | `getKSTClock`, `DEFAULT_ALARM`, `runAlarmSweep`, `/api/alarms/tick`, 푸시 주소에 `?tab=`,<br>`pickNewerPlan`, `positionForNotice`, `ensureTodayNotice`, `parseChapterTitle`, `lastAutoNoticePosition` |
| `src/lib/verseRef.ts` | **신규** — 장을 포함한 구절명 만들기 |
| `public/sw.js` | `notificationclick` 재작성 (`askToOpenTab`) |
| `src/App.tsx` | `tabFromUrl`, 서비스워커 메시지 수신 |
| `src/types.ts` | `AlarmConfig.lastFiredDate` |
| `src/components/ReactionBar.tsx` | 누른 상태 색 |
| `src/components/MeditationFeed.tsx` | 기도 인원 상자 색 |
| `src/components/NotificationSettings.tsx` | 시간 아래 안내 문구 |

---

## 7. 추가 작업 (2026-08-13)

### 🔥 알림이 거의 안 가던 진짜 원인 — Cloud Run 의 CPU 조이기
푸시를 `res.json()` **뒤에** 던져두고 기다리지 않았다.
Cloud Run 은 응답이 끝나면 그 인스턴스의 CPU 를 거의 0 으로 줄인다.
그래서 전송이 **시작만 되고 끝나지 않아** 알림이 조용히 사라졌다.

> 테스트 알림만 되던 이유가 이것이다 — 그것 하나만 `await` 하고 있었다.

**수정:** 새 공지 / 새 묵상 / 묵상 댓글·반응 / 감사 나눔·댓글·반응 **전부 응답 전에 await**.
한 건이 오래 걸려도 화면이 멈추지 않게 `PUSH_SEND_TIMEOUT_MS = 4000` 상한.

⚠️ **앞으로 절대 `res.json()` 뒤에 비동기 작업을 던져두지 말 것.** 안 끝난다.
(자정 공지·시간 알림을 1분 타이머 + Cloud Scheduler 로 돌리는 이유도 같다)

### 등록된 기기가 0대였다
`GET /api/push/status` 를 새로 만들어 확인해 보니 **전 교인 통틀어 구독 0건.**
코드를 고쳐도 켠 사람이 없으면 아무 알림도 안 간다.
→ 알림설정에 **지체별 알림 켜짐 현황**(관리자만) 추가. 누가 껐는지 눈으로 확인 가능.

### @이름 부르기
- 서버 `findMentionedUserIds` / `pushMentions` — 글·댓글에서 `@이름` 을 찾아 알림
- `관리자(목사님)` 처럼 괄호가 붙은 이름은 `@관리자` 로도 불린다
- 같은 글에서 다른 알림을 이미 받는 사람은 빼서 **두 번 울리지 않게** 함
- 화면: `MentionText` 로 강조, `MentionPicker`("지체 부르기")로 이름을 눌러 입력
  → 어르신께는 자동완성 드롭다운보다 이 방식이 쉽다

### 긴 묵상 접기
`CollapsibleText` — 윗부분만 보이고 '더 보기'로 편다.
높이를 **ResizeObserver 로 실제 측정**한다. 글씨 크기 설정을 바꿔도 맞게 동작하고,
짧은 글에는 버튼이 아예 안 나온다.

### 만진 파일 (추가)
| 파일 | 내용 |
|---|---|
| `server.ts` | `sendPushToUsers` 반환값·타임아웃·로그, `findMentionedUserIds`, `pushMentions`, `/api/push/status`, 모든 푸시 await |
| `src/components/CollapsibleText.tsx` | **신규** — 긴 글 접기 |
| `src/components/MentionText.tsx` | **신규** — `@이름` 강조 |
| `src/components/MentionPicker.tsx` | **신규** — 이름 눌러 넣기 |
| `src/lib/mentions.ts` | **신규** — 멘션 규칙 (서버와 동일하게 유지할 것) |
| `src/components/NotificationSettings.tsx` | 알림 켜짐 현황 카드 |

---

## 8. 0단계 보안 조치 (2026-08-19~20)

### 🚨 발견한 것
| 항목 | 상태 |
|---|---|
| GitHub 저장소 | **공개** — API 로 확인 (200) |
| `firebase-applet-config.json` | 그 저장소에 **커밋돼 있음** |
| `firestore.rules` | `allow read, write: if true` |
| 비밀번호 | 4자리 PIN **평문 저장**, 시도 제한 없음 |
| 권한 확인 | 몸통(body)의 `adminId`·`requestorId` 를 **그대로 신뢰** |

마지막 항목이 특히 나빴다. 회원 목록은 로그인 없이 볼 수 있으므로
**아무나 남의 계정을 지우거나 비밀번호를 바꿀 수 있었다.**

### ✅ 완료 (커밋 `b73b5d6`, 배포 확인)
- **비밀번호 암호화** — scrypt. Node 기본 기능이라 설치할 것 없음 (`server/auth.ts`)
  - 기동 시 남아 있는 평문을 일괄 변환
  - 등록·본인수정·관리자변경·로그인시 승급, **네 곳 모두** 해시
  - ⚠️ `admin-update-pin` 을 처음에 빠뜨렸다가 테스트로 잡았다. 비밀번호를 쓰는 곳은 반드시 전수 확인할 것
- **로그인 증표(토큰)** — HMAC 서명, 180일. `src/lib/session.ts` 의 `authFetch` 로 전송
  - 증표의 역할을 그대로 믿지 않고 **매번 DB 로 재확인**
  - 탈퇴자의 증표는 즉시 무효
- **시도 제한** — 10분 10회. 실서비스에서 11번째 차단 확인
- 보호한 라우트: `admin-update-pin`, 계정 삭제, 내 정보 수정, `/api/push/status`

**실서비스 확인:** 예전 구멍 401·403 / 로그인 화면 목록 200(정상) / 오늘말씀·묵상 34건 그대로

### ⏸ 남은 것 — Firestore 규칙 잠금
코드는 준비됐으나 **아직 커밋하지 않았다** (`server/firebaseDb.ts`, `firestore.rules`).

**이유: 프로젝트가 둘로 갈려 있다.**
| | |
|---|---|
| Firestore 가 있는 곳 | `elemental-diode-2w1xt` (Firebase) |
| Cloud Run 이 도는 곳 | `dulcet-medley-471901-m4` (GCP) |

지금은 브라우저용 SDK 로 접속해서 규칙만 열려 있으면 통했다.
관리자 SDK 로 바꾸면 **Cloud Run 서비스 계정에 Firestore 프로젝트 권한을 따로 줘야 한다.**
안 주고 배포하면 데이터를 못 읽어 앱이 빈 화면이 된다.
(`[SAFETY]` 가드가 있어 데이터가 지워지지는 않는다)

⚠️ **두 개의 함정**
1. `GOOGLE_CLOUD_PROJECT` 환경변수는 Cloud Run 프로젝트를 가리킨다. Firebase 프로젝트가 아니다.
   → `readSettings()` 는 **설정 파일을 먼저** 본다. 순서를 뒤집으면 엉뚱한 DB 를 본다
2. 규칙을 먼저 잠그고 SDK 를 나중에 바꾸면 **그 사이에 앱이 멈춘다**. 반드시 SDK 먼저

**순서:** ① 저장소 비공개 → ② IAM 권한 부여 → ③ 관리자 SDK 배포·확인 → ④ 규칙 잠금

---

## 9. 데이터를 목사님 소유 프로젝트로 이전 (2026-08-30)

### 왜 옮겼나
8단계에서 "Firestore 규칙 잠금"이 막혀 있었다. 원인을 파고 보니 권한 문제가 아니라
**소유권 문제**였다.

| | |
|---|---|
| `elemental-diode-2w1xt` (데이터) | 목사님 역할 = **로그 뷰어**. 소유자 줄 자체가 없음 |
| `dulcet-medley-471901-m4` (앱) | 목사님 = **소유자** |

데이터 프로젝트는 Google 이 자동 생성한 것이라 관리 권한이 넘어오지 않았다.
서비스 계정도 못 만들고("새 서비스 계정을 만들 권한이 없습니다"), IAM 부여 버튼도 비활성.
**즉 그 프로젝트의 규칙은 영원히 못 잠근다.** 그래서 옮기는 것 말고 답이 없었다.

### 얼마나 열려 있었나 (실측)
```
GET https://firestore.googleapis.com/v1/projects/elemental-diode-2w1xt/databases/<id>/documents/app_data/main_db
→ HTTP 200   (자격증명 없이, 아무나)
```
교인 18명의 묵상·기도제목·감사가 인터넷에 그대로 노출돼 있었다.

### 한 일
1. **백업 2부** — `C:\Users\beeeb\Downloads\CHO_백업\` (저장소 밖). 이전 전 / 배포 직전
2. **gcloud 설치 + 로그인** — 이 PC 에 Cloud SDK 582.0.0. `gcloud auth login` + `application-default login`
3. **Firestore 생성** — `dulcet-medley-471901-m4` / `(default)` / **asia-northeast3(서울)** / 삭제 잠금 켬
   - 위치는 나중에 못 바꾼다. Cloud Run 과 같은 서울로 맞췄다 (응답도 빨라짐)
4. **데이터 이전** — 17개 항목 전부 개수 대조 일치
5. **관리자 SDK 전환 + 배포** (`b4514d1` → 리비전 `cho-00084-qbr`)
   - `firebase-applet-config.json` 에서 apiKey 등 접속 정보 제거 (저장소가 아직 공개)
6. **Cloud Scheduler 등록** — `cho-alarm-tick`, `*/5 * * * *`, Asia/Seoul, GET `/api/alarms/tick`

### ⚠️ 규칙 잠금(8단계의 ④)은 **필요 없어졌다**
새 프로젝트에는 **Firebase 가 붙어 있지 않다.** 따라서 클라이언트 SDK 로 들어오는 길이
아예 없고, 접근은 IAM 으로만 통제된다. "규칙"이라는 무른 문이 없는 게 오히려 더 단단하다.
```
새 DB  → HTTP 403 (자격증명 없이)
옛 DB  → HTTP 200
```
`firestore.rules` 는 기록용으로 남겨 뒀다. 이 프로젝트에는 적용 대상이 없다.

### 이전 중 생긴 일 (정상)
배포 전환 시점이 자정과 겹쳐, 8/30 오늘말씀이 **양쪽에서 각각 자동 생성**됐다.
내용은 같고 id 만 다르다 (`notice-lbdmfulgg` / `notice-v6w6vv49c`). 데이터 손실은 없다.

### 함정 메모
- Cloud Run 서비스 계정은 자기 프로젝트에 이미 `roles/editor` 를 갖고 있다 → **권한 부여 불필요**
- Git Bash 에서 `gcloud.cmd` 는 **한 줄로** 불러야 한다. 줄바꿈(`\`)을 쓰면
  공백 들어간 설치 경로 때문에 깨진다. `cygpath -d` 로 짧은 경로를 쓰면 안전하다
- PowerShell 히어스트링(`@'...'@`)을 Bash 도구에 쓰면 `@` 가 그대로 들어간다 (커밋 제목 오염)

### 남은 것
- [ ] **옛 프로젝트 데이터 삭제** — 아직 HTTP 200 으로 열려 있다. 최우선
- [ ] **저장소 비공개 전환** — 목사님이 GitHub 설정에서
- [ ] 1MB 한 문서 구조를 낱장(서브컬렉션)으로 분리 — 현재 166KB/1024KB, 주당 25KB 증가

---

## 10. 공동체 나누기 — 앱 하나를 여러 교회가 (2026-08-30)

### 구조
```
app_config/global                     ← 앱 전체 공유 (VAPID, 증표 비밀키)
app_config/communities                ← 공동체 명단 (이름·가입코드)
communities/{cid}                     ← 회원·진도·설정
communities/{cid}/meditations/{id}    ← 낱장
```
우리 교회의 cid 는 `cho` (= DEFAULT_COMMUNITY_ID). 9단계에서 이미 그 자리에 옮겨 뒀다.

### 격리의 핵심은 딱 하나
**어느 공동체인지를 앱이 보낸 값으로 정하지 않고, 로그인 증표에 서명해 넣은 값으로만 정한다.**
(`cidOf()` / `TokenPayload.cid`)

앱이 보내는 `x-community` 머리말은 **로그인 전 화면**(이름 목록·로그인·가입)에서만 쓴다.
로그인한 요청은 증표가 무조건 이긴다. 그래서 남의 공동체 이름표를 적어 보내도 소용없다.
→ 시험 6번에서 실제로 확인함.

### 전역 `db` 를 없앤 이유
라우트마다 `const db = dbOf(req)` 로 자기 공동체 것을 집어야 하는데,
전역이 남아 있으면 **한 곳만 빠뜨려도 조용히 남의 공동체 데이터를 만진다.**
없애 두면 빠뜨린 자리가 타입 검사에서 바로 걸린다. (실제로 이 방법으로 라우트 52곳을 잡았다)

`saveDb(db)` 는 그대로 둘 수 있었다 — `ownerOf` (WeakMap) 가 그 덩어리의 주인을 기억한다.
호출부가 60군데라 매번 공동체를 넘기게 했으면 반드시 빠뜨렸을 것이다.

### 🚨 이번에 같이 막은 구멍들
| 구멍 | 상태 |
|---|---|
| **가입 화면에서 아무나 '관리자' 선택 가능** | 막음. 가입은 언제나 일반 지체, 권한은 기존 관리자가 준다 |
| 동기화 때마다 **모든** 공동체에 `admin-1`(비번 1234) 주입 | 우리 교회에만 적용되도록 한정 |
| 예시 데이터 정리 규칙이 **흔한 이름**을 지움 (김은혜·박요한·이선민·최영진) | 우리 교회에만 적용되도록 한정 |

첫 번째가 특히 나빴다. 주소만 알면 누구나 관리자가 되어 공지를 올리고 계정을 지울 수 있었다.

### 새로 생긴 API
| | |
|---|---|
| `GET /api/communities/current` | 로그인 화면 제목용. 이름만 알려준다 |
| `POST /api/communities/resolve` | 가입코드 → 공동체. **시도 제한 있음** (6자리는 두드리면 뚫린다) |
| `POST /api/communities/create` | 만든 사람이 첫 관리자. 관리자가 자동으로 생기는 유일한 곳 |
| `GET /api/communities/mine` | 가입코드는 관리자에게만 |
| `POST /api/communities/regenerate-code` | 코드가 새어나갔을 때 |
| `POST /api/auth/users/role` | 관리자 세우기·내리기. 마지막 관리자는 못 내린다 |

### 화면
- `CommunityGate` — 가입코드 입력 / 새 공동체 만들기
- `CommunitySettings` — 알림설정 탭 위. 가입코드 보기·재발급, 지체 권한
- `LoginScreen` — 제목이 공동체 이름으로. 맨 아래 '다른 공동체로 들어가기'
- `session.ts` 의 `installApiInterceptor()` — **모든 /api 요청에 증표와 공동체를 자동으로 붙인다.**
  화면 곳곳의 fetch 를 하나하나 고치지 않아도 되고, 빠뜨릴 자리도 없다

### 지금 쓰시는 분들에게 달라지는 것
**없다.** 기억해 둔 공동체가 없으면 서버가 우리 교회로 본다 (`cidOf` 마지막 줄).
증표에 `cid` 가 없던 옛 로그인도 우리 교회로 친다 → **아무도 튕기지 않는다.**

### 시험 (Firestore 끊고 로컬에서)
격리 24항목 + 회귀 13항목 + 버그 재발 방지 8항목 전부 통과.
특히 "B 증표 + A 이름표 → 여전히 B 것만" 을 반드시 유지할 것.

### 남은 것
- [ ] **저장소 비공개 전환** — 목사님이 GitHub 설정에서
- [ ] 성경 본문 저작권 (대한성서공회) — 스토어 출시 전 필수
- [ ] 앱 운영자(isAppOwner) 화면 — 공동체 목록·정지. 타입만 만들어 두고 아직 안 씀
- [ ] 공동체 탈퇴/이동, 공동체 삭제

---

## 11. 성경읽기 챌린지 + 탭 정리 (2026-08-31)

### 목표 팝업
하루 한 번, 그날 처음 들어올 때만 뜬다. 본 날짜를 `bible_med_goal_popup_seen` 에
한국 날짜로 적어 둔다. 예전에는 탭을 옮길 때마다 떠서 성가셨다.

### 챌린지 — 진행률을 따로 세지 않는다
**성경통독의 `completedChapters` 를 그대로 읽는다.** (`server/challenge.ts`)
통독에서 '읽음'을 누르면 챌린지 진행률에 바로 반영되고, 두 숫자가 어긋날 일이 없다.
읽음 표시 형식은 `"마가복음 3장"` — 이 형식이 바뀌면 챌린지도 같이 깨진다.

| API | |
|---|---|
| `GET /api/challenges/current` | 챌린지 + 참가자별 진행률. **여기서 완료 판정도 한다** |
| `POST /api/challenges` | 시작 (관리자). 한 번에 하나만 |
| `POST /api/challenges/:id/participants` | 참가자 바꾸기 (관리자) |
| `POST /api/challenges/:id/join` | 나도 참가 / `{leave:true}` 로 그만두기 |
| `POST /api/challenges/:id/finish` | 마치기 (관리자) |
| `GET /api/challenges/books` | 66권 목록 |

완료 판정: **목표일이 지났거나 참가자 전원이 다 읽으면** `done` + `completedDate`.

### 탭 규칙
```
평소:      오늘말씀 · 감사칭찬 · 묵상나눔 · 성경통독 · 나의기록 · 알림설정
챌린지 중: 오늘말씀 · 챌린지  · 묵상나눔 · 성경통독 · 나의기록 · 알림설정
```
- 감사칭찬과 묵상나눔의 **자리를 맞바꿨다** (감사가 앞)
- 챌린지가 도는 동안 감사 탭은 접힌다. 끝난 **당일까지는** 챌린지 탭이 남아 축하하고,
  **다음 날** 감사 탭이 돌아온다 (`visibleChallenge()` 가 날짜로 판정)
- 관리자는 챌린지가 없을 때도 그 화면에 들어갈 수 있다 — 거기서 시작하기 때문.
  입구는 **알림설정 탭 맨 위 '성경읽기 챌린지' 카드**

`TAB_DEFS` / `visibleTabs` 한 곳에서 정의하고 **PC 탭 바와 모바일 하단 바가 같이 따라온다.**
예전에는 두 바에 버튼을 각각 손으로 써 두어 하나만 고치면 서로 어긋났다.

### 알림
챌린지 참가자에게는 각자 정한 알림 시간에 **진행률 문구**가 대신 간다
(`challengeAlarmText`). 시간·요일 설정은 그대로 쓰고 내용만 바뀐다.

### ⚠️ 또 걸린 함정
스크립트로 코드를 넣을 때 **정규식의 백슬래시가 삼켜졌다.**
`/^\d{4}-\d{2}-\d{2}$/` → `/^d{4}-d{2}-d{2}$/` 가 되어 어떤 날짜도 통과하지 못했다.
(8월에 PIN 검사에서 똑같이 당했다) → 코드에 정규식을 넣을 때는 백슬래시를 피하거나
넣은 뒤 반드시 `grep` 으로 확인할 것.

### 시험
서버 18항목 통과 (권한·중복 시작 차단·통독 연동·중간 참가·완료 판정·삭제).
화면 확인: 탭 순서 교체, 챌린지 중 감사 탭 숨김, '읽으러 가기' → 통독 탭 이동.

---

## §12. 영성일기·챌린지가 재시작마다 사라지던 버그 (2026-09-01)

### 증상
일기를 쓰고 저장하면 그 자리에서는 보이는데, 나중에(배포 후·컨테이너 재시작 후)
다시 들어가면 목록이 텅 비어 있었다.

### 원인
`syncAndRefreshWithFirestore()` 가 로컬/원격을 합칠 때 `mergedDb` 를
**필드를 손으로 하나씩 적어** 만든다. 여기에 `journals` 와 `challenges` 를
적어 넣지 않아서, 서버가 뜰 때마다 두 항목이 `undefined` 가 되어 메모리에서 사라졌다.
(같은 실수를 `pushSubscriptions` 에서 이미 한 번 겪었고 주석까지 달아 뒀는데 또 했다.)

### 데이터는 살아 있었다
`saveList()` 의 안전장치 — "목록이 N건 → 0건이 되면 삭제를 건너뛴다" — 가
Firestore 의 낱장을 지키고 있었다. 확인 결과 `communities/cho/journals` 에 1건 그대로.
안전장치가 없었다면 교인이 쓴 글이 실제로 지워졌을 것이다.

### 고친 것
1. `mergedDb` 에 `challenges`, `journals` 를 id 기준 합치기로 추가.
2. `getEmptyDb()` 에도 두 칸 추가.
3. **마지막 그물** — 합치기 목록에 적히지 않은 키가 있어도
   `remoteDb[key] ?? localDb[key]` 로 무조건 들고 간다.
   앞으로 `DatabaseSchema` 에 필드를 더해도 조용히 지워지지 않는다.

### 새 필드를 추가할 때
`DatabaseSchema` 에 목록을 하나 더 붙이면 확인할 곳:
- `server/firebaseDb.ts` 의 `SPLIT_KEYS` (계속 쌓이는 목록이면)
- `server.ts` 의 `getEmptyDb()`
- `server.ts` 의 `mergedDb` (이제는 마지막 그물이 받쳐 주지만, 합치는 규칙이
  필요하면 여전히 직접 적어야 한다 — 예: id 로 합칠지, 최신 것만 둘지)

---

## §13. 새 비주얼 디자인 적용 — 그라데이션 헤더 · 테두리 없는 카드 (2026-09-04)

### 배경
Artifact 시안(claude.ai/code/artifact/07a4996a-...)으로 여러 라운드 확정한 새 디자인을
실제 코드에 반영했다. **겉모습만 바꾸는 작업** — 서버·상태·이벤트 핸들러는 전혀
건드리지 않았고, 특히 탭 좌우 스와이프(`App.tsx`)와 성경 장 좌우 스와이프
(`BibleReader.tsx`)의 `useSwipe`/`data-no-tab-swipe`/`dragRef` 구조는 그대로 뒀다.

### 색 토큰 (`src/index.css`)
```css
--color-mist: #F9F9F9;        /* 박스 채움색(외곽선 없이 배경차로만 구분) */
--color-mist-hover: #F0F0F0;
--color-teal: #2B8577;        /* 구약 */
--color-teal-deep: #195C50;
```
그리고 두 헬퍼 클래스:
- `.grad-forest` → `linear-gradient(135deg, #2F7358, #153A2B)` — 헤더·신약·기본 CTA
- `.grad-teal` → `linear-gradient(135deg, #2B8577, #195C50)` — 구약·보조 CTA

새 버튼/카드를 만들 때는 색을 직접 hex로 반복해 적지 말고 이 두 클래스를 쓸 것.

### 구조 변경 (`App.tsx`) — 이 세션 이전에 이미 반영됨
- 헤더가 `.grad-forest` 그라데이션으로, 톱니바퀴 아이콘이 생겼다.
- **설정이 하단 탭에서 빠지고 톱니바퀴로 이동** (`showSettings` state). 하단 탭 6→5개.
- `?tab=settings` 딥링크·푸시 postMessage 모두 `setShowSettings(true)`로 분기 처리됨.

### 이번에 한 것 — 나머지 화면 리스타일
- **`DailyNotice.tsx`**: 말씀 카드 회색 박스는 이전 세션에 이미 제거되어 있었음(코드에
  "상자를 뺐다" 주석 다수 발견) — 이번엔 색만 새 팔레트로. '체크인 한 동역자들'
  이름 목록을 **기본 접힘 + 클릭해서 펼치기**(`showReaders` state)로 바꿔 비중을 줄이되
  기능은 그대로 뒀다.
- **`BibleReader.tsx`**: 마지막 읽은 곳 + 구약/신약 버튼을 하나의 `#F9F9F9` 박스로
  묶고 `border-t` 구분선 추가. 구약 버튼 `.grad-teal`, 신약 버튼 `.grad-forest`.
  이전/다음 장, 통독완료, 묵상쓰기 버튼 전부 그라데이션으로. **스와이프 관련 JSX는
  한 줄도 안 건드림.**
- **`MeditationFeed.tsx` / `DailyGratitude.tsx`**: 글쓴이 이름의 노란 알약 배지
  (`bg-[#FFBA00]`)를 평범한 굵은 텍스트로. 속(sok) 필터 탭바는 원래 있던 기능
  그대로 두고 활성 탭만 그라데이션으로. 글쓰기 버튼들 그라데이션 적용.
- **`MyMeditations.tsx`**: 히어로 카드(목표 진행률) `.grad-forest`, 검색+필터
  카드는 `#F9F9F9`/외곽선 없이. "말씀 체크" 필터가 `.grad-teal`로 구분되게.
  (시안의 "두 박스 합치기"는 이 화면 구조와 안 맞아 적용 안 함 — 목적이 다른
  두 카드라 억지로 합치면 오히려 어색함)
- **`NotificationSettings.tsx`**: 글씨 크기 카드를 4칸 그리드 + 트랙 배경으로
  세그먼트 컨트롤처럼 바꿈(`scales` 배열·`onClick` 로직은 그대로, 마크업만).
  요일 선택 칩·PIN 변경 버튼도 그라데이션/외곽선 제거.
- **`CommunitySettings.tsx`**: 저장·초대문구 복사 버튼 `.grad-forest`.
- 남아있던 실제 박스 테두리(`border-[#E3E9E2]` 류, 장식용 아님) 전부 제거하고
  배경색 대비로 대체 — 체크박스 등 기능상 필요한 테두리는 그대로 둠.

### 확인한 것 / 안 한 것
`npm run build` 통과 확인(타입 에러 없음). **`npm run dev` 로 직접 눌러보며 확인은
안 했다** — 이 프로젝트의 개발 서버는 실제 운영 중인 Firestore(진짜 교인 데이터)에
그대로 붙는데, 로그인 계정 정보가 없어 자동화로 만지는 게 부적절하다고 판단해서다.
다음에 이어서 확인할 사람은 실제 로그인 후: 탭 스와이프, 장 스와이프, 톱니→설정→
뒤로가기, 오늘말씀 번역본 전환/읽음체크/동역자목록 펼치기, 통독완료/구약신약 팝업,
속 필터, 글씨크기 세그먼트가 실제로 커지는지를 확인할 것.

---

## §14. §13 이후 — 색만으로는 부족해서 구조까지 시안대로 맞춤 (2026-09-04)

### 배경
§13에서 색만 바꿨더니 목사님이 스크린샷을 여러 번 보내며 "시안이랑 아직 많이 다르다"고
지적하셨다. 실제로 놓친 것들:
- 헤더가 초록으로 안 보임 → `FontSizeControl` 버튼이 자체 흰 배경(`bg-[#F5F5F5]`)을 가지고 있어
  좁은 화면에서 헤더 대부분을 덮어버림. 톱니처럼 반투명 원형 버튼(`bg-white/15`)으로 교체.
- `DailyNotice.tsx` 제목이 옛 구조("오늘의 말씀 공지" + 아이콘뱃지 + 고정 캡션) 그대로였음
  → "오늘의 말씀" + "{구절} · {날짜}" 한 줄로 교체. 날짜도 저장된 `"2026-09-04"` 형식을
  그대로 보여주고 있어서 `formatKoreanDate()` 헬퍼를 새로 만들어 "9월 4일 금요일"로 변환.
- `MeditationFeed.tsx`에 있어야 할 탭 제목("묵상 나눔" + 부제)이 아예 없었음 — 추가.
- 여러 화면이 시안의 얇은 `.nav-card`/`.post-card`/`.row` 구조가 아니라 옛날처럼
  진한 그라데이션 히어로 카드, 이중 박스(박스 안에 또 박스), `shadow-sm` 흰 카드를 그대로 쓰고 있었음.

시안 HTML(`cho_settings_design_test.html`)을 처음부터 끝까지 다시 정독해서
`.page-title`/`.nav-card`/`.post-card`/`.mini-avatar`/`.row`/`.chip`/`.segment` 등
실제 px 값·색까지 그대로 뽑아낸 뒤, 그 스펙으로 구조 자체를 다시 짰다.

### 이번에 구조까지 바꾼 것
- **`src/index.css`**: `--color-mist`/`--color-teal` 계열에 `.grad-forest`/`.grad-teal`
  헬퍼 클래스 추가 (135deg 그라데이션, 신약=진초록/구약=청록). 새 버튼 만들 때 이 클래스를 쓸 것.
- **`App.tsx`**: 헤더 패딩을 시안 값(`pt-11 pb-8 px-[18px]`)에 맞추고, `<main>`을
  `-mt-[18px] bg-white rounded-t-[22px]`로 감싸 헤더 위로 살짝 겹쳐 올라오는
  "흰 시트" 효과를 넣었다(시안의 `.sheet`). 페이지 배경을 `#F0F0F0` → `bg-white`로
  바꿨다 — `#F9F9F9`/`#F0F0F0` mist 카드들은 흰 배경 위에서만 대비가 산다.
  설정 화면의 계정/안내 박스를 흰 카드에서 `#F9F9F9` row-group(아이콘 원형 칩 + 글 + 화살표)으로.
- **`DualBibleText.tsx`/`FormattedBibleText.tsx`**: 절 번호가 `w-[18px] text-right`
  고정폭 우측정렬이던 것을 시안처럼 자연스러운 좌측 흐름으로 바꿈(번호가 제목과 같은
  왼쪽 선에서 시작하게).
- **`DailyNotice.tsx`**: 제목 구조 교체(위 참조), 바깥 흰 카드·안쪽 `bg-white` 이중 래핑 제거,
  `pl-[10px]` 류 정렬 보정값 제거(이제 필요 없음).
- **`BibleReader.tsx`**: 그라데이션 히어로 카드를 없애고 진행률+마지막 읽은 곳+구약/신약을
  전부 하나의 `#F9F9F9` `.nav-card`에 통합(목표설정·체크리스트는 작은 아이콘 버튼으로 축소).
  본문을 감싸던 흰 카드(`bg-white rounded-3xl shadow-sm`)를 없애고 장 제목을 상자 밖으로 뺐다.
  **스와이프 관련 JSX(`data-no-tab-swipe`/`swipeHandlers`/`dragRef`)는 이번에도 손대지 않음.**
- **`MeditationFeed.tsx`**: 탭 제목 추가, 글쓰기 버튼 두 개를 `flex-1` 동일 너비로
  (순서도 시안대로 나누기→영성일기), 필터 바를 버튼 아래로 이동. 글 카드를
  `bg-white shadow-sm` → `bg-[#F9F9F9]` 무그림자로, 본문을 감싸던 이중 박스 제거.
- **`DailyGratitude.tsx`**: 진초록 그라데이션 히어로 배너를 통째로 제거하고 다른 탭과 같은
  평범한 페이지 제목 + 전체 너비 버튼으로. 글 카드에 미니 아바타(연녹 원, 이름 마지막 글자) 추가,
  이중 박스 제거.
- **`MyMeditations.tsx`**: 그라데이션 히어로 + 진행률 막대를 없애고, 시안처럼 "말씀
  체크리스트"/"내 나눔 목표" 평범한 두 행(`nav-card`)으로 — 실제 통계(이번 달 묵상/감사 횟수,
  체크해 둔 구절 수)는 각 행의 부제 텍스트로 그대로 살려서 정보 손실 없게 함.
  "내가 쓴 글" 섹션 라벨 추가, 카드 스타일 통일.
- **`NotificationSettings.tsx`**: 제일 큰 작업. "알림"/"계정" 섹션 라벨 + `#F9F9F9`
  row-group으로 전면 재구성. 가입코드·지체 권한 관리처럼 내용이 많은 항목은
  `CommunitySettings.tsx`와 같은 패턴(평소엔 행만 보이다가 누르면 펼쳐짐)으로 처리해서
  기능은 하나도 안 줄이면서 시안의 얇은 리스트 느낌을 유지했다.
- **`CommunitySettings.tsx`**: "가입코드·초대하기"/"지체 권한 관리"를 누르면 펼쳐지는
  행으로 바꿈(`showJoinCode`/`showMembers` state 추가) — 안의 실제 기능(재발급, 복사,
  공유, 권한 변경)은 전부 그대로.
- **`ReactionBar.tsx`**: 비활성 반응 버튼 배경을 `#F5F5F5` → `white`로 (카드가
  `#F9F9F9`가 되면서 거의 안 보였음).

### 다음에 참고할 것
시안 원본은 `C:\Users\beeeb\Downloads\cho_settings_design_test.html` (artifact:
`claude.ai/code/artifact/07a4996a-adbb-485b-92b5-300025e9d77a`). 새 화면을 만들 때
"이게 시안 어디에 해당하나"부터 그 HTML의 `<section class="panel" data-panel="...">` 를
찾아 대조할 것 — 색만 보고 넘기면 이번처럼 구조 차이를 놓친다.
여전히 `npm run dev` 라이브 확인은 못 했다(운영 DB). 목사님이 시크릿 창 또는
브라우저 캐시 지운 뒤 `localhost:3000` 새로고침해서 확인해야 함.

---

## 15. 대규모 수정 묶음 (2026-09-05) — 성경 읽기 흐름 · 설정 재설계 · 로그인/공동체 화면

목사님이 한 번에 13가지를 주셨다. 번호는 요청 그대로.

### 성경 읽기 흐름 (1·2·8)
- **(1) 본문 아무 데나 누르면 화면이 본문에 맞춰진다.** `BibleReader.alignReader()` /
  `DailyNotice.alignReader()` — `readerRef.scrollIntoView({block:"start"})`.
  **이미 맞아 있으면(±6~28px) 아무것도 하지 않는다** — 절을 고를 때마다 화면이 움직이면 성가시다.
- **(2) 장을 넘기면(버튼·드래그 모두) 자동 정렬 + 1절부터.** `goToChapter()` 에
  `setPendingScroll(true)` 추가. 본문 상자 스크롤은 기존 `result.reference` 효과가 0으로 되돌린다.
- **(8) 성경이 사라지던 현상.** 원인은 `handleSearchQuery(searchStr, keepPrevious)` 의
  `if (!keepPrevious) setResult(null)` — 스와이프만 `true`를 넘기고 있어서
  '이어서 읽기'·권/장 선택·오늘말씀→통독 이동은 전부 본문을 지우고 로딩 카드를 띄웠다.
  **`keepPrevious` 인자를 아예 없애고 항상 보던 본문을 유지**하도록 바꿨다.
  실패해도 본문을 지우지 않고 알림만 띄운다. 대신 장 제목 옆에 작은 "불러오는 중"을 붙였다.

### 성경통독 간소화 (9)
진행률 막대·목표·체크리스트가 본문 위를 차지하던 것을 **한 줄 버튼**(진행률 %·완료/목표·얇은 막대)
으로 줄이고, 내용은 `showProgressModal` 팝업으로 옮겼다. 팝업 안에서 목표 설정 / 통독
체크리스트 모달로 이어진다. (기존 두 모달은 그대로 살아 있음)

### 구절 선택 (6)
`DualBibleText` — 이어진 절을 고르면 한 덩어리로 보이게. `space-y-[11px]` 를 없애고
절마다 `marginBottom`(아래 절도 골랐으면 0) + 모서리 둥글기를 위/아래만 살리는 방식.

### 설정 전면 재설계 (3)
시안(`cho_settings_design_test.html` 의 `data-panel="settings"`)의 짜임 그대로:
**섹션 라벨 + 줄마다 따로 떨어진 옅은 상자(.row) + 누르면 팝업**.
- 새 파일 **`SettingsUI.tsx`** — `SectionLabel` / `RowGroup` / `Row` / `Switch` / `SettingModal`
  (휴대폰은 아래에서 올라오는 시트, PC는 가운데 창). 다른 화면에서도 재사용 중(BibleReader 진행률).
- 새 파일 **`ProfileModal.tsx`** — 이름·PIN 수정. 서버(`/api/auth/users/update`)가 이름과 PIN을
  **함께** 요구하므로 어느 문으로 들어와도 두 칸을 다 보여준다(제목·설명만 다름).
- 새 파일 **`AccountSettings.tsx`** — 계정 묶음(내 프로필 / 비밀번호 변경 /
  관리자는 지체 계정 관리, 지체는 탈퇴 / 로그아웃). 관리자용 PIN 초기화·계정 삭제는 팝업 안에 그대로.
- `NotificationSettings.tsx` 는 **글씨 크기 + 알림만** 담당하도록 정리(계정 부분은 위로 이관).
  글씨 크기는 시안의 슬라이딩 세그먼트(흰 알약이 미끄러진다). 아침 묵상 알림·지체별 현황은 팝업.
- `CommunitySettings.tsx` 도 펼침 → 팝업 방식으로.
- **머리말 이름 단추 차림표**: 내 프로필 / 비밀번호 변경 / 다른 이름으로 로그인 / 로그아웃.

### 흔들림·겹침 (11·12)
- **(11) 탭 전환이 출렁이던 것** — `y: 15 → -15` 로 위아래로 움직이던 모션을
  `openTab(tab, dir)` 로 방향을 받아 **가로로만** 미끄러지게 바꿨다(0.16s).
- **(12) 머리말 위 여백** — `env(safe-area-inset-top) + 1.25rem`.
  아이폰(노치)·갤럭시(설치형)에서는 기기가 알려주는 높이만큼 자동으로 더 준다.
  `index.html` 에 `viewport-fit=cover` 가 있어야 값이 읽힌다(이미 있음).

### ⚠️ z-index 함정 (이번에 고침, 다음에 또 걸릴 수 있음)
`<main>` 에 `z-10` 이 있으면 **그 안에서 여는 모든 팝업이 하단 탭 바(z-50) 아래로 깔린다**
(쌓임 맥락이 10에 갇힌다). `main` 의 z-index 를 빼고 `relative` 만 남겼다 —
머리말 위로 겹쳐 올라오는 것은 DOM 순서만으로 된다. 같은 이유로 계정 차림표는
`<header>`(overflow:hidden) 밖으로 빼서 `fixed` 로 그린다.

### 나머지
- **(5) 묵상나눔 글씨** — 이름 `text-xs → text-base/lg`, 본문 `text-sm → text-base`,
  날짜·배지·댓글·필터 알약도 한 단계씩. rem 기반 토큰만 써서 '글씨 크기' 설정과 같이 커진다.
- **(7) 쓰던 묵상 보존** — `localStorage["cho_med_draft_<userId>"]` 에 글자를 칠 때마다 저장,
  다시 들어오면 되살린다(창은 안 연다 — 버튼이 "묵상 이어쓰기"로 바뀐다).
  올리면/‘취소’를 누르면 지운다. '닫기'는 글을 남긴다. 오늘말씀에서 구절을 가져오면
  **기존 글을 지우지 않고 위에 얹는다.**
- **(10) 좋아요** — 누르면 붉은 하트(#E0455A)로 채워지고, 기도할게요는 청록(#2B8577).
- **(13·14) 로그인 / 공동체 선택 화면** — 앱 안쪽과 같은 짜임(초록 그라데이션 머리말 +
  올라오는 흰 시트 + `#F9F9F9` 입력칸 + 그라데이션 버튼)으로 다시 그렸다. 로직은 그대로.

### 확인한 것 / 못 한 것
`npm run build`·`tsc --noEmit` 통과. 로그인 화면과 공동체 선택 화면은 실제 브라우저로 확인함.
**로그인 안쪽 화면들은 운영 DB라 계정 없이 확인 못 했다** — 목사님이 직접 눌러보셔야 한다.

### 15-1. 배포 직전 미세 수정 (같은 날)
- **반응 버튼**: 글씨는 늘 `#6F8377` 그대로 두고 **아이콘 색만** 바뀐다
  (좋아요 `#E0455A` 하트, 기도할게요 `#2B8577`). 글씨까지 물들면 목록이 알록달록해진다.
- **`SettingModal` 위치**: 휴대폰에서 아래로 올라오던 시트를 **화면 가운데**로 바꿨다.
  아래에 뜨면 하단 탭 바 근처라 손에 가리고 눈이 먼저 가지 않는다.
- **정렬 범위 확대**: `alignReader` 를 본문 상자가 아니라 **탭 화면 전체**에 걸었다.
  대신 `button, a, input, select, textarea, label, .fixed` 안을 누른 경우는 건너뛴다
  (버튼이 할 일을 방해하지 않고, 팝업 안을 눌렀을 때 뒤 화면이 움직이지 않게).

### 15-2. 팝업 위치 — 전부 화면 맨 위로
목사님 지적: 영성일기·통독 진행률 창이 화면 **아래**에 떠서, 버튼을 누르고도 눈을 한참
내려야 보였다. 세 창을 모두 `items-start` + `pt-[calc(env(safe-area-inset-top)+0.75rem)]`
으로 바꿔 화면 맨 위에 붙게 했다 (등장도 아래→위가 아니라 위에서 살짝 내려오는 쪽으로).
- `SettingsUI.SettingModal` (설정 전부 + 통독 진행률)
- `JournalModal` (영성일기)
- `BibleReader` 의 성경 선택(권/장/절) 팝업 — 마지막 남은 '아래에서 올라오는' 창이라 같이.
창 높이는 `max-h-[calc(100vh - env(safe-area-inset-top) - 1.5rem)]`, 내용이 길면 창 안에서만 스크롤.

### 15-3. 마무리 조정 (같은 날)
- **묵상 나눔 글씨** 한 눈금 축소: 본문 `1rem → 0.92rem`, 이름 `1rem(PC 1.125rem) → 0.97rem`,
  구절·기도제목·댓글 `0.875rem → 0.88rem`. rem 기준이라 '글씨 크기' 설정과 함께 커지는 건 그대로.
  (앱 전체가 `html{font-size:111%}` 이라 1rem ≈ 17.8px 이라는 걸 기억할 것)
- **반응 아이콘 색** — 좋아요·기도할게요 둘 다 `#FFBA00`.
  머리말 오른쪽 이름 동그라미와 같은 색이 이 앱의 포인트 색이다. 글씨는 `#6F8377` 고정.
- **머리말 조명** — 빛 이동 10초, 흰빛 13%, 바탕 흐름 22초.
- **화면 꺼짐 방지** (`src/lib/keepAwake.ts`, `useKeepAwake`) — 오늘 말씀·성경통독 화면에서
  Wake Lock 을 건다. 한 구절을 곱씹으면 손을 안 대니 금방 꺼졌다.
  탭을 떠나면 놓아주고, 다른 앱 갔다 오면 다시 건다(visibilitychange 로 재획득 — 시스템이
  화면을 숨기면 잠금이 저절로 풀리므로 이 처리가 없으면 한 번 나갔다 오면 안 걸린다).
  안드로이드 크롬·삼성인터넷 정상, 아이폰은 Safari 16.4+ 필요, 절전 모드면 거절될 수 있다.

### 15-4. ⚠️ 팝업 뒤 검은 막이 화면을 다 안 덮던 문제 (원인 = 내가 만든 것)
목사님 지적: "팝업창 뒤 검은 박스가 애매하게 조금만 있다."

**원인.** `position: fixed` 는 보통 화면을 기준으로 자리를 잡지만, 조상 중에 `transform`
또는 `will-change: transform` 이 걸린 것이 하나라도 있으면 **그 조상 상자**가 기준이 된다.
11번(탭 전환 부드럽게)을 하면서 탭 화면 `motion.div` 에 `will-change: transform, opacity`
를 걸어 둔 탓에, 탭 안에서 여는 모든 팝업의 `inset-0` 이 화면이 아니라 탭 내용 크기로
줄어들었다. 브라우저에서 재보니 1280×720 이어야 할 막이 300×200 으로 나왔다.

**고친 방법 (두 겹).**
1. `src/components/ModalPortal.tsx` 신설 — `createPortal(children, document.body)`.
   탭 안에서 여는 팝업을 전부 이걸로 감쌌다. body 밑에는 그런 조상이 없으니 어떤 화면에서
   열든 늘 화면 전체를 덮는다.
   적용: BibleReader(성경 선택·목표·체크리스트·진행률), MeditationFeed(속 관리·영성일기),
   MyMeditations(말씀 체크리스트·나눔 목표), ChallengeTab(만들기·참가자), SettingsUI(설정 전부).
2. `App.tsx` 의 `tabMotion` 에서 `will-change` 제거 — 원인 자체를 없앴다. 주석도 남겼다.

**다음에 새 팝업을 만들 때:** 탭 화면 안에서 여는 것이면 반드시 `<ModalPortal>` 로 감쌀 것.
안 감싸면 이 문제가 다시 난다 (화면에서는 멀쩡해 보이다가 특정 탭에서만 이상하다).
겸사겸사 챌린지의 두 팝업도 다른 팝업과 같게 화면 맨 위로 맞췄다.

---

## 16. 통독 주간 계획 (2026-09-06)

### 요청
성경통독 진행률 줄의 `>` 를 설정 아이콘으로, 앞의 동그란 아이콘은 제거.
통독 설정에 **1주일 계획**을 — 구약 하루 3장이면 월요일 1~3장 식으로. 누르면 그 성경으로 바로.
**화요일을 못 읽고 수요일에 열면 7~9장이 아니라 4~6장으로 표기.** 읽으면 완료 표시.
목표 설정에서 읽는 요일을 월~금 / 월~토 / 월~일 로 고를 수 있게.

### 자료 구조 (server.ts + types.ts)
`UserBibleProgress` 에 셋을 더했다. 셋 다 없어도 되는 값이라 예전 자료가 그대로 돈다.
- `planScope: 'all' | 'OT' | 'NT'` — 다음에 읽을 장을 어느 순서에서 뽑을지.
  없으면 `targetChapters`(1189/929/260)로 미루어 짐작한다(`scopeOf`).
- `readingDays: number[]` — 0=일 … 6=토. 없으면 월~금.
- `readLog: Record<'YYYY-MM-DD', string[]>` — **그날 읽은 장**.
  이게 없으면 지난 요일 칸에 무엇을 읽었는지 지어내야 한다. `toggleChapter` 처리에서 함께 쌓는다
  (해제는 그날 기록에 있을 때만 뺀다 — 며칠 전 장을 오늘 해제해도 그날 기록은 안 건드린다).

### 계산 (`src/lib/readingPlan.ts`)
`buildWeeklyPlan(progress, today)` 가 이번 주(월요일 시작) 요일별 줄을 만든다.
- **지난 요일**: `readLog` 에 남은 그날 읽은 장만. 없으면 빈 칸("읽지 못한 날"). 지어내지 않는다.
- **오늘·앞으로**: 아직 안 읽은 장을 성경 순서로 늘어놓고 앞에서부터 하루치씩.
  → 하루 밀리면 다음 칸이 저절로 당겨진다. 이게 요청의 핵심이다.
- 오늘 몫을 이미 다 읽었으면 오늘 칸은 읽은 것을 보여주고 완료로, 떼어 뒀던 몫은 내일 칸으로 돌린다.

검증(목사님 예시 그대로): 월 1~3 완료 / 화 빈칸 / **수(오늘) 4~6** / 목 7~9 / 금 10~12.

### 화면 (`BibleReader.tsx`)
- 진행률 줄: 앞 동그라미 제거, 오른쪽 꺾쇠 → 설정 아이콘.
- 진행률 팝업에 "이번 주 계획" — 요일 동그라미(완료=초록✓, 오늘=금색, 예정=회색),
  범위, 상태 글, [읽기 시작]. 줄을 누르면 팝업이 닫히며 그 장으로 넘어간다.
- 목표 설정: 미리 정한 세 목표가 `planScope` 도 함께 정한다. 요일 7칸 토글 +
  월~금 / 월~토 / 월~일 빠른 선택.

### 다음에 손볼 때
계획은 **날짜가 아니라 진도**를 따른다. "며칠에 몇 장" 을 날짜로 못박는 방식으로 바꾸면
밀렸을 때 목사님이 싫어하신 그 동작(7~9장 표기)으로 돌아간다.

### 16-1. 주간 계획 손질 · 시작할 말씀 (2026-09-06)
- 목표 설정 제목에서 **(개인 전용)**, "목표 장 수 (권장 preset)" 에서 **(권장 preset)** 삭제.
- 장 수를 직접 치던 입력칸을 **"시작할 말씀"** 버튼으로 바꿨다. 누르면 권 고르기 팝업이 뜬다.
  고르면 `planStartBook` 에 저장되고, **목표 장 수는 그 권부터 끝까지로 자동 재계산**된다
  (마태복음 → 260장). `planSequence(scope, startBook)` 이 앞쪽 권을 잘라낸다.
- 주간 계획에서 **지나간 날 줄을 없앴다.** 이제 `buildWeeklyPlan` 은 달력 주가 아니라
  **오늘부터 앞으로 7일** 중 읽는 요일만 만든다 (그래서 표가 비는 일이 없다).
  `PlanRow.when` 에서 "past" 를 뺐고 `startOfWeek` 도 지웠다.
- 줄 오른쪽에 **성경 아이콘 버튼**을 두어 누르면 그 본문으로 바로 넘어간다
  (줄 전체를 누르던 방식 → 아이콘만. 줄이 버튼 안에 버튼을 품지 않도록 <div> 로 바꿨다).

### 16-2. 계획 줄 표기 (2026-09-06)
- 작은 글씨를 "월요일/화요일" 대신 **상태**로: `완료` / `지나감`(지난 날인데 안 읽음) /
  `오늘` / `예정`. 완료는 진초록(#195C50)으로 쓰고 오른쪽에 체크를 붙인다.
- **읽은 날은 요일 동그라미에 불이 들어온다**(grad-forest). 안 읽었으면 그대로 회색,
  오늘은 금색. 동그라미 안 글자는 체크가 아니라 늘 요일이다 (어느 날인지 계속 보이게).
- 줄 전체가 버튼 — 요일을 누르든 어디를 누르든 그 본문으로 넘어간다.
  (오른쪽 성경 아이콘 버튼은 뺐다. 버튼 안에 버튼이 없어야 줄 전체를 누를 수 있다)
- '완료 / 지나감' 을 보여주려면 지난 요일 줄이 있어야 해서, 계획표를 다시
  **이번 주(월~일) 중 읽는 요일**로 되돌렸다. 지난 요일은 그날 실제로 읽은 것(readLog)만,
  없으면 "—"에 '지나감'.

### 16-3. '지나감' 대신 앞으로 읽을 장 (2026-09-06)
못 읽고 지나간 날 칸을 "— 지나감" 으로 비워 두지 않는다. 그 몫이 오늘로 넘어온 것이므로
**앞으로 읽을 장**(=아직 안 읽은 맨 앞 하루치)을 대신 넣고 화면에서는 **연하게** 보여준다.
그래서 지난 화요일 칸과 오늘 칸에 같은 범위가 뜰 수 있는데, 이는 "그 몫이 오늘로 넘어왔다"는
뜻이다 — 오늘 칸만 진한 글씨 + 금색 동그라미 + 테두리라 서로 구별된다.
**오늘 칸이 늘 아직 안 읽은 맨 앞 장이라는 원칙(4~6장)은 그대로다.** 이걸 지키려고
지난 칸에 순번을 앞당겨 배정하지 않았다 — 그렇게 하면 오늘이 7~9장이 되어 처음 요청과 어긋난다.
글씨 진하기: 완료·오늘 = 진하게(#14261E), 나머지 = 연하게(#A8B3A9). 상태 글자는 완료/오늘만 쓴다.

---

## 17. ⚠️ 설정이 자꾸 리셋되던 원인 + 주간 계획 규칙 확정 (2026-09-06)

### 설정 리셋 — Firestore 동기화가 새 항목을 버리고 있었다
`server.ts` 의 Firestore 병합(`mergedProgress[uId] = { ... }`)이 **필드를 하나하나 적어 넣는**
방식이었다. 그래서 나중에 더한 `planScope` / `readingDays` / `planStartBook` / `readLog` 가
동기화가 돌 때마다 통째로 사라졌다. 목표를 저장해도 잠시 뒤 원래대로 돌아간 이유가 이것.
**고친 방법:** 최신 쪽(`newer`)을 통째로 펼쳐 담고(`...newer`) 합쳐야 하는 것만 덮어쓴다.
`readLog` 도 날짜별 합집합으로 병합한다.
=> **앞으로 UserBibleProgress 에 항목을 더할 때 이 자리를 고치지 않아도 살아남는다.**
(같은 형태의 병합이 다른 자료에도 있는지 새 필드를 더할 때마다 확인할 것)

### 주간 계획 규칙 (최종)
목사님 지시: "다음 계획이니까 항상 규칙이야." 하루 3장이면
**월 마태복음 1~3 / 화 4~6 / 수 7~9** — 요일에 하루치를 **차례대로** 붙인다.
중간에 하루를 건너뛰어도 이 순서는 **밀리지 않는다** (예전의 '당겨 오기'는 폐기).
- 이번 주 시작점(`anchorBlock`): 이번 주에 읽은 장이 있으면 그중 가장 앞선 하루치,
  없으면 아직 다 못 읽은 첫 하루치. 월요일 몫을 읽어도 월요일 칸이 그 자리에 남는다.
- 읽었는지는 **색으로만** 구별: 읽은 날 = 진초록(#195C50) 글씨 + 요일 동그라미에 불,
  아직 안 읽은 날 = 연한 회색(#A8B3A9). 상태 글자는 '완료'와 '오늘'만.

---

## 18. 리딩지저스 통독 플랜 (2026-09-07)

교회가 나눠 준 **리딩지저스(그리스도중심성경읽기) 통독표**를 앱에 통째로 넣고,
성경통독과 오늘의 말씀 양쪽에서 그 통독표를 따를 수 있게 했다.

### 통독표 자료
- `src/data/readingJesus.ts` — **도구로 만든 파일이다. 손으로 고치지 마라.**
  2025년 통독표 365일치를 그대로 옮겼다. `"창1~4"` 같은 문구를
  `ranges: [["창세기",1,4]]` 로 풀어 두었다. 강해 영상만 있는 날(주일)과
  특별주간은 `ranges` 가 빈 배열이다.
- 통독표 전체 분량은 **1189장** — 성경 전체가 빠짐없이 들어간다 (중복도 없다).
- 인쇄본에는 `2025-10-16` 이 `고전13~15` 로 되어 있어 **고린도전서 16장**이 한 해 통독에서
  통째로 빠져 있었다. 목사님 확인을 받아 `고전13~16` 으로 바로잡았다 (2026-09-07).
  이런 오탈자 수정은 데이터 파일이 아니라 `tools/generate-reading-jesus.mjs` 의
  `CORRECTIONS` 에 적는다 — 통독표를 다시 만들어도 살아남는다.
- `src/lib/readingJesus.ts` — 날짜↔통독표 맞추기.
  **52주(364일)를 한 바퀴**로 돈다. 364는 정확히 52주라서 한 바퀴를 돌아도
  요일이 그대로 맞는다 → "주일엔 강해, 월~토엔 읽기" 짜임새가 해가 바뀌어도 유지된다.

### 성경통독 탭
- 제목 오른쪽에 **[리딩지저스 통독 플랜]** 단추. 켜면 같은 자리가 **[일반 통독]** 이 되어
  언제든 되돌릴 수 있다. (`UserBibleProgress.planMode`)
- 전환할 때 `goalTitle` · `targetChapters` 는 **건드리지 않는다.** 리딩지저스일 때만
  화면에서 "리딩지저스 통독" / 1188장으로 보여준다 → 되돌리면 원래 목표가 그대로 살아 있다.
- 진행률 줄이 "**리딩지저스 통독 진행률** n%" 로 바뀌고, '하루 n장' 대신 '통독표대로'.
- **이번 주 통독표(월~일)가 팝업이 아니라 화면에 늘 있다.** 줄을 누르면 그날 말씀으로 이동.
  다 읽은 날은 요일 동그라미에 불 + 진초록, 아직 안 읽은 날은 연하게 (일반 통독과 같은 규칙).
- **[전체 스케줄 확인]** 단추 → 달 탭이 있는 팝업에서 한 해 통독표를 전부 본다.
- 진행률 팝업에서는 리딩지저스일 때 '목표 설정'이 '전체 스케줄 확인'으로 바뀐다
  (하루 장 수·목표 장 수를 정할 것이 없으므로).

### 오늘의 말씀 (관리자)
- 자동 공지 플래너에 **공지 방식**이 생겼다: `한 장씩 자동 공지` / `리딩지저스 통독표`.
  (`BiblePlan.mode`)
- 통독표를 고르면 **시작할 통독표 날짜** 단추 → 달별 팝업에서 한 날을 고른다.
  고른 날(`rjPlanDate`)이 **오늘**(`rjStartDate`)에 놓이고, 다음 날부터 하루씩 따라간다.
- 저장하면 그날 이미 나간 **자동** 공지를 걷어내고 다시 만든다 → 고른 말씀이 바로 오늘 올라온다.
  (관리자가 손으로 쓴 공지는 `createdBy` 가 달라 건드리지 않는다)
- 읽을 분량이 없는 날(강해 주일·특별주간)은 공지를 만들지 않는다 → 앞 공지가 그대로 남는다.

### 여러 장짜리 공지를 위한 서버 손질
리딩지저스는 하루에 여러 장을 읽힌다. `parseAndGenerateBibleText` 는 한 장짜리만 다룬다.
- `parsePassageTitle()` — `"마태복음 1~3장"`, `"요엘 1장 ~ 아모스 3장"` 을 장 목록으로 푼다.
- `buildPassageText()` — 장이 바뀌는 자리에 `"마태복음 2장"` 같은 **절 번호 없는 줄**을 넣어 잇는다.
  절 번호가 없으니 화면에서는 소제목처럼 보인다.
- `withVerseText` / `canRebuildVerseText` 가 이걸 쓴다 → **본문은 여전히 DB 에 저장하지 않는다.**
- `DualBibleText` — 여러 장이 들어오면 절 번호가 겹친다. 겹치면 번호가 아니라 **순서로**
  두 번째 번역본을 맞춘다.
- `DailyNotice` 의 절 체크 저장은 제목에 `~` 가 있으면 건너뛴다 (책 이름이 잘못 잘리므로).

### 통독표를 다시 만들어야 할 때
`reading_jesus_2025.json` 같은 원본에서 도구로 만든다. 규칙:
줄임말→권 이름 표(`여호수아=수`, `하박국=합`, `스바냐=습` — `bibleBooks.ts` 의 `shortName` 은
이 셋이 잘못 들어 있으니 쓰면 안 된다), `isSectionStart` 인 날은 읽을 분량 없음,
`"창세기1"` 처럼 줄임말 뒤에 글자가 붙으면 편 이름이지 범위가 아니다.

### 팝업 안에서 옆으로 밀면 탭이 넘어가던 문제 (2026-09-07)
전체 스케줄 팝업에서 달 목록을 옆으로 넘기면 감사 탭으로 넘어가면서 팝업이 닫혔다.
**원인:** 팝업은 `createPortal` 로 body 밑에 그리지만 **React 트리로는 탭 안쪽**이라,
팝업에서 시작한 손짓이 `#view-portal` 의 탭 넘김 핸들러까지 그대로 올라온다.
**고친 방법:** `App.tsx` 의 `tabSwipeHandlers.onTouchStart` 에서
`viewPortalRef.current.contains(e.target)` 로 **DOM 상 바깥에서 온 손짓이면 흘려보낸다.**
=> 앞으로 새로 만드는 팝업도 포탈로 그리기만 하면 따로 손댈 것이 없다.

### 이번 주 통독표 자리 (2026-09-07)
본 화면에 따로 두던 상자를 없애고 **통독 진행률 팝업 안**으로 옮겼다.
진행률 줄도 흰 상자(`bg-white rounded-2xl p-3.5`)로 만들어 **상자 아무 데나 눌러도** 팝업이 열린다.

### 오늘의 말씀 관리자 설정을 한 창으로 (2026-09-07)
"📖 말씀 일일 자동 공지 플래너 설정" 이라는 긴 문구와 화면 위 "말씀 수정" 단추를 없앴다.
대신 **[오늘의 말씀 설정]** 한 줄(오른쪽에 톱니 아이콘)을 누르면 팝업 한 창에서 다 한다:
말씀 직접 수정 · 자동 공지 방식(한 장씩 / 리딩지저스 통독표) · 시작 자리 · 활성화 · 저장.
펼침(inline) 대신 `SettingModal` 팝업이라 화면 위쪽이 훨씬 가벼워졌다.

---

## 19. 리딩지저스를 '날짜 없는 순서표'로 다시 짬 (2026-09-07)

목사님이 2026년 통독표(사진 6장)를 주시면서 방향을 바꾸셨다.
**통독표에 박힌 날짜를 따라가지 말고, 공동체가 정한 일정에 순서표를 얹으라**는 것.

### 통독표는 이제 순서만 갖는다
`src/data/readingJesus.ts` 가 날짜를 버리고 **270개 순서표**가 되었다 (45주 × 6일).
강해 영상만 보는 주일·특별주간·쉬는 날은 전부 뺐다 — 읽을 분량이 있는 날만 남는다.
주 제목(1주 창세기 … 45주 요한계시록)은 인쇄본에 적힌 것을 그대로 쓴다
(`tools/generate-reading-jesus.mjs` 의 `WEEK_TITLES`). JSON 의 '편' 이름은 주와 어긋나는
데가 있었다 — 14주는 '유배기'가 아니라 '역대하'다.

2026년 통독표 사진과 한 주씩 맞춰 보았고 45주 전부 같았다. 1189장, 빠짐도 겹침도 없다.

### 날짜는 공동체가 정한다
`BiblePlan` 에 세 가지가 들어간다 (관리자가 오늘의 말씀 설정에서 정한다).

| 항목 | 뜻 |
|---|---|
| `rjStartDate` | 통독을 시작하는 날. **공동체마다 다르다** |
| `rjReadingDays` | 읽는 요일 (0=일 … 6=토). 월~금 / 월~토 / 정한 요일만 |
| `rjBreaks` | 쉬는 기간 (방학·특별주간). 양쪽 끝 날짜 포함 |

`buildRjSchedule(settings)` 가 시작날부터 하루씩 걸어가며 **읽는 요일이고 방학이 아닌 날**에
순서표를 차례대로 얹는다. 방학이 끼면 뒤가 그만큼 밀린다. 하루를 못 읽어도 계획은
당겨지지도 밀리지도 않는다 — 공동체 전체가 늘 같은 날 같은 본문을 본다.

### 화면
- **성경통독**: 진행률 팝업 안 '이번 주 통독표'(월~일). 읽는 날이 아닌 칸에는
  방학 이름 / '쉬는 날' / '통독 시작 전' / '통독 마침' 이 적힌다.
- **전체 스케줄**: 45주를 주별로 쭉 — 열면 이번 주로 저절로 내려간다.
  (달 탭은 없앴다. 이제 날짜가 공동체마다 다르므로 '몇 월'보다 '몇 주'가 기준이다)
- **오늘의 말씀 설정**(관리자): 시작날 · 읽는 요일 7칸 + 월~금/월~토/매일 · 쉬는 기간 추가·삭제.
  아래에 "오늘 올라갈 말씀"과 "마치는 날"이 바로 보인다.

### 없어진 것
`rjPlanDate`(통독표 안에서 시작 자리 고르기)와 52주 순환(`RJ_CYCLE`)은 사라졌다.
통독표가 날짜를 갖지 않으므로 둘 다 뜻이 없어졌다.

### 통독 일정을 개인도 정할 수 있게 (2026-09-07)
관리자만 정하던 리딩지저스 일정을 **지체도 자기 것으로 정할 수 있게** 했다.

- `UserBibleProgress` 에 `rjFollow` (community | personal) 와 개인용
  `rjStartDate` · `rjReadingDays` · `rjBreaks` 가 붙었다.
- 성경통독 진행률 팝업 맨 위 **[내 통독 일정]** 줄 → 팝업에서 *공동체 일정* / *내 일정* 을 고른다.
  공동체를 고르면 그 일정을 보여만 준다 (고치는 것은 관리자 몫).
- 고른 쪽이 비어 있으면 다른 쪽으로 메운다 — 화면이 빈 채로 남지 않게.
  (`rjFollow === "personal" ? rjPersonal || rjCommunity : rjCommunity || rjPersonal`)
- 일정을 정하는 칸은 `components/ReadingJesusScheduleForm.tsx` 하나로 뽑아,
  관리자 화면(오늘의 말씀 설정)과 지체 화면(성경통독)이 **같은 칸**을 쓴다.

오늘의 말씀 자동 공지는 여전히 **공동체 일정만** 따른다 — 공지는 공동체 전체에 하나이므로.

### 앱 사용법 튜토리얼을 화면별·버튼별로 다시 씀 (2026-09-07)
`AppGuide.tsx` 가 탭마다 슬라이드 하나이던 것에서 **장(화면) → 단계(버튼)** 구조로 바뀌었다.

- 9장 39단계 (관리자는 관리자 장까지, 지체는 8장 35단계).
  시작하기 · 오늘 말씀 · 묵상 일기 · 성경 읽기방 · 리딩지저스 · 감사 칭찬 ·
  챌린지 · 나의 기록 · 설정·계정 · (관리자)
- 단계마다 그때 짚는 **버튼 이름을 알약으로** 보여준다 ("읽었습니다", "전체 스케줄 확인").
- 맨 위 목차 알약을 누르면 그 장으로 바로 건너뛴다. 다음/이전은 장을 넘어 이어진다.
- `isAdmin` 을 받아 관리자 장을 붙인다 (`App.tsx` 에서 넘긴다).
- `GUIDE_VERSION` 을 "2" 로 올렸다 → **모든 지체에게 한 번 더 뜬다.**
  설정이 탭에서 빠지고 리딩지저스가 생기는 등 바뀐 것이 많아 한 번은 다시 보여 드리는 게 낫다.

내용을 고칠 때는 `CHAPTERS` 배열만 손보면 된다 — 화면 짜임새는 건드릴 일이 없다.

### 앱 사용법을 실제 화면 위 스포트라이트로 (2026-09-07)
목사님 지적: "튜토리얼 이런 거 말고 진짜 실제 페이지가 보여야지."
따로 그린 슬라이드를 버리고, **지금 보고 있는 진짜 화면을 어둡게 덮고**
설명할 버튼만 뚫어서 금색 테를 두르는 방식으로 다시 만들었다.

**어떻게 뚫나** — 마스크나 SVG 가 아니라, 뚫을 자리 크기의 빈 div 에
`box-shadow: 0 0 0 9999px rgba(6,26,20,.78)` 를 줘서 바깥을 통째로 덮는다.
가볍고 어느 브라우저에서도 안 깨진다.

**짚을 자리** — 각 화면 요소에 `data-guide="이름"` 을 달아 두고 안내가 찾아 쓴다.
지금 22곳. `SettingsUI` 의 `Row` 는 `guide` 라는 짧은 이름으로 받는다.
요소를 못 찾으면(글이 아직 없다든지) 뚫지 않고 가운데 말풍선만 띄운다 — 안내가 멈추지 않는다.

**화면 이동** — 단계마다 `tab` / `settings` 를 들고 있고, `onNavigate` 로 App 에 알려
그 화면으로 먼저 옮긴 뒤 자리를 잰다 (탭 애니메이션 260ms → 스크롤 → 380ms 뒤 측정).

**자리 배치** — 말풍선은 뚫은 자리 아래에 두되 아래가 좁으면 위로 올린다.
조작 단추는 뚫은 자리가 화면 아래쪽이면(하단 탭 막대처럼) 위로 올라간다.

내용을 고칠 때는 `STEPS` 배열만 손보면 된다. 새 버튼을 짚고 싶으면
그 요소에 `data-guide` 를 달고 `STEPS` 에 한 줄 더하면 끝이다.

### 안내를 "한 화면씩 한꺼번에" 로 (2026-09-07, 최종)
한 단계에 버튼 하나씩 짚던 것은 목사님이 "완전 별로" 라고 하셨다.
"그냥 한 페이지에서 한번에 설명하는 게 좋을 듯" → 그 화면의 버튼을 **한 번에 다** 뚫고
아래 설명판에 ①②③ 번호로 한꺼번에 적는다. 카카오·네이버 앱 안내와 같은 방식이다.

- 7화면: 화면 둘러보기 · 오늘 말씀 · 묵상 일기 · 성경 읽기방 · 감사 칭찬 · 나의 기록 · 설정
- 구멍이 여럿이라 box-shadow 로는 안 된다 → **SVG mask** 로 여러 자리를 한 번에 뚫는다.
- 화면 아래쪽(하단 탭 막대)에 뚫은 자리가 있으면 설명판을 그 위로 올린다
  (lowHoles / panelBottom). 안 그러면 설명판이 덮어 버려 번호를 봐도 못 찾는다.
- 못 찾은 자리는 번호가 회색으로만 남는다 — 설명은 그대로 보이고 안내는 멈추지 않는다.

내용을 고칠 때는 PAGES 배열만 손보면 된다.


### 안내를 되돌리고, 앱 색을 새 팔레트로 (2026-09-07)

**안내(앱 사용법)** — 실제 화면에 구멍을 뚫던 두 판(스포트라이트 · 한 화면씩)을 모두 버리고
그 앞의 **장·단계 안내**(9장 39단계, AppGuide 판 2)로 되돌렸다. `git checkout 50e98e6` 로
AppGuide.tsx 와 그때 여기저기 붙였던 `data-guide` 표시를 함께 되돌렸다 — 지금 앱에는
`data-guide` 가 한 곳도 없다. (되돌린 판이 남긴 자취를 찾으려면 `git log 01973a7` 참고)

**색** — 목사님이 주신 그라데이션 표를 따랐다.

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| 크림 | #FFFDEE | 고른 구절 배경 |
| 연둣빛 | #E2FBCE | 설정 아이콘 동그라미 등 밝은 면 |
| 라임 | #E3EF26 | 포인트 — 뱃지 · 이름 동그라미 · 켜진 탭 (글씨는 늘 #0C342C) |
| 초록 | #076653 | 갈래 이름 · 강조 글씨 · 그라데이션 위쪽 |
| 진초록 | #0C342C | 제목 |
| 먹초록 | #06231D | 본문 · 그라데이션 아래쪽 |

- 머리말/버튼 그라데이션: `grad-forest` #0B7A62→#06231D, `grad-teal` #0F8F72→#076653,
  새로 `grad-ink`(#0C342C→#06231D) · `grad-lime` · `grad-cream` · `tabbar-float`.
- **가독성** — 흐린 글씨 #6F8377→#4E7568, 보조 글씨 #4A6B57→#1E6B57,
  '연하게' 쓰던 #A8B3A9→#5E7F71. 흰 바탕에서 거의 안 보이던 상자 #F9F9F9→#EFF6E2 로
  한 단계 내렸다 (설정 줄 · 묵상 글 카드 · 통독 상자가 이제 면으로 구분된다).
- 통독 장 알약이 상자와 같은 색이라 안 보이던 것 → 흰색으로.
- 성경 번역본 글자색도 팔레트 안으로 (NIV 갈색 #8A6642 → 올리브 #5C6A3A).

**하단 탭 막대** — 흰 띠에서 **떠 있는 먹초록 알약**으로 바꿨다. 켜진 탭은 라임 글씨에
반투명 알약이 깔리고, 그 알약이 `layoutId="tabbar-pill"` 스프링으로 **미끄러져** 옮겨 간다
(아이폰 탭 바의 그 움직임). 막대가 떠 있으므로 바깥 `pb` 를 5.5rem→6.25rem 으로,
흰 시트에 `min-h-[calc(100vh-88px)]` 을 주어 막대 뒤가 바탕색으로 비지 않게 했다.

색을 또 바꿀 때는 `scratchpad/recolor.mjs` 같은 **한 벌 치환**으로 하는 편이 안전하다
(색이 코드 곳곳에 arbitrary value 로 박혀 있어 한 곳씩 고치면 반드시 빠뜨린다).

### 앱 색 꾸미기 — 설정에서 직접 고른다 (2026-09-07)

목사님이 "박스며 폰트며 RGB 값을 직접 고르는 창" 을 원하셨다.
설정 → **꾸미기 → 앱 색 꾸미기** (관리자만). 저장하면 **공동체 모두**의 앱 색이 바뀐다.

**어떻게 되는가** — 앱의 색은 화면 코드 곳곳에 `bg-[#EFF6E2]` 처럼 박혀 있다(1,200 곳 남짓).
그걸 다 고치는 대신 **덮어쓰기 규칙**을 만들어 `<style id="cho-user-theme">` 에 넣는다.

```css
[class~="bg-[#EFF6E2]"] { background-color: var(--u-box) !important }
```

`src/lib/theme.ts` 의 `ROLE_HEXES` 가 "이 갈래는 이 색들을 덮는다" 는 표다.
**색을 새로 쓰거나 값을 바꾸면 이 표도 같이 고쳐야 한다** — 안 그러면 그 색만 안 바뀐다.

- 갈래 15개(면 6 · 글씨 5 · 포인트 4) + 그라데이션 3벌(시작·끝·방향) + 글꼴 3가지
- 미리 만든 색 4벌 (기본 초록 / 라임 연둣빛 / 크림·먹초록 / 깊은 숲)
- 고르는 즉시 앱 전체에 입혀진다. **저장 없이 닫으면 되돌아간다**
- 손잡이나 '앱 보기' 를 누르면 창이 접혀 앱을 그대로 만지며 색을 확인할 수 있다
- 꾸미기 창 자체는 앱이 안 쓰는 색(#FEFEFE·#12211C·#F2F3EE)으로만 짰다 —
  어떤 색을 고르든 이 창만은 늘 읽힌다

**어디에 남는가** — `GET/POST /api/theme` · `db.theme`.
POST 는 관리자만, 서버가 `#RRGGBB` 인 값만 걸러 담는다(스타일 자리에 들어가는 값이라).
기기에도 `bible_med_theme` 로 기억해 두고 `main.tsx` 의 `bootTheme()` 이 뜨자마자 입힌다
(서버 값을 기다리면 기본색이 한 번 번쩍인다).

색이 이상해졌으면 꾸미기 창의 **기본색** → 저장이면 원래대로 돌아온다.

### 되돌리기 · 어두운 화면 (2026-09-08)

목사님 말씀: "튜토리얼도 최초로 만든 버전으로 원복해줘, 디자인도 원복해! 다크버전
설정할 수 있게 해줘" — 되돌릴 범위를 여쭈니 **전부 원복**.

**안내(튜토리얼)** — 맨 처음 판(50fdc17)으로 되돌렸다. 탭을 하나씩 소개하는 8장 슬라이드,
옆으로 밀어 넘기고 건너뛸 수 있다. 스포트라이트 판들이 남긴 `data-guide` 표시와
`Row` 의 `guide` 속성도 모두 걷어냈다.

**디자인** — 9월 7일 색 바꾸기(51bf714) 이전 상태로 통째 되돌렸다
(`git checkout 317873c -- <그 커밋이 건드린 파일들>`). 진초록·금색, 흰 하단 탭 막대,
#F9F9F9 상자가 돌아왔다. 새 팔레트가 그리우시면 **앱 색 꾸미기 → 미리 만든 색 →
'라임 연둣빛'** 한 번으로 그 색이 그대로 입혀진다 (프리셋에 담아 두었다).

**어두운 화면(다크)** — 설정 맨 위 **화면**: 밝게 / 어둡게 / 기기 설정 따름.
공동체 색과 달리 **기기마다 각자** 고른다 (`bible_med_theme_mode`).

색을 덮어쓰는 방식은 그대로지만, 어둡게에서는 한 가지가 더 필요했다 —
**같은 갈래가 글씨로도 바탕으로도 쓰인다**. 진초록(#0C3B2E)은 제목 글씨이면서
진초록 단추의 바탕이다(34곳). 글씨는 밝아져야 하지만 단추 바탕까지 밝아지면
그 위 흰 글씨가 사라진다. 그래서 갈래마다 변수를 둘로 나눴다:

- `--u-title`     — 글씨로 쓰일 때
- `--u-title-bg`  — 바탕으로 쓰일 때 (`DARK_SURFACE` 표, 어두울 때만 갈라짐)

이 방식으로 닿지 않는 것(그림자·붉은 알림 상자·금빛 상자 안 글씨)은 index.css 의
`html[data-theme="dark"]` 규칙에서 손본다.

⚠️ 안내 화면(AppGuide)의 색은 일부러 `ROLE_HEXES` 에 **없는** 값이다(#0C3C2F 처럼
끝자리만 다르게). 늘 진초록 한 판이어야 하는데 색이 뒤집히면 흰 글씨가 사라진다.

### 다른 공동체에도 같은 색 · 어두운 화면의 성경 본문 (2026-09-08)

**색 옮기기** — 은혜교회(cho)에서 꾸민 색을 나머지 네 공동체에도 그대로 넣었다.
Firestore 의 `communities/{id}` 문서에서 **theme 칸 하나만** merge 로 썼다
(회원·묵상은 손대지 않았다). 넣은 곳: c-yqz4dcnj · c-sttchadj · c-d67jhhcc · c-5mhmc7az
(모두 색이 없던 상태였다).

⚠️ 서버는 공동체 데이터를 메모리에 올려 두고 저장할 때 문서를 통째로 덮어쓴다
(`communityRef.set(base)`). 그래서 이렇게 밖에서 고친 뒤에는 **새 리비전을 배포해서**
인스턴스가 Firestore 를 다시 읽게 해야 한다. 이번에도 곧바로 배포했다.

**어두운 화면에서 성경이 안 보이던 것** — 본문 색을 화면 코드의 인라인 style 로
박아 두어(`style={{ color: colorOf(...) }}`) 덮어쓰기 규칙이 닿지 않았다. 까만 바탕에
짙은 회색(#333333) 글씨가 그대로 남았던 것. 색을 index.css 의 `[data-bv]` 로 옮기고
화면에는 번역본 표시만 남겼다.

- `[data-bv]` 는 `var(--u-scripture)` 를 쓴다 → '앱 색 꾸미기'의 성경 본문 색도 이제 먹는다
- 대조용 두 번째 번역본(우리말·NIV)만 자기 색을 지키고, 어두울 때는 밝은 쪽으로 바꾼다
- 어두운 화면의 성경 글씨는 #F2F7F3 으로 시원하게

교훈: **색은 인라인 style 로 넣지 말 것.** 넣는 순간 꾸미기·다크에서 그 자리만 남는다.

### 탭 순서 (2026-09-08)

오늘 말씀 → **성경통독** → 묵상일기 → 감사칭찬 → 나의기록.
아침에 말씀을 받고, 이어서 통독하고, 그러고 나서 묵상을 적는 하루 흐름을 따랐다.
`App.tsx` 의 `visibleTabs` 한 줄이 전부다 — PC 탭 줄, 아래 탭 막대, 좌우로 밀어 넘기는
순서가 모두 이 목록 하나를 본다.

### 앱 사용법을 '예시 화면 + 번호 설명' 으로 (2026-09-08)

목사님 말씀: 오늘의 말씀 → 성경통독 → 묵상일기 → 감사칭찬 → 나의 기록 순서로,
지금 기능을 반영해서, 오늘말씀은 **예시 화면을 보여 주며** 설명해 달라
(구글플레이 앱 소개 화면처럼).

7장 — 인사 · 오늘 말씀 · 성경통독 · 묵상일기 · 감사칭찬 · 나의 기록 · 설정.
장마다 **그 화면의 예시 그림**(AppGuide.tsx 안에서 div 로 그린다)을 얹고,
그림의 각 줄에 붙은 번호 ①②③ 를 아래에서 그대로 풀어 설명한다.
알아 두면 좋은 것은 그 아래 옅은 상자에 따로.

- 번호는 그림 **왼쪽 칸**에 세운다(`Row` 부품). 처음에 그림 위에 얹었더니 글씨를 가렸다.
- 예시 그림 색(M 상수)은 `ROLE_HEXES` 에 없는 값만 쓴다 — 꾸미기·다크에 흔들리면
  '앱은 이렇게 생겼습니다' 가 거짓말이 된다.
- 내용은 지금 기능 기준: 번역본 대조 · 구절 골라 묵상 · 리딩지저스 통독 플랜 ·
  속 나눔 · 영성일기 · @이름 · 체크리스트 · 나눔 목표 · 밝게/어둡게 · 글씨 크기 · 알림.
- 안내 판 번호를 5 로 올렸다 (모두 한 번 더 보게 된다).

지금도 `GUIDE_FOR_ADMIN_ONLY = true` — 관리자에게만 뜬다. 모두에게 열려면 false 로.

### 안내 색 · 새 프리셋 · 통독을 공동체 대표로 (2026-09-08)

**안내(튜토리얼)도 앱 색을 따라간다** — 전에는 색이 뒤집혀 글씨가 사라질까 봐 진초록으로
못 박아 두었다. 이제 갈래 이름(CSS 변수)으로만 쓰고 **뜻을 짝지어** 안전하게 만들었다:
바탕은 머리말 그라데이션 + 옅은 검은 막(0.30) → 그 위 글씨는 늘 흰색,
포인트(--u-point) 위 글씨는 늘 진한 색(#2A2213). 예시 그림도 --u-card/--u-title/--u-box …
를 쓰므로 꾸미기·어둡게가 그대로 반영된다.

**미리 만든 색 넷** — colours.cafe #336 · #186 · #341 · #54 를 옮겼다
(민트 그린 · 코랄 크림 · 바다 파랑 · 모래 노랑). 표의 색을 그대로 못 쓰는 자리가 있다:
'보조 강조'는 흰 글씨를 얹는 단추 바탕이라 밝으면 글씨가 사라져 한두 단계 내려 썼다.

**기본색** — `APP_DEFAULT_THEME` 신설. 꾸미기의 '기본색' 단추가 되돌리는 곳이
지금 앱이 입고 있는 색(은혜교회에서 맞춘 청록·코랄)이 되었다.
`DEFAULT_THEME` 은 '화면 코드에 박힌 값' 이라 덮어쓰기 기준으로만 남는다 — 헷갈리지 말 것.

**통독을 공동체 대표로** — 성경통독 → 진행률 → 내 통독 일정에서, **관리자**가
'공동체 일정' 을 고르면 읽기 전용 요약 대신 **일정 폼**이 열린다.
[공동체 전체 일정으로 정하기] 하나로 ① 공동체 일정을 따르는 모든 지체의 통독표와
② `<오늘의 말씀>` 자동 공지가 함께 정해진다 (`POST /api/bible-plan` · mode readingJesus · active).

⚠️ 그 API 에 **관리자 확인이 없었다** (누구나 공동체 통독·자동 공지를 바꿀 수 있었음).
이번에 `requireAdmin` 을 붙였다. 오늘의 말씀 설정도 관리자 화면이라 영향 없다.

**오늘의 말씀 제목** — 공동체가 리딩지저스로 도는 중이면 제목이
**'리딩지저스 오늘의 말씀'** 으로 바뀐다. 그러려면 통독 방식을 모두가 알아야 해서
`fetchBiblePlan()` 을 관리자 전용에서 전체로 풀었다 (GET 은 원래 공개).

### 초대 링크 · 카톡에서 설치 (2026-09-10)

**초대 링크** — `https://…/?join=가입코드` 를 누르면 **그 공동체 로그인 화면이 바로** 열린다.
코드를 옮겨 적는 세 걸음(주소 치기 → '다른 공동체로 들어가기' → 코드 입력)이 사라졌다.

- `src/lib/invite.ts` 의 `applyInviteFromUrl()` 이 **화면을 그리기 전에**(main.tsx) 돈다.
  그래야 로그인 화면이 처음부터 초대받은 공동체로 열린다. 초대가 아니면 곧바로 그린다.
- 서버는 그대로 `POST /api/communities/resolve` 를 쓴다 (새 API 없음, IP 횟수 제한 그대로).
- 다른 공동체에 로그인해 있었다면 그 증표를 놓고 간다. 자료는 그대로 있고,
  로그인 화면의 '다른 공동체로 들어가기' 에서 한 번에 돌아갈 수 있다.
- 주소창의 `?join=` 은 곧바로 지운다 (새로고침해도 다시 처리되지 않게).
- 초대 문구(설정 → 우리 공동체 → 가입코드·초대하기)도 **링크 한 줄**로 바꿨다.
  코드는 링크 안에 들어 있고, 새로 발급하면 옛 링크는 그 순간 통하지 않는다.

**카톡에서 바로 설치는 안 된다** — 홈 화면 설치는 브라우저만 할 수 있는 일이라,
카카오톡·인스타 같은 '앱 안의 브라우저' 에서는 아무리 눌러도 설치되지 않는다.
그래서 그런 브라우저로 열렸을 때 위쪽에 안내를 띄우고 **[브라우저로 열기]** 를 준다
(`InAppBrowserNotice`). 넘어가면 원래 있던 설치 안내(PWAInstallPrompt)가 저절로 뜬다.

- 카카오톡: `kakaotalk://web/openExternal?url=…` (안드로이드·아이폰 모두 열린다)
- 라인: 주소에 `openExternalBrowser=1`
- 그 밖의 안드로이드: `intent://…;package=com.android.chrome;end`
- 아이폰의 나머지 앱: 길이 없다 — "오른쪽 아래 ⋯ → Safari로 열기" 안내만 드린다

### 가입코드를 눈앞에서 치웠다 (2026-09-10)

목사님 말씀: "가입코드는 왜 있어야 해? 이제 필요 없잖아."

맞다 — **옮겨 적는 코드**는 이제 필요 없다. 다만 그 여섯 자리는 초대 링크 안에 들어 있는
**열쇠**라서 없앨 수는 없다 (없애면 주소만 알면 아무나 그 교회 묵상을 볼 수 있다).
그래서 지우는 대신 **눈앞에서 치웠다**.

- 설정 → 우리 공동체: '가입코드 · 초대하기' → **'초대하기'** (링크만 보인다)
- 창 안에서도 링크가 먼저다. 여섯 자리는 맨 아래 '링크가 새어나갔나요?' 를 눌러야 나오고,
  버튼 이름도 '새로 발급' → **'링크 새로 만들기'** 로 바꿨다 (누르면 옛 링크가 막힌다)
- 공동체 문 앞(CommunityGate): '가입코드로 들어가기' 를 큰 메뉴에서 뺐다.
  맨 아래 작은 글씨 '초대 링크 대신 코드를 받으셨나요?' 로 남겨 두었다 —
  링크를 잃어버리셨을 때의 뒷문이다.

즉 코드는 **기능은 그대로, 화면에서만 사라진** 상태다.

### 초대는 누구나 · 앱 색은 각자 (2026-09-10)

**초대는 지체 누구나** — `GET /api/communities/mine` 이 관리자에게만 주던 joinCode 를
**로그인한 지체 모두**에게 준다. 이미 우리 안에 계신 분께 숨길 이유가 없다.
설정의 '초대하기' 줄에서 관리자 뱃지를 뗐다.
다만 **링크 새로 만들기(옛 링크 막기)는 관리자만** — 모두의 링크를 한 번에 끊는 일이라서.

**앱 색은 각자 자기 기기에서** — 공동체 전체에 적용되던 것을 **개인 설정**으로 바꿨다.

- 저장이 서버로 가지 않는다. `bible_med_theme`(localStorage)에만 남는다
- 다른 지체·다른 공동체에는 아무 영향이 없다
- 고른 적이 없으면 `APP_DEFAULT_THEME`(지금 앱 모습)이 기본이다 — 그래서 아무도
  손대지 않으면 지금과 똑같이 보인다
- App.tsx 의 `/api/theme` 불러오기를 뺐다. 서버의 `GET/POST /api/theme` 와
  `db.theme` 는 남아 있지만 **이제 아무도 쓰지 않는다** (나중에 지워도 된다)

**미리 만든 색 넷을 새 색상표로** — videoinfographica #112 · #153 · #54 · #108
→ 파랑 오렌지 · 코랄 네이비 · 모래 노랑 · 청록 노랑.

### 두 가지 고침 (2026-09-10)

**① '읽었습니다' 를 누르면 오늘 말씀 본문이 사라지던 것**

자동으로 올라간 공지는 성경 본문을 **저장하지 않는다** — 구절명만 있으면 되살릴 수 있어서
`verseText: ""` 로 둔다(autoPost* 두 곳). 그래서 내보낼 때마다 `withVerseText()` 로 채워야 하는데,
**읽음 표시 응답에만 그게 빠져 있었다.** 화면은 그 응답으로 공지를 갈아 끼우므로
본문이 빈 채로 남아 글씨가 사라졌다. 공지를 돌려주는 세 곳(읽음·새 글·수정)에 모두 붙였다.

교훈: `db.notices` 를 **그대로 내보내는 자리를 만들지 말 것**. 반드시 `withVerseText()` 를 거친다.

**② 글을 쓰다가 옆으로 밀면 탭이 넘어가던 것**

두 겹으로 막았다.
- `useSwipe` — 손짓이 `input/textarea/select/contenteditable` 에서 **시작**하면 아예 받지 않는다
  (성경 장 넘기기·안내에는 글쓰기 칸이 없어 영향 없음)
- 탭 넘기기 — 어딘가에 **글을 쓰는 중**(`document.activeElement` 가 글쓰기 칸)이면 넘기지 않는다.
  칸 밖 여백에서 손이 스쳐도 쓰던 화면이 넘어가지 않는다

확인: 글쓰기 칸에서 밀기 → 그대로. 평소 화면에서 밀기 → 묵상 나눔 → 감사와 칭찬 (정상).

### 계정은 한 곳에서만 (2026-09-10)

설정 화면 안에 '계정' 묶음(내 프로필·비밀번호 변경·지체 계정 관리 또는 탈퇴·로그아웃)이 있었는데,
같은 것이 **머리말 이름 단추의 차림표**에도 그대로 있었다 — 한 가지를 두 곳에서 관리하는 셈이었다.
계정은 이름 단추 한 곳으로 모았다.

- `src/components/AccountSettings.tsx` **삭제** → `src/components/AccountModals.tsx` 로 바뀜.
  줄(Row)들은 없애고 **팝업 두 개만** 남겼다 — 관리자의 '지체 계정 관리', 지체의 '계정 탈퇴'.
  `mode: "members" | "leave" | null` 로 열고 닫는다. 지체 목록은 화면이 뜰 때가 아니라
  **창을 열 때** 불러온다.
- `App.tsx` — 설정에서 `<AccountSettings>` 를 뺐고, 차림표에 관리자면 '지체 계정 관리',
  지체면 '계정 탈퇴'(빨간 글씨) 를 넣었다. `accountModal` 상태 하나로 `<AccountModals>` 를 연다.

**기능은 하나도 없어지지 않았다** — 지체 비밀번호 초기화·계정 삭제·탈퇴 모두 그대로다.
차림표 항목 배열에는 `danger: false` 를 **모든 줄에** 달아 두었다. 하나만 달면 타입이 갈라져
`item.danger` 를 읽을 수 없다.

확인: 관리자 차림표 5줄(…지체 계정 관리…), 지체 차림표 5줄(계정 탈퇴 = #B3261E),
'지체 계정 관리' 눌러 팝업 열림, 설정 화면에는 계정 글자가 남아 있지 않음.

### 카톡 안내 문구 줄이기 (2026-09-10)

좁은 자리에 버튼과 곁들임 안내를 **나란히** 두었더니 '브라우저로 / 열기' 처럼
낱말이 끊겨 읽기 어려웠다. 문구를 핵심만 남기고 짜임을 세로로 폈다.

- 제목 "{앱} 안에서는 앱 설치가 안 됩니다" → **"{앱}에서는 설치가 안 됩니다"**
- 본문 "브라우저로 열면 홈 화면에 앱처럼 설치하실 수 있습니다." → **"브라우저로 열면 홈 화면에 설치됩니다."**
- 곁들임(`externalHint`) "…⋮ 를 누르고 '다른 브라우저로 열기' 를 골라주세요." → **"…⋮ → '다른 브라우저로 열기'"**
- 버튼은 **한 줄을 통째로** 쓰고(w-full, whitespace-nowrap), 곁들임은 그 아래 한 줄로 내렸다.
  버튼 앞 아이콘(ExternalLink)은 뺐다.

확인: 카카오톡 UA 로 띄워 한 줄씩 끊기지 않고 들어가는 것을 눈으로 봄.

**덧 — 안내가 뒷글자를 덮던 것 (2026-09-10)**

이 안내는 `fixed` 로 화면 맨 위에 떠 있어서, 뒤에 있는 앱 이름·인사말을 **덮어**
글자가 반쯤 잘려 보였다. 문구가 아니라 **겹침**이 원인이었다.

- 안내가 떠 있는 동안 `document.body.style.paddingTop` 에 그 높이를 넣어 화면을 아래로 민다.
  높이는 `ResizeObserver` + `resize` 로 다시 잰다 (글씨 크기·기기 방향이 바뀌면 달라진다).
  닫히거나 사라지면 `paddingTop` 을 되돌린다.
- 한국어 줄바꿈은 `break-keep` (낱말 단위).
- 곁들임 안내는 `<Hint>` 로 뽑아, `→` 뒤(눌러야 할 메뉴 이름)를 `whitespace-nowrap` 으로
  묶었다. '다른 브라우저로 / 열기' 처럼 갈라지면 글이 잘린 것처럼 보인다.

확인: 412 · 360 · 320px 에서 잘리는 글자 없음, 카드 아래로 앱 화면이 밀려 내려옴.

## 처음 오신 분을 위한 세 가지 (2026-09-10)

시안(claude.ai/code/artifact/c940ec3e-…)의 1·3·4번을 넣었다.
**처음 오신 분이 막히는 곳은 기능이 아니라 "지금 뭘 눌러야 하지" 다.**

**① 오늘 함께 읽을 말씀 카드** — `src/components/TodayVerseCard.tsx`

- 오늘말씀이 **아닌** 화면 맨 위에 뜬다. 누르면 곧장 오늘말씀으로 넘어간다.
- 튜토리얼과 같은 차림 — 그라데이션 바탕(`--u-grad-main`)에 포인트색 딱지
  ("관리자가 정한 오늘의 말씀"), 구절명은 포인트색 굵은 글씨.
- **읽음 표시를 하시면 사라진다.** 오늘말씀에서 읽음을 누르면
  `cho:notice-read`(= `NOTICE_READ_EVENT`) 를 쏘고, 카드가 그때 다시 확인한다.
- `App.tsx` 에서는 자리에서 빼지 않고 `hidden` 으로만 감춘다 — 탭을 오갈 때마다
  다시 불러오지 않기 위해서다.

**③ 그 자리에서 한 번만 뜨는 손가락 안내** — `src/components/CoachMark.tsx`

- 두 곳: 성경통독 `bible-swipe`(옆으로 밀면 장이 넘어간다), 오늘말씀 `notice-pick`
  (절을 누르면 그 구절만 골라 묵상을 쓴다).
- 기억은 `localStorage["coach:<id>"]`. 누르거나 12초가 지나면 사라지고 다시 안 뜬다.
- 손가락 동그라미는 `.coach-finger` / `.coach-tap` (index.css). 움직임 줄이기 설정 존중.
- **본문 상자를 감싸지 않는다** — 상자 바로 뒤에 높이 0인 `relative` 칸을 두고
  그 안에서 `absolute` 로 띄운다. 장 넘기기 손짓 배선(`data-no-tab-swipe`,
  `swipeHandlers`, `dragRef`)은 손대지 않았다.

**④ 고른 구절 막대** — `src/components/PickedVerseBar.tsx`

- 첫 구절을 고르는 순간 화면 아래에서 올라온다. 훑어 내려도 따라온다.
- `ModalPortal` 로 body 에 그린다 — 탭 전환에 걸린 transform 때문에 그냥 두면
  `fixed` 가 화면이 아니라 탭 내용을 기준으로 잡힌다.
- 하단 탭 바(z-50) 보다 낮은 z-45, `bottom: env(safe-area-inset-bottom)+4.75rem`.
- 오늘말씀·성경통독이 함께 쓴다. 두 곳의 묵상 쓰기 로직은 `writeWithPicked()` 하나로
  묶어 단추와 막대가 같은 길을 쓴다.

확인: 가짜 응답으로 띄워 카드·안내·막대를 눈으로 봄 (실서버 자료는 건드리지 않았다).

**덧 — 겹치던 단추 정리 (2026-09-10)**

구절을 고르면 아래 막대가 그 일을 다 한다 (몇 구절인지 · 지우기 · 묵상 쓰기).
본문 밑에 있던 같은 단추들은 겹치기만 하므로 **고른 구절이 있을 때는 내린다.**

- 막대 글씨 "{n}구절 고름" → **"{n}구절 체크"**
- 오늘말씀 — '고른 n구절로 묵상 쓰기' · '선택 해제' 없앰. 고른 구절이 **없을 때만**
  '이 말씀으로 묵상 쓰기' 를 남긴다 (구절을 고르지 않고 본문 전체로 쓰는 길이 여기뿐이다)
- 성경통독 — 노란 '{n}개 구절을 골랐어요 / 선택 해제' 띠 통째로 없앰.
  '이 말씀으로 내 묵상 쓰기' 도 고른 구절이 없을 때만 보인다

**① 카드 → 하루 첫 화면으로 (2026-09-10)**

'오늘 함께 읽을 말씀' 은 탭 위의 카드가 아니라 **앱을 하루에 처음 열 때 뜨는
독립된 화면**이 되었다 (튜토리얼과 같은 차림 — 그라데이션 바탕 + 눌러 준 검은 막).
`src/components/TodayVerseCard.tsx` **삭제** → `src/components/TodayVerseIntro.tsx`.

- 화면 아무 곳이나 누르면 사라지고 **그대로 오늘말씀 탭**으로 들어간다 (`onEnter`).
- 하루에 한 번 — `localStorage["todayVerseIntroOn"] = 공지 날짜`. 서버가 준 날짜를
  그대로 쓰므로 기기 시계가 틀어져도 하루 기준이 어긋나지 않는다.
- 이미 **읽음 표시**를 하셨으면 뜨지 않는다. 튜토리얼이 떠 있으면 미룬다(`enabled`).
- 떠 있는 동안에는 목표 요약 팝업을 미룬다 (`introOpen` → `GoalSummaryPopup` 가림).
- 읽음을 알리던 `cho:notice-read` 신호는 카드와 함께 없앴다.

확인: 화면이 뜨고 → 누르면 오늘의 말씀으로 들어가며 → 날짜가 저장되는 것까지 눈으로 봄.

**덧 — 첫 화면 딱지는 통독 방식을 따른다 (2026-09-10)**

"관리자가 정한 오늘의 말씀" 이라고 고정해 두었던 딱지를 방식에 맞춰 바꾼다.
`/api/bible-plan` 을 보고 `active && mode === "readingJesus"` 면 **"리딩지저스"**,
아니면 **"오늘 나눌 말씀"**. 못 받아오면 평소 이름("오늘 나눌 말씀") 그대로 둔다.
(오늘말씀 제목이 '리딩지저스 오늘의 말씀' 으로 바뀌는 것과 같은 기준이다)

확인: 두 방식으로 각각 띄워 딱지가 바뀌는 것을 봄.

**덧 — 첫 화면 짜임 (2026-09-10)**

셋이 가운데 몰려 아래가 허전했다. 위·가운데·아래로 나눠 화면을 고르게 채운다.

- 위: 날짜 ("9월 11일 금요일") — `koreanDate()`
- 가운데: 딱지 · 인사 · 구절명 · **본문 첫 구절 한 줄** (`firstVerse()` — 절 번호를 떼고
  52자가 넘으면 줄인다). 무슨 말씀인지 미리 마음에 얹어 드린다.
- 아래: "화면 아무 곳이나 누르셔도 됩니다"
- `justify-between` + 위아래 안전영역 여백. 390×844 · 360×640 에서 넘침 없음.

**덧 — 첫 화면 다시 보기 (2026-09-10)**

설정 > 안내 에 **"오늘 첫 화면 다시 보기"** 를 넣었다 ('앱 사용법 다시 보기' 바로 위).

- `TodayVerseIntro` 에 `replay` (숫자) 를 넘긴다. 이 값이 바뀌면 한 번 더 띄운다.
- 일부러 부르신 것이므로 그때는 **'오늘 봤음' 도 '이미 읽음' 도 따지지 않는다** (`forced`).
- 눌러서 들어가면 `forced` 를 내리고 오늘 날짜를 다시 저장한다.

확인: 오늘 이미 보고 읽음까지 한 상태에서 → 평소엔 안 뜨고, 눌렀을 때만 뜨는 것을 봄.

**덧 — 오늘 말씀이 아직 없는 날 (2026-09-10)**

`/api/notices/today` 는 오늘 것이 없으면 **가장 최근 공지를 대신 내어 준다.**
그걸 그대로 믿어서 **어제 말씀이 '오늘 함께 읽을 말씀' 으로 뜨고 있었다.**

- 받아온 공지의 `date` 를 **한국 날짜**(`Intl … timeZone: "Asia/Seoul"`)와 견주어
  오늘 것이 아니면 말씀 대신 **안내 화면**을 띄운다.
  - 딱지 "아직 오늘 말씀 전" (포인트색 대신 흰 반투명 — 오늘 말씀과 눈으로 구분된다)
  - 관리자: "지체들이 기다리고 있습니다. 오늘 말씀을 올려 주세요." + [오늘 말씀 올리러 가기]
  - 지체: "곧 올라옵니다. 먼저 성경통독을 이어 가셔도 좋습니다." + [둘러보기]
- 이 안내는 **'오늘 봤음' 으로 적지 않는다** — 조금 뒤에 말씀이 올라오면 다시 여실 때
  진짜 말씀을 보셔야 하기 때문이다. (그 판에서 이미 봤으면 그 열림 동안에만 안 뜬다)
- 공지가 아예 없어 `null` 이 와도 같은 안내가 뜬다. 서버가 안 되는 때(fetch 실패)만
  조용히 넘어간다 — 평소 화면이 먼저다.

확인: 지난 공지만 있는 상황을 만들어 관리자·지체 두 경우 다 안내 화면이 뜨는 것을 봄.

**덧 — 말씀이 없는 날 문구·행선지 (2026-09-10)**

- 제목: "오늘의 말씀이 설정되지 않았습니다" (관리자·지체 같은 문구)
- 지체: "관리자님의 설정을 기다리고 있습니다." + **[성경통독으로 바로가기]** → `bible` 탭
- 관리자: 부르는 말을 이름 대신 **"관리자님,"** 으로. "지체들이 기다리고 있습니다.
  오늘 말씀을 올려 주세요." + [오늘 말씀 올리러 가기] → `notice` 탭
- `onEnter` 가 **어느 탭으로 갈지**(`"notice" | "bible"`) 를 함께 넘긴다.
  말씀이 올라온 날은 늘 오늘말씀이다.

확인: 두 역할로 각각 띄워 문구와 행선지(지체→성경통독, 관리자→오늘말씀)를 봄.

## 초대 링크로 들어오면 설치 창이 안 뜨던 것 (2026-09-10)

세 가지가 겹쳐 있었다.

**① 설치 신호를 놓치고 있었다 (진짜 원인)**

크롬은 `beforeinstallprompt` 를 페이지가 뜨자마자 보내고, 그 신호는 **딱 한 번만** 온다.
그런데 그것을 받는 자리가 React 컴포넌트(`PWAInstallPrompt`) 의 `useEffect` 였다 —
화면이 붙기 전에 신호가 오면 **아무도 못 받고 사라져** 그 방문 내내 설치 창이 안 떴다.
초대 링크로 들어오면 `main.tsx` 가 공동체를 알아보느라(`applyInviteFromUrl`) 화면을
더 늦게 붙이므로 놓칠 확률이 훨씬 높았다.

→ `index.html` 의 `<head>` (가장 먼저 도는 자리)에서 붙잡아 `window.__installPromptEvent`
에 담아 두고 `cho:installready` 로 알린다. 컴포넌트는 붙을 때 그것을 집어 온다.

**② 첫 방문에 화면이 통째로 새로 고쳐졌다**

`sw.js` 가 `clients.claim()` 을 하므로 **처음 들어온 사람도** `controllerchange` 를 맞는다.
갈아탈 옛 버전이 없는데도 `location.reload()` 가 돌아, 초대 링크로 막 들어온 화면이
1~2초 만에 새로 고쳐졌다. → 스크립트가 뜰 때 `navigator.serviceWorker.controller` 가
있었는지(`__hadController`) 를 보고, 없었으면 새로고침하지 않는다.

**③ 설치 단추가 보이지 않았다**

배너 카드가 `bg-[#0C3B2E]` 인데 그 안 '앱 설치하기' 단추도 `bg-[#0C3B2E]` 였다 —
같은 색이라 글씨만 떠 있는 꼴이었다. → 금색(`#FFBA00`) + 진한 글씨로 도드라지게.

**바뀌지 않는 것** — 카카오톡·라인 같은 앱 안 브라우저에서는 신호 자체가 오지 않는다
(설치는 브라우저만 할 수 있다). 그때는 지금처럼 `InAppBrowserNotice` 가 안내한다.
아이폰은 어느 브라우저든 신호가 없어 '공유 → 홈 화면에 추가' 안내만 뜬다.

**덧 — 설치 안내는 신호가 없어도 뜬다 (2026-09-10)**

라이브를 크롬 엔진으로 검사해 보니 **앱은 설치 조건을 다 갖췄다**
(매니페스트 오류 없음 · 서비스워커 활성·제어 중 · 설치 불가 사유는 검사 브라우저가
시크릿이라 뜬 `in-incognito` 하나뿐). 그런데도 안 뜨는 까닭은 크롬 신호가
**안 올 때가 많기 때문**이다 — 첫 방문(서비스워커가 막 깔리는 중), 아이폰(신호 자체가 없음),
이미 설치됨, 7일 숨김.

그래서 신호만 기다리지 않는다.

- `beforeinstallprompt` 가 오면 → 지금처럼 진짜 설치 창을 여는 금색 단추
- **3.5초 기다려도 안 오면** → 기기에 맞는 **손으로 설치하는 법**을 알려 드린다
  (안드로이드 ⋮ → '앱 설치' / 아이폰 사파리 공유 → '홈 화면에 추가' /
   아이폰 다른 브라우저는 사파리로 열라고 / 데스크톱은 주소창 설치 아이콘)
- 카카오톡 등 앱 안 브라우저에서는 **조용히 있는다** (`inAppBrowser()`) —
  거기서는 설치가 안 되고 `InAppBrowserNotice` 가 따로 안내한다
- 설정 > 안내에 **"앱 설치하는 법"** 을 넣었다. 7일 숨김을 해 두었어도 부르면 뜬다
  (`replay` 숫자를 올려 알린다)

확인: 방금 닫아 둔 상태 → 자동으로는 안 뜨고, 설정에서 부르면 뜨는 것을 봄.

**덧 — 초대 링크는 카톡을 건너뛰고 브라우저로 (2026-09-10)**

초대 링크에 `openExternalBrowser=1` 을 붙였다. 카카오톡·라인은 이 쪽지가 붙은 링크를
**자기 안에서 열지 않고 기기의 기본 브라우저로 넘겨 준다.** 설치는 브라우저만 할 수 있으니,
'카톡 안내 → [브라우저로 열기]' 한 걸음이 통째로 사라진다.
(`applyInviteFromUrl` 이 주소를 치울 때 이 쪽지도 함께 지운다)

**설치까지 자동은 못 한다** — `beforeinstallprompt.prompt()` 는 **사람이 누른 직후에만**
열 수 있다 (브라우저 규칙). 아이폰은 그런 창 자체가 없다. 그래서 가장 짧은 길은
안드로이드에서 [앱 설치하기] → 크롬 확인창 [설치], 두 번이다.

## 글 속 링크와 유튜브 (2026-09-10)

찬양 영상 링크를 묵상에 붙여 넣으셔도 그동안은 그냥 **글자**였다 —
길게 눌러 복사한 뒤 브라우저에 붙여 넣어야 열렸다.

- `src/lib/links.ts` — `splitLinks()` (글을 '글자'와 '주소' 조각으로 나눈다.
  주소 끝에 딸려온 마침표·괄호는 떼어 글자로 돌려보낸다), `youtubeId()`
  (youtu.be · watch?v= · shorts · embed · live 를 알아본다. 아이디 모양이
  아니면 버린다), `youtubeIdsIn()`
- `src/components/YouTubeCard.tsx` — 처음에는 **그림 한 장**만. 누른 그 영상만
  재생기(iframe)로 바뀐다. 글이 여럿인 화면에서 재생기를 전부 띄우면 굼떠진다.
  주소는 `youtube-nocookie.com`.
- `MentionText` — @이름에 더해 **주소를 눌러 열리는 링크**로 그린다.
  영상 상자는 문단(p) 안에 못 들어가므로 바깥을 `div` 로 바꿨다.
  `embedVideo` 를 주면 글 아래에 영상을 붙인다 (댓글에는 안 붙인다).
- 묵상 본문은 길면 접히는 상자(`CollapsibleText`) 안이라 **영상만 상자 밖**에 붙였다.
  안에 두면 접힌 채 잘린다. 감사·칭찬 글은 접히지 않아 `embedVideo` 로 붙인다.
- 한 글에 영상은 **앞의 둘까지만**.

**아직 안 한 곳** — '나의 기록'(`MyMeditations`)은 글을 그냥 글자로 그린다.
링크가 필요하면 거기도 `MentionText` 로 바꿔야 한다.

**덧 — 영성일기 단추를 연하게 (2026-09-10)**

'묵상 나누기'(진초록)와 '성도의 영성일기'(청록)가 나란히 진해서 무엇이 먼저인지
보이지 않았다. 여기서 먼저 하실 일은 묵상 나누기이므로, 일기는 연한 바탕
(`#F1F4EE`) 에 초록 글씨로 한 톤 낮췄다. (한 화면에 채운 단추는 하나 — 시안 6번)

## 지체마다 개인 묵상방 (2026-09-11)

'전체'는 원래 있었지만, 지체 한 분 한 분의 방은 관리자가 손으로 만들어야 했다.
이제 **지체마다 자기 이름의 방을 서버가 갖춰 둔다.**

**서버** (`server.ts`)
- `ensurePersonalSoks(db)` — `GET /api/soks` 를 부를 때마다 훑어서
  없는 분의 방을 만들고(`id: "sok-me-<userId>"`, `name: "<이름>의 묵상방"`,
  `memberUserIds: [본인]`, `ownerId: 본인`), **이름이 바뀌었으면 방 이름도 고친다.**
  주인이 자기 방에서 빠져 있으면 되돌린다. 새로 가입하셔도 첫 화면에서 바로 생긴다.
- 개인 방은 **지울 수 없다** (`DELETE /api/soks/:id` 가 거절). 지워도 곧 다시 생겨 헷갈린다.
- `SokGroup.ownerId` 추가 (`src/types.ts`).

**방 글이 새어 나가지 않게** (`GET /api/meditations`)
- 예전에는 목록을 통째로 내려보내고 **화면에서만** 가렸다 — 글은 이미 기기에 도착해 있었다.
  이제 "나와 내가 초대한 지체만 봅니다" 라고 적어 드리므로 서버가 그 약속을 지킨다.
- 증표(`whoIs`)로 내가 속한 방을 추려 그 방 글과 전체 공유 글만 내어 준다.
  증표가 없으면 **전체 공유 글만** — 옛 화면을 쓰셔도 앱이 비지 않고, 남의 방 글도 안 샌다.
- 그래서 목록을 받는 세 곳(`MeditationFeed` · `MyMeditations` · `GoalSummaryPopup`)을
  `authFetch` 로 바꿨다. **/api/meditations 를 새로 부르는 곳이 생기면 반드시 authFetch 로.**

**화면** (`MeditationFeed.tsx`)
- 알약: 전체 → **내 묵상방**(맨 앞) → 다른 방들
- 글 올릴 곳 고르기: "이 글을 어디에 올릴까요?" — 전체 공유 / 내 묵상방 / 그 밖의 방
- 개인 방은 **관리자라도** 초대받지 않으면 보이지 않는다 (속은 지금까지처럼 관리자가 다 본다)
- 관리자 '속 관리' 에는 **관리자가 만든 속만** 보인다 (개인 방 18개가 섞이면 정작 관리할
  속이 묻힌다). 개수만 한 줄로 알려 드린다.

**덧 — 어느 방인지, 누가 보는지 적어 드린다 (2026-09-11)**

방을 골라도 화면 문구는 그대로여서, 내 방에 쓴 글을 남이 볼까 걱정되실 수 있었다.

- 알약 바로 아래 **한 줄**(`whoCanSee`) — 늘 보인다
  - 내 방: 🔒 "나만 볼 수 있는 방입니다 — 초대하시면 그 지체도 함께 보십니다"
    (초대하신 분이 있으면 "(초대하신 n분도 함께 보십니다)")
  - 다른 속: "OO 식구들만 볼 수 있는 방입니다"
  - 전체: "우리 공동체 모두가 함께 보는 글입니다"
- 빈 화면 문구도 방에 맞춰 — 내 방이면 "내 묵상방에 아직 글이 없습니다 /
  나만 볼 수 있는 방입니다. 남에게 보이기 어려운 마음도 이곳에 편히 적어 보세요."

## 영성일기는 켜신 분께만 (2026-09-11)

라이브를 세어 보니 영성일기는 **한 분(7편)만** 쓰고 계셨다. 그런데도 단추가 늘 있어
새로 생긴 '내 묵상방' 과 역할이 겹쳐 보였다. **기본을 '없음' 으로** 바꾼다.

- `src/lib/journalPref.ts` — `journalOn()` / `setJournalOn()`,
  `localStorage["journalEnabled"]`, **기본 꺼짐**. 기기마다 따로 정한다.
- 설정 > **묵상 > '영성일기 쓰기'** 스위치. 켜면 묵상일기 화면에 단추가 돌아온다.
- `MeditationFeed` 에 `journalEnabled` 를 넘긴다. 꺼져 있으면 단추가 아예 없고
  '묵상 나누기' 가 한 줄을 다 쓴다.
- **써 두신 일기는 그대로 남는다** — 보이느냐 마느냐일 뿐이다. 다시 켜면 다 보인다.

확인: 기본값에서 단추 없음 → 설정에서 켜면 다시 보임.

**덧 — '전체' → '전체공유방' (2026-09-11)**

알약과 글 올릴 곳 고르기에서 이름을 맞췄다. 옆의 '내 묵상방' 과 나란히 놓이니
'전체' 한 마디로는 그것도 방이라는 것이 안 보였다.
