# 📖 목장 나눔(CHO) — 작업 기록 & 인수인계

> 이 문서 하나로 창을 껐다 켜거나 다른 세션에서도 이어서 작업할 수 있습니다.
> **최종 업데이트: 2026-09-15**
> 이전 문서: `작업기록_HANDOFF_260811.md`
> (함정 5가지·구조 설명은 `작업기록_HANDOFF_260807.md` 를 먼저 볼 것)

---

## 0. ⚡ 이번 세션 3줄 요약

1. **앱 이름과 아이콘을 통째로 갈았다** — `말씀나눔` → **목장 나눔**, 아이콘은 익투스 물고기
2. **리딩지저스에 '여섯 걸음' 안내를 붙였다** — 가이드 메뉴를 만들지 않고, **읽는 자리에 따라붙게** 했다
3. **처음 들어오면** 앱 사용법 다음에 **글씨 크기·알림을 한 번 맞춘다**

⚠️ 그리고 **앱 사용법이 이제 모든 지체에게 열렸다** (6번 항목 — 꼭 읽을 것)

---

## 1. 앱 이름 — 말씀나눔 → 목장 나눔

| 어디 | 무엇 |
|---|---|
| `public/manifest.json` | `name` · `short_name` (홈 화면 라벨) |
| `index.html` | `<title>`, `apple-mobile-web-app-title` |
| `public/sw.js` | 푸시 알림 기본 제목 |
| `src/App.tsx` | 공동체 이름이 아직 없을 때의 기본값 |
| `LoginScreen.tsx` · `PWAInstallPrompt.tsx` | 화면 문구 · 이미지 alt |
| `CommunitySettings.tsx` | 카카오톡 초대 문구 |

---

## 2. 아이콘 — 익투스 물고기

짙은 남색·초록 그라데이션 바탕에 무지개 익투스.

### 만드는 법

```bash
npm install --no-save sharp potrace
node tools/make-icons.mjs
```

- 원본: `tools/assets/app-logo-src.png` (둥근 사각형 1024, 바깥은 투명)
- 만들어지는 것: `icon-192` / `icon-512` / `icon-maskable-512` / `apple-touch-icon` /
  `badge-96` / `app_icon.jpg`
- `sharp` 는 이 도구에서만 쓰므로 **`package.json` 에 넣지 않았다** (배포 빌드가 느려진다)

### 왜 모서리까지 꽉 찬 정사각형인가

원본은 모서리가 둥글지만 **OS 가 알아서 모서리를 깎는다.** 둥근 그림을 그대로 넣으면
둥근 아이콘 안에 또 둥근 아이콘이 박힌 모양이 된다. 그래서 빈 모서리를 배경
그라데이션이 이어지도록 메운다 — 가장자리 색을 번지게 한 뒤(`spread`) 그 자리만
반복해서 문질러 편다(`smooth`).

### 함정 — 둥근 가장자리의 반투명 화소

거기엔 **흰색이 섞여 있다.** 그냥 두면

- 아이콘 모서리에 **허연 테두리**가 남고
- 배지에는 **둥근 사각형 윤곽이 유령처럼** 남는다

→ 아이콘은 경계를 4줄 깎아내고 다시 채우고, 배지는 알파 250 미만을 버린다.

### 알림 배지는 왜 흰 실루엣인가

**안드로이드는 배지의 투명도만 읽고 색은 버린다.** 컬러 아이콘을 넣으면 상태표시줄에
그냥 네모로 보인다. 그래서 물고기만 흰 실루엣으로 오려냈다.
(알림 목록에 크게 뜨는 아이콘 `icon-192` 는 컬러 그대로다)

### 적응형(maskable)

5% 안쪽으로 줄여 안전영역(가운데 80% 원) 안에 물고기를 넣었다.

### ⚠️ 이미 설치한 사람에게는 바로 안 바뀐다

| 어디 | 언제 바뀌나 |
|---|---|
| 알림 아이콘 | 앱 한 번 열면 **바로** |
| 안드로이드 홈 화면 | 크롬이 WebAPK 를 갱신할 때까지 길면 하루쯤. 급하면 지웠다 재설치 |
| iPhone 홈 화면 | **재설치해야** 바뀜 (iOS 가 설치 시점 아이콘을 박아 둔다) |

`sw.js` 캐시 버전을 **v7 → v8** 로 올렸다. 안 올리면 옛 아이콘이 캐시에서 계속 나온다.

---

## 3. 탭 아이콘과 머리말 로고

### 하단 '성경통독' 탭 — `src/components/BibleIcon.tsx`

기존 `BrandMark`(말씀 줄기를 두 손이 감싼 모양)는 선이 많아 **19px 에서 뭉갰다.**
실제 크기로 후보 일곱을 나란히 그려 보고 고른 것이 **표지에 십자가가 있는 덮인 성경책**.

짝이 맞는다 — **오늘말씀 = 펼친 책**(오늘 읽을 한 장) / **성경통독 = 성경 한 권**(전체).
펼친 책 두 개를 쓰면 두 탭이 헷갈린다.

작은 크기가 전부라 책갈피·책장 선은 일부러 넣지 않았다. 다른 탭이 전부 lucide 라서
같은 규격(24 칸, 선 1.8, 둥근 끝)을 따랐다. `AppGuide` 의 성경통독 장 아이콘도 같이 맞췄다.

### 머리말 로고 — `src/components/IchthysMark.tsx`

**손으로 다시 그리지 않았다.** 앱 아이콘 원본에서 물고기만 골라내 potrace 로 윤곽선을
딴 것이다. 홈 화면 아이콘과 머리말 로고가 **한 획도 어긋나지 않아야** 같은 앱으로 읽힌다.

다시 따야 하면 순서는 이렇다.

1. 원본에서 물고기만 남긴다 — 배경은 가장 밝은 채널이 **78 을 못 넘는다**
2. 둥근 테두리의 흰빛 섞인 화소가 끼지 않게 **가장자리 8% 는 아예 보지 않는다**
3. potrace 로 윤곽선을 딴다
4. **잉크가 실제로 닿는 칸**으로 viewBox 를 맞춘다
   (potrace 가 내주는 좌표는 조절점까지 포함해 실제보다 넓다)

물고기는 가로:세로 ≒ **2:1** 이라 `size` 가 **가로 길이**다. 정사각 칸에 억지로 끼우면
옆 아이콘보다 작아 보인다. 머리말은 `size={26}`.

`BrandMark` 는 **로그인 화면·공동체 선택 화면·AppGuide 의 '우리 공동체' 칩**에 그대로 남아 있다.

---

## 4. 리딩지저스 — 여섯 걸음 안내

### 핵심 생각

**읽으러 들어오게 만들지 말고, 읽는 자리에 따라붙게 한다.**
가이드 메뉴를 하나 만들면 아무도 안 들어간다. 대신 **오늘 읽는 곳이 6권 중 어디인지**를
늘 옆에 붙여 두면, 안 찾아도 저절로 알게 된다.

### 데이터를 새로 만들지 않았다

`src/data/readingJesusVolumes.ts`

통독표(`readingJesus.ts`)에는 **몇 권짜리 여정인지가 안 적혀 있다.** 그런데 여섯 걸음의
경계가 **마침 책 경계와 그대로 맞아떨어진다.** 그래서 표에 아무것도 덧붙이지 않고,
**오늘 읽는 책 이름만 보고** 몇 권째인지 찾는다 (`volumeOfBook`).
통독표가 바뀌어도 이 파일은 손댈 일이 없다.

| 권 | 범위 | 한 줄 주제 |
|---|---|---|
| 1권 언약의 서막 | 창세기~여호수아 | 내가 너희의 하나님이 되고 |
| 2권 실패와 갈망 | 사사기~에스더 | 우리에게도 왕을 주소서 |
| 3권 지혜와 노래 | 욥기~아가 | 고통 속에서도 신뢰할 수 있는가 |
| 4권 선지자의 외침 | 이사야~말라기 | 심판 뒤에 올 회복을 보라 |
| 5권 복음의 성취 | 마태복음~로마서 | 약속하신 그분이 바로 이분이다 |
| 6권 교회의 사명 | 고린도전서~요한계시록 | 다시 오실 왕을 기다리며 |

### 네 자리

**① 권 띠** — `BibleReader`, 진행률 바로 아래
`1권 · 언약의 서막` + 한 줄 주제. 매일 저절로 보이고, 권이 넘어가면 저절로 바뀐다.
오늘 분량의 첫 책 → 없으면 마지막 읽은 곳 → 그것도 없으면 창세기(첫 걸음).
쉬는 날에 띠가 사라지면 오히려 이상해서 이렇게 이어 붙였다.

**② 여정 지도** — `RjJourney.tsx`, 띠를 누르면 열린다
여섯 줄이 **한 화면에 들어와야** 하므로 **기본은 전부 접고 지금 읽는 권만 펼친다.**
여섯을 다 펼쳐 두면 아무도 안 읽는다.

**③ 두 질문** — `RjAskCard.tsx`, 본문 끝 묵상 쓰기 단추 다음
리딩지저스는 읽는 법이 이 두 줄이 전부다.
**처음 한 번만 펼쳐 보이고 그 뒤로는 접힌 한 줄**(`rjAskSeen`).
매일 펼쳐 두면 성경이 그만큼 밀려 내려간다 — 예전 '오늘의 통독 실천 제안' 상자가 그랬다.

**④ 처음 고를 때 세 장** — `RjIntro.tsx`
리딩지저스를 **처음 고른 그때 한 번만**(`rjIntroSeen`).
①성경 전체가 예수를 가리킨다 ②구약은 질문, 신약은 해답 ③6권의 여정.

### 지킨 것

- 글은 교회에서 받은 리딩지저스 설명을 줄인 것이다. **새로 지어내지 않았다.**
- 4가지 관점 전문·요셉·제사 예시는 **넣지 않았다.** 길어서 아무도 안 읽는다.
  그 핵심은 각 권의 '예수님' 한 줄에 이미 들어가 있다.
- **어성경에는 넣지 않았다** (`isReadingJesus` 로만 막는다).

---

## 5. 첫 실행 설정 — 글씨 크기·알림

`src/components/FirstRunSetup.tsx`

앱 사용법을 닫은 **바로 그 자리**에서 이어 뜬다.
글씨 크기와 알림은 설정에 들어가야 만날 수 있어서 아무도 안 건드리는데,
처음 한 번 맞춰 두면 그 뒤로 쓰는 내내 편해지는 것들이다.
어르신께는 글씨 크기가, 공동체에는 알림이 특히 그렇다.

- 딱 한 번만 (`firstRunSetupDone`). 둘 다 **기기별 설정**이라 표시도 기기에 남긴다.
- 아이폰이 아직 홈 화면에 설치 안 된 상태면, 알림 대신 **설치부터 하시라는 안내**가 뜬다.
- `FontSizeSegment` 를 `NotificationSettings` 에서 **파일로 빼냈다**
  (`components/FontSizeSegment.tsx`). 설정 화면과 첫 실행이 **같은 것**을 쓴다 —
  두 군데에 따로 그려 두면 한쪽만 고쳐져 어긋난다.

---

## 6. ⚠️ 앱 사용법이 모든 지체에게 열렸다

`GUIDE_FOR_ADMIN_ONLY` 를 **`true` → `false`** 로 바꿨다 (관리자 시험 기간 종료).

5번 기능("가입하면 앱 설명 다음에 설정")을 하려면 이 빗장을 풀어야 했다.

**그래서 기존 지체들도 다음에 앱을 열면 `사용법 6장 → 글씨·알림 설정` 이 한 번 뜬다.**
한 번 보고 나면 다시 안 뜬다.

> **답을 기다리는 것** — 기존 지체에게는 안 뜨게 하고 **신규 가입자만** 보게 할지.
> 하려면 가입 시점(`createdAt`)을 기준으로 가르면 된다.

---

## 7. 이번 세션 배포 기록

| 리비전 | 빌드 | 무엇 |
|---|---|---|
| `cho-00181-nxh` | `2ebb74dc` | 앱 이름 "목장 나눔" + 익투스 아이콘 세트 |
| `cho-00182-nhs` | `b5400fb1` | 하단 성경통독 탭 아이콘 → 십자가 성경책 |
| `cho-00183-fvt` | `82dfa792` | 머리말 로고 → 익투스 |
| `cho-00184-d9x` | `cf1c29a5` | 리딩지저스 여섯 걸음 안내 + 첫 실행 설정 |

배포는 `main` 에 푸시하면 Cloud Build → Cloud Run `cho`
(asia-northeast3, 프로젝트 `dulcet-medley-471901-m4`) 로 자동으로 나간다.

---

## 8. 새로 생긴 파일

```
tools/make-icons.mjs                    아이콘 세트 생성기
tools/assets/app-logo-src.png           로고 원본 (여기만 갈아 끼우면 된다)

src/components/BibleIcon.tsx            성경통독 탭 아이콘
src/components/IchthysMark.tsx          머리말 익투스 (원본에서 딴 벡터)
src/components/FontSizeSegment.tsx      글씨 크기 세그먼트 (설정·첫실행 공용)
src/components/FirstRunSetup.tsx        첫 실행 — 글씨 크기·알림
src/components/RjJourney.tsx            리딩지저스 여정 지도
src/components/RjAskCard.tsx            리딩지저스 두 질문
src/components/RjIntro.tsx              리딩지저스 처음 안내 세 장
src/data/readingJesusVolumes.ts         여섯 걸음 데이터 + volumeOfBook()
```

---

## 9. 아직 남은 것

- **앱 사용법을 기존 지체에게도 보일지** (6번) — 답 기다리는 중
- **어성경(WTBT) 통독표의 빈 곳** — 52주 표에 **51장이 한 번도 안 읽힌다**
  (시편 101~150편, 오바댜 1장).
  제안해 둔 것: 시편 101~150편을 16주(역대상+잠언)와 17주에 나눠 넣고,
  오바댜는 19주(요나·요엘 소선지서)에 넣기. **아직 답을 못 받았다.**

---

## 10. 일하는 규칙 (그대로 유지)

- **묻지 말고 바로 배포** — 작업이 끝나면 커밋·푸시까지 한 번에
- `gcloud` 는 **Bash 도구에서 안 된다.** PowerShell 도구를 써야 한다
- 파일이 CRLF/LF 섞여 있다. bash heredoc 은 `\s`·`${}` 를 망가뜨리니
  **node 스크립트로 고친다**
- 좌우 밀기 배선(`data-no-tab-swipe`, `swipeHandlers`, `dragRef`)은 **건드리지 않는다.**
  위에 가드만 얹는다
- `.gitignore` 는 `db.json`, `db.json.tmp`, `db.c-*.json` 을 계속 막아야 한다
  (실제 지체 이름과 PIN 해시가 들어 있다)
- 개발 서버는 **운영 Firestore 에 붙는다.** 확인은 백엔드 없는 vite 서버(5199)나
  API 를 가로챈 Playwright 로만 한다
- **영성일기 내용은 절대 들여다보지 않는다.** 쓴 사람 수만 센다

---

## 11. 본문·댓글 손보기 + 형광펜 (2026-09-15)

### 댓글이 "패딩이 무너져" 보이던 진짜 이유

`MeditationFeed` 의 댓글 목록 상자는 **흰색**(`bg-white`)인데, 그 안의 댓글 말풍선도
**흰색**(`bg-white/80`)이었다. 흰 바탕에 흰 말풍선 → 테두리도 여백도 없는 것처럼 보였다.
**패딩이 무너진 것이 아니라 말풍선이 안 보였던 것이다.**
거기에 `rounded-3xl`(24px)까지 얹혀 한 줄짜리 댓글은 글씨가 둥근 모서리에 닿았다.

- 말풍선 `bg-[#F4F6F3]` / `rounded-2xl` / `px-3.5 py-2.5`
- 글 칸에 `flex-1 min-w-0` — 이게 없으면 긴 글이 상자를 밀고 나가 여백이 무너져 보인다
  (`MyMeditations` 2곳, `DailyGratitude` 1곳도 같은 이유로 함께 고쳤다)
- 댓글 입력칸에 `bg-[#F5F5F5]` 와 `bg-white` 가 **둘 다** 붙어 있던 것도 정리

### 본문 상자를 화면 높이에 맞춰 늘렸다

`src/lib/useFillHeight.ts`

오늘 말씀과 성경통독 모두, 본문 상자 높이를 **숫자로 박아** 두었다
(`max-h-[60vh]` / `max-h-[calc(100vh-14rem)]`). 기기마다 남는 자리가 달라서
어떤 폰에서는 아래가 휑하게 비고 어떤 폰에서는 몇 줄 못 보고 굴려야 했다.

- 이제 **재서 정한다** — 카드 머리(`readerRef`)부터 상자까지 쌓인 높이를 실제로 재고,
  화면 높이에서 그만큼과 하단 탭 바(104px, PC 는 32px)를 뺀 값을 상자 높이로 준다
- 화면을 굴려도 이 거리는 변하지 않으므로 **값이 춤추지 않는다.**
  글씨 크기를 바꾸거나 번역본을 하나 더 얹으면 위가 두꺼워지므로 그때만 다시 잰다
  (`ResizeObserver`)
- 결과: 본문을 화면 맨 위로 맞추면 **제목 바로 아래부터 탭 바 직전까지** 꽉 찬다
- ⚠️ **상자를 없애지는 않았다.** 한 번 없애 봤더니(본문이 제 길이대로 흐르고 화면이
  스크롤) 장 넘김·절로 데려가기·`alignReader` 가 전부 어긋났다. 상자는 그대로 두고
  높이만 키우는 것이 맞다

### 칠한 자리가 잘리던 것

절 줄은 `-mx-1.5 px-1.5` 로 **상자 밖 6px 까지** 배경을 그린다. 그런데 상자에
`overflow-x-hidden` 이 걸려 있어 그 6px 이 잘려 나갔다.
→ 좌우 여백 `px-1.5` 를 **상자 쪽에** 줬다. 절 줄은 그대로 두었으므로
칠한 자리가 상자 테두리에 딱 맞아떨어진다.

### 형광펜 — 색 두 가지, 장을 옮겨도 남는다

`src/lib/verseHighlight.ts`

예전 색은 `#FFFBEE` — 거의 흰 크림색이라 **골랐는지 안 골랐는지 눈에 띄지 않았다.**

- **노랑 `#FFE49C` / 초록 `#BFE6C8`** 두 가지. 더 늘리면 고르는 일이 일이 된다
- 색은 사람이 고른 것이라 **인라인 style 로 칠한다** — 꾸미기(테마)가 바뀌어도
  내가 칠한 곳은 그대로여야 한다
- 색 고르기는 아래 막대(`PickedVerseBar`)에 넣었다. 누르면 **이 장에 칠해 둔 것도
  그 색으로 따라 바뀌고**, 다음에 체크하는 구절도 그 색이 된다
- 마지막에 고른 색은 기기에 기억한다(`verseHighlightColor`)

**장을 옮겨도 남는다.** 예전에는 장이 바뀌면 `setPickedVerses(new Map())` 로 지웠다 —
형광펜으로 칠해 놓고 다음 장에 갔다 오면 칠한 자리가 사라지는 셈이었다.
이제 `BibleReader` 는 내가 체크한 구절을 **한 번 받아 두고**(`savedMarks`, 책\|장\|절),
그 장에 돌아오면 칠한 색이 그대로 보인다. 장이 바뀔 때는 **아래 막대만** 닫는다.

- 아래 막대는 **이 장에서 실제로 뭔가를 눌렀을 때만** 올라온다(`barOpen`).
  칠해 둔 장에 들어설 때마다 막대가 따라 올라오면 성가시다
- 막대의 ✕(지우기)는 이제 **칠한 것도 함께 지운다** (안 그러면 다시 들어올 때 되살아난다)
- `SavedVerse.color` 를 더했다. 색이 없던 시절에 체크해 둔 것은 **노랑**으로 본다
- 색만 바꾸는 길을 새로 냈다 — `POST /api/saved-verses/color`.
  토글로는 못 한다(같은 절을 다시 부르면 체크가 풀린다)

> 오늘 말씀은 공지 제목("요한1서 5장")에서 책·장을 뽑아 쓴다.
> 리딩지저스처럼 **여러 장을 한 번에 올린 날**("마태복음 1~3장")은 책·장을 특정할 수
> 없어 체크리스트에 남기지 않는다 — 예전부터 그랬고 그대로 뒀다.

### 도구

`scratchpad/patch.cjs` — **줄바꿈이 섞인 파일**(CRLF/LF)에서도 찾아 바꾸는 도구.
`BibleReader.tsx` 는 한 파일 안에 CRLF 줄과 LF 줄이 섞여 있어서, 앵커의 줄바꿈을
한 가지로 못 박으면 못 찾는다. 앵커는 `\r?\n` 으로 맞추고, 바꿔 넣는 글은
**찾은 자리가 쓰던 줄바꿈**을 따른다.
(그리고 이 환경의 bash heredoc 은 백슬래시를 먹는다 — 패치 스크립트는 Write 로 쓸 것)

---

## 12. 형광펜 다듬기 (2026-09-16)

- **파스텔로 눕혔다** — 노랑 `#FFE49C` → `#FFF0C2`, 초록 `#BFE6C8` → `#D9EFE0`.
  성경 본문 위에 칠하는 것이라 색이 진하면 **글자보다 색이 먼저** 눈에 든다.
  흰 바탕과는 분명히 다르되 읽는 것을 방해하지 않을 만큼만.
- **아래 막대가 5초 뒤 저절로 사라진다** (`showBarAwhile`).
  말씀을 체크한다고 다 묵상을 쓰는 것은 아니다. 색만 칠하고 계속 읽는 쪽이 훨씬
  많은데 막대가 계속 떠 있으면 읽는 화면을 내내 가렸다.
  절을 누르거나 색을 고르면 시간이 다시 5초부터 간다.
- ⚠️ 그래서 **'묵상 쓰기' 단추를 늘 보이게** 바꿨다. 예전에는 체크한 구절이 있으면
  (`pickedVerses.size === 0` 조건) 숨었는데, 이제 칠한 것이 남아 있는 데다 막대까지
  사라지므로 **묵상을 쓸 길이 아예 없어진다.** 체크한 것이 있으면 단추 글씨가
  "체크한 n구절로 묵상 쓰기" 로 바뀐다.
- 체크한 구절을 다시 누르면 색이 지워진다 (원래 토글이라 그대로 동작한다).

### 성경통독 본문 아래의 '두 질문' 상자를 뺐다

`RjAskCard.tsx` 삭제. 읽는 화면에서 **말씀 아래에 설명이 붙으면 거슬린다**는 말씀.
'오늘의 통독 실천 제안'(2026-08-31)에 이어 두 번째로 뺀 것이다 —
본문 아래에는 아무것도 두지 않는 것이 맞다.

리딩지저스를 어떻게 읽는지는 **처음 고를 때 세 장**(`RjIntro`)과
**권 띠를 누르면 열리는 여정 지도**(`RjJourney`)가 이미 말해 준다.

---

## 13. 읽는 화면 다듬기 (2026-09-19)

- **형광펜 한 단계 더 눕혔다** — 노랑 `#FFF0C2` → `#F3E8CC`, 초록 `#D9EFE0` → `#D6E5DA`
- **아래 막대는 3초 뒤 사라진다** (5초 → 3초)

### 구절을 고를 때마다 화면이 위로 튀던 것

화면 아무 데나 누르면 본문을 화면 머리에 맞춰 주는 규칙(`alignReader`)이 있는데,
**구절을 고르는 것까지 거기 걸렸다.** 읽던 자리에서 구절 하나 칠하려고 눌렀을 뿐인데
화면이 위로 올라가 버렸다.
→ `alignReader` 에서 `[data-verse]` 안을 누른 것은 그냥 지나간다.
(오늘 말씀·성경통독 두 곳 모두)

### 성경·장과 번역본 고르기를 본문 위에 붙였다

- 성경통독은 제목을 키웠고(`text-base` → `text-lg sm:text-xl`),
  오늘 말씀은 성경·장이 **날짜 옆 작은 회색 글씨에 묻혀 있던 것**을 본문 머리로 올려 키웠다
  (날짜만 부제로 남겼다)
- 둘 다 `sticky top-0` 으로 **본문을 쭉 내려도 따라다닌다**

> ⚠️ **함정 — 이 앱에서 페이지 쪽 sticky 는 붙지 않는다.**
> `index.css` 의 `body { overflow-x: hidden }` 때문에 페이지를 기준으로 한 sticky 는
> 붙을 자리를 잃고 그냥 밀려 올라간다. (처음에 그렇게 붙였다가 안 붙는 것을 확인했다)
> **본문 상자 안쪽**에 넣어야 한다 — 그 상자가 스스로 굴러가는(`overflow-y-auto`) 상자라
> sticky 가 제대로 붙는다. 사람이 "쭉 내리는" 것도 페이지가 아니라 본문이니 이쪽이 맞다.
> 고정줄의 흰 바탕은 `-mx-1.5 px-1.5` 로 상자 끝까지 넓힌다 —
> 칠한 구절이 그 아래로 지나갈 때 좌우 끝에 색이 삐져나오지 않게.
