# 📖 성경묵상 공유앱(CHO) — 작업 기록 & 인수인계

> 이 문서 하나로 창을 껐다 켜거나 다른 세션에서도 이어서 작업할 수 있습니다.
> **최종 업데이트: 2026-08-07 (커밋 `8540a3a` 기준)**
> 이전 문서: `작업기록_HANDOFF.md`, `작업기록_HANDOFF_060729.md` (배경 설명은 그쪽 참고)

---

## 0. ⚡ 지금 상황 3줄 요약

1. **비용·속도 구조 개선 완료** — Firestore 문서 118KB → 54KB, 폴링 트래픽 287배 감소
2. **약 7개월 뒤 앱이 멈출 문제를 제거** (공지가 성경 본문을 DB에 매일 복사하던 구조)
3. 라이브 정상 동작 중 → https://cho-1006806795339.asia-northeast3.run.app

---

## 1. 핵심 주소·ID

| 항목 | 값 |
|---|---|
| **로컬 폴더 (이번 세션)** | `C:\Users\beeeb\Downloads\CHO` |
| GitHub | https://github.com/beeeber12-cloud/CHO |
| **라이브 (Cloud Run)** | https://cho-1006806795339.asia-northeast3.run.app |
| Firebase 프로젝트 | `elemental-diode-2w1xt` |
| Firestore 문서 | `app_data/main_db` (**DB 전체가 문서 1개**) |
| GCP 프로젝트 | `dulcet-medley-471901-m4` |
| 배포 방식 | `main` 브랜치 push → Cloud Run 자동 배포 (**약 2~3분**) |

⚠️ 옛 링크 `ais-pre-...run.app` 은 은퇴. 반드시 `cho-...run.app` 에서 확인할 것.

---

## 2. 🚨 이 프로젝트에서 반드시 알아야 할 함정 5가지

### ① 로컬 서버를 띄운 채 데이터를 고치면 되돌아온다
운영 중인 Cloud Run 서버가 **옛 데이터를 메모리에 들고 있다가 다음 저장 때 통째로 다시 써 넣는다.**
이번에 공지 정리를 두 번 했는데 두 번 다 되돌아왔다.
→ **데이터 정리는 반드시 "코드 배포 완료 후"에.** 더 나은 방법은 저장 경로 자체에서 막는 것(§3-①처럼).

### ② 로컬 서버가 안 죽는다 (포트 3000)
`TaskStop` 으로 백그라운드 작업을 멈춰도 node 프로세스가 살아 있는 경우가 있다.
살아 있는 옛 프로세스가 옛 코드로 Firestore 에 계속 써서 작업을 망친다.
```powershell
$p = (netstat -ano | Select-String ":3000\s+.*LISTENING" | ForEach-Object { ($_ -split '\s+')[-1] } | Select-Object -Unique)
$p | ForEach-Object { Stop-Process -Id $_ -Force -Confirm:$false }
```

### ③ Git Bash 에서 curl 로 한글을 보내면 깨진다
`curl -d '{"lastReadBook":"로마서"}'` → DB에 `�θ���` 로 저장됨 (실제로 사고 냄).
→ 한글이 들어가는 API 호출은 **Node 스크립트**로. `Buffer.from(JSON.stringify(...),'utf8')` 사용.

### ④ 동기화 병합에서 새 필드를 빠뜨리면 조용히 지워진다
`server.ts` 의 `syncAndRefreshWithFirestore` 가 `mergedDb` 를 필드별로 다시 만든다.
여기 안 적은 필드는 **동기화마다 사라진다.** (푸시 구독에서 겪음)
새 필드를 추가하면 반드시 이 병합 객체에도 추가할 것.

### ⑤ 정렬은 '박스'가 아니라 '글자' 좌표로 재야 한다
절 번호가 오른쪽 정렬이라 **칸의 왼쪽 끝과 숫자가 그려지는 위치가 약 10px 다르다.**
`getBoundingClientRect()` 로 요소를 재면 "맞다"고 나오는데 눈에는 어긋나 보인다.
→ `document.createRange().selectNodeContents(텍스트노드)` 로 글자 좌표를 잴 것.

---

## 3. 이번 세션(08-01 ~ 08-07) 작업 내용

### ① 🔥 공지에서 성경 본문 분리 — 가장 중요
**문제:** 매일 올라가는 말씀 공지가 성경 본문 전체(3~4KB)를 Firestore 문서에 복사 저장.
하루 4.3KB씩 증가 → **약 212일(7개월) 뒤 1MiB 한도 도달 → 저장 전면 실패** 예정이었다.

**해결 (`7470d36`, `c83ced7`)**
- 공지는 **구절명만 저장**, 본문은 내보낼 때 `withVerseText()` 가 로컬 개역개정에서 채운다
- **`saveDb()` 와 Firestore 병합 시점에 `stripStorableVerseText()`** 로 저장 직전 걸러낸다
  (일회성 정리로는 운영 서버와의 경쟁에서 못 이겨서, 저장 경로 자체를 막는 방식으로 바꿈)
- 서버 부팅 시 빈 본문을 다시 채워 저장하던 블록 제거 (이게 있으면 재시작마다 문서가 다시 부푼다)
- **AI에게 성경 본문을 받지 않는다** — Gemini가 본문까지 생성하게 되어 있어 틀린 구절이 섞일 수 있었다. 이제 본문은 항상 로컬 원문, AI는 묵상 가이드만.

**결과:** 문서 118KB → **54KB**, 증가량 4.3KB/일 → **0.46KB/일** (한도까지 약 5.8년)

### ② 폴링 경량화 (`a73456f`)
4개 화면이 각각 4초 타이머로 목록 전체를 받아오던 것을 바꿈.
- 서버: `GET /api/revision` (26바이트) — `saveDb()` 마다 번호가 올라감
- 클라이언트: `src/lib/revision.ts` 의 **공용 구독기 1개**가 번호만 확인, 바뀔 때만 목록 요청
- 화면이 가려져 있으면(`document.hidden`) 확인도 건너뜀

**결과:** 1시간 열어둘 때 **6.4MB → 23KB** (287배)

### ③ 성경 본문 오프라인 캐시 (`7470d36`)
`/api/bible/search` 응답을 전용 캐시 `bible-text-v1` 에 영구 보관. 성경은 변하지 않으므로 앱 버전이 올라가도 안 지움. 한 번 연 장은 통신 없이 즉시 열리고 비행기모드에서도 읽힌다.

### ④ 내일 공지 사전 생성 (`7470d36`)
오늘 공지를 응답한 **뒤** 백그라운드로 다음 날 공지를 만들어 둔다 (`prepareTomorrowNotice`).
새벽 6~8시 첫 접속자가 생성(AI 호출 포함)을 기다리던 문제 해소.
미리 만든 공지가 오늘 노출되지 않도록 fallback 을 `date <= today` 로 제한.

### ⑤ 성경 통독이 권을 넘어 이어지게 (`f9f7b4b`)
요한1서(5장까지)인데 6·7·8·9·10장을 계속 만들어 **매번 5장 본문이 반복 노출**되고 있었다.
- `getNextChapterInOrder()` — 마지막 장이면 다음 권 1장으로
- `normalizePlanPosition()` — 없는 장을 가리키면 다음 권 1장으로 교정
- 성경 읽기방 버튼도 `다음 권 (요한2서 1장)` 으로 표기
- 잘못 생성된 요한1서 8·9·10장 공지는 사용자 승인 후 삭제함

### ⑥ 화면 정렬·여백 (`8626537`, `1d20466`, `185df31`, `8540a3a`)
- 흰 카드 안에 같은 흰 카드가 있어 **좌우 여백이 두 번** 먹히던 것 제거 (전역 `.bg-white` 규칙이 그림자를 넣어 별개 박스처럼 보였음)
- 본문 폭 화면의 73% → **88%**
- 좌우 기준선 통일: **왼쪽 21.1px(절 숫자 글자 위치) / 오른쪽 401px**
  - 왼쪽 정렬: 날짜, '마음에 닿은 구절을 눌러보세요', 책·장 제목 → `pl-[10px] sm:pl-[16px]`
  - 오른쪽 정렬: 본문, 성경 읽기방에서 보기, 구절명, 이 말씀으로 묵상 쓰기
- ⚠️ **절 번호 자체는 건드리지 말 것** (오른쪽 정렬 `w-[18px] sm:w-6`). 한 번 왼쪽 정렬로 바꿨다가 되돌림.

### ⑦ 배포해도 옛 화면이 남던 문제 (`7af1a09`, `d8e1aeb`)
서비스워커가 HTML까지 캐시하고 '저장된 것 먼저' 방식으로 내줘서, 새로 배포해도 옛 화면이 계속 보였다. 여러 번 껐다 켜야 반영됨.
- **HTML은 항상 새로 받아온다**(network-first), 통신 실패 시에만 저장본
- 새 버전이 적용되면 **한 번만 자동 새로고침**(`controllerchange`)
- **알림설정 맨 아래에 빌드 시각 표시** → 어느 버전인지 눈으로 확인 가능
  (`vite.config.ts` 의 `__BUILD_TIME__`)

### ⑧ 기타
- 월 진행률에 지난달 결과 한 줄 추가 (매월 1일 0으로 보여 "고장났다"는 오해 방지) `01e4743`
- 감사 나눔 날짜 기본값을 UTC → KST 로 통일 `01e4743`
- 나의기록 탭에서 '전체 기록' 삭제, 3개 칩으로 정리 `65687dc`
- 알림설정의 '앱 설치 안내' 삭제, '앱을 닫아둬도 오는 알림' → **'새 글 알림'** `d474d2b`
- 오늘말씀에서 절을 누를 때 저장되는 구절명 오류 수정 (`요한1서 7장` → `요한 1장` 으로 잘리던 문제) `65687dc`

---

## 4. 현재 상태 스냅샷 (2026-08-07)

| 항목 | 값 |
|---|---|
| Firestore 문서 크기 | **54.8 KB** / 한도 1024 KB |
| 공지 | 21건 (본문이 저장된 것은 관리자 작성 2건뿐) |
| 통독 플래너 | `요한복음` 3장 예정, active |
| 라이브 앱 버전 | `2026-08-07 00:06` |
| 최신 커밋 | `8540a3a` |

---

## 5. 아직 안 된 것 / 다음 할 일

### 🔴 남은 개선 (권장 순서)
1. **페이징(무한 스크롤)** — `/api/meditations` 가 여전히 전체를 반환.
   지금은 '변경 있을 때만' 받으니 급하지 않지만, 글이 수백 건 쌓이면 한 번에 받는 양이 커진다.
   최신 20건씩 끊어 오기 권장.
2. **Firestore 문서 분리** — DB 전체가 문서 1개인 구조는 그대로. 근본적으론 컬렉션 분리가 맞다.

### ⚠️ 사용자 조치 필요 (내가 할 수 없음)
- **`GEMINI_API_KEY` 가 Cloud Run 에 설정 안 됨** → 성경Q&A 동작 불가.
  현재 `src/App.tsx` 의 `SHOW_QNA_TAB = false` 로 **탭을 숨겨 둔 상태**. 키를 넣으면 `true` 로 바꿀 것.
- **푸시 알림 실기기 검증** — 서버 로직만 확인함. 아이폰은 홈 화면 설치 필수(iOS 16.4+).

### 📌 사용자에게 알려두고 답 못 받은 것
- 말씀 체크 목록에 없는 장 4건 (`요한1서 7:2, 7:3, 10:2, 10:4`) — 실제로는 5장 구절.
  고치면 중복이 생겨서 손대지 않음.
- 요한1서 6장·7장 공지는 없는 장이지만 **사용자 묵상 글이 붙어 있어 남겨둠.**

---

## 6. 작업 방법 메모

```bash
# 로컬 실행 (프론트+서버 한 번에)
npm run dev            # tsx server.ts → http://localhost:3000

# 프론트 변경 후 (서버는 dist 를 서빙)
npx vite build

# 타입 체크
npx tsc --noEmit

# 배포: main 에 push 하면 Cloud Run 이 자동 빌드 (2~3분)
git push origin main
```

**배포 완료 확인법** — 번들에 방금 넣은 문자열이 들어갔는지 확인:
```bash
js=$(curl -s "https://cho-1006806795339.asia-northeast3.run.app/" | grep -o 'assets/index-[A-Za-z0-9_-]*\.js' | head -1)
curl -s "https://cho-1006806795339.asia-northeast3.run.app/$js" | grep -c "찾을문자열"
```

**커밋 메시지에 한글·따옴표가 들어갈 때** — 인자로 넘기면 깨지므로 파일로:
```bash
git commit -F 메시지파일.txt
```

**⚠️ 프로덕션 데이터로 테스트했으면 반드시 정리할 것.**
이번 세션에서 테스트로 만든 말씀 체크·통독 북마크를 매번 원복했음.
특히 `/api/gratitudes/create`, `/api/meditations/create`, 리액션은 **전 교인에게 푸시 알림이 나간다.** 테스트 금지.
안전한 저장 트리거는 `/api/bible-progress` (푸시 없음).

---

## 7. 파일 지도 (이번 세션에서 만진 곳)

| 파일 | 역할 |
|---|---|
| `server.ts` | 전부. `withVerseText`, `stripStorableVerseText`, `getNextChapterInOrder`, `prepareTomorrowNotice`, `/api/revision` |
| `src/lib/revision.ts` | **신규** — 변경 확인 공용 구독기 |
| `public/sw.js` | 서비스워커. HTML network-first, 성경 전용 캐시 `bible-text-v1` |
| `index.html` | 새 버전 적용 시 자동 새로고침 |
| `vite.config.ts` | `__BUILD_TIME__` 주입 |
| `src/components/DailyNotice.tsx` | 오늘말씀. 정렬·구절 체크 저장 |
| `src/components/BibleReader.tsx` | 성경통독. 이전/다음 권 이동, 정렬 |
| `src/components/FormattedBibleText.tsx` / `DualBibleText.tsx` | 절 렌더링 (**절 번호 건드리지 말 것**) |
| `src/components/MyMeditations.tsx` | 나의기록. 목표 진행률, 말씀 체크 |
| `src/components/NotificationSettings.tsx` | 알림설정. 하단에 앱 버전 표시 |
